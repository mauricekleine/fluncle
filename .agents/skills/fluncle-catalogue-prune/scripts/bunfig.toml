# These scripts hit PRODUCTION Turso with `op`-resolved credentials and DELETE rows. Their tests
# must never reach the network. `[test] preload` runs ONLY under `bun test`, so a real prune run is
# untouched. See packages/test-support.
[test]
preload = ["@fluncle/test-support/preload"]
