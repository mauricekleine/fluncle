{
  "name": "@fluncle/extension",
  "version": "0.1.1",
  "private": true,
  "description": "Fluncle Lens — surface the findings hidden across the web.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "bun run ./scripts/build.ts",
    "bundle": "bun run ./scripts/bundle.ts",
    "dev": "bun run ./scripts/build.ts --watch",
    "icons": "bash ./scripts/make-icons.sh",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@fluncle/contracts": "workspace:*",
    "@fluncle/test-support": "workspace:*",
    "@types/bun": "catalog:",
    "@types/chrome": "~0.2.2",
    "typescript": "catalog:"
  }
}
