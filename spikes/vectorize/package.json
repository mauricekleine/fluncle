{
  "name": "@fluncle/spike-vectorize",
  "version": "0.0.0",
  "private": true,
  "description": "THROWAWAY de-risk spike — Cloudflare Vectorize latency + recall vs Turso exact cosine. DO NOT MERGE.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "gen:tracks": "bun run gen-corpus.ts --index tracks",
    "gen:centroids": "bun run gen-corpus.ts --index centroids",
    "load": "bun run load.ts",
    "recall": "bun run recall-ab.ts",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260719.1",
    "@types/bun": "~1.3.14",
    "typescript": "~7.0.2",
    "wrangler": "~4.110.0"
  }
}
