# Track Lifecycle

Canonical architecture for what happens to a track from the moment Fluncle finds it. This is a **stable reference** — the durable contract the surfaces and agents build against. (Living, changeable plans live in [ROADMAP.md](./planning/ROADMAP.md); this is not that.)

The shape in one line: **adding a track is fast and synchronous; enriching it is slow and asynchronous.** A find is listenable the instant it's added; everything expensive (audio analysis, the video, the spoken observation) arrives a little later, written back by an agent. **The async work is pulled, not pushed:** each enrichment step is a self-healing queue an agent cron drains, so a failed or never-run step is simply picked up on the next tick — nothing blocks the add, nothing stays stuck.

## Why two phases

`apps/web` runs as a Cloudflare Worker — HTTP only, no `ffmpeg`, tight CPU/time limits. Audio analysis and video rendering need `ffmpeg` + real compute, which can only run off-Worker (locally, or on an agent runtime). So the work splits cleanly along that line, and the constraint becomes the design.

## Phase 1 — Add (synchronous, in the Worker)

Triggered by `fluncle admin tracks publish <spotify-url>` → `POST /api/admin/tracks` → `publishTrack()` (`apps/web/src/lib/server/publish.ts`). Everything here is cheap HTTP, so the track is in the playlist and visible within one request:

- **Spotify** metadata: title, artists, album, artwork, `duration_ms`, `isrc`, `popularity` (`spotify.ts`).
- **Deezer** (by ISRC, best-effort): record `label` + a live `preview_url` (`deezer.ts`). This is a fallback pointer for `/api/preview`, not a durable media artifact.
- **Discogs** (read-only, best-effort): resolve the finding to its Discogs release id (`in_release_id` + `in_master_id`) by artist + title — Discogs has no ISRC search (`discogs.ts`). The `discogs.com/release/{id}` URL becomes a per-finding `sameAs` on `/log`. No-ops without `DISCOGS_USER_TOKEN`; a miss never blocks the add.
- **Log ID**: the permanent Galaxy coordinate, generated and stored (`log-id.ts`). This is the cross-surface marker — `fluncle://<id>` — used everywhere (web `/log/<id>`, SSH, CLI, TikTok). It supersedes any ad-hoc post marker. When Spotify omits the ISRC, the add flow looks it up on Deezer (search by artist + title, duration-confirmed) **before** the coordinate is computed, so the hash seeds from the recording's real identity; the Spotify id remains the last-resort seed.
- Publish to the Spotify playlist + Telegram.
- **Push** (best-effort, gated): after the Telegram post lands, `publishTrack` fires `notifyNewFinding` (`push.ts`) so the mobile crew gets a notification a fresh banger is live. Same side-channel discipline as the Last.fm love and the edge-cache purge — fire-and-forget on `waitUntil`, never throws, never blocks the add, and a **NO-OP until `EXPO_ACCESS_TOKEN` is set** (the mobile push backend). A new mixtape fires the same way on its `distributing→published` flip (`mixtape-social.ts`, transition-guarded so the per-platform finalize can't double-notify).

### The log page (`/log/<id>`)

Every coordinate-bearing finding has a permanent web surface at `https://www.fluncle.com/log/<log-id>` — the archival-plate register: server-rendered definitional prose (the coordinate as subject, in both the bare and `fluncle://` forms), the decode, logbook fields, the finding's footage, and `MusicRecording` JSON-LD carrying the Log ID as two `identifier` PropertyValues. The full enumeration lives at `/log` and in `sitemap.xml` (per-finding `lastmod` from `coalesce(updated_at, added_at)`). The cinematic register of the same entry is the Stories dialog over the home feed (route-masked to the same URL); `/stories/<id>` 301s here. **No Log ID → no log page**: a straggler shows a bare `#NN` in the feed until its coordinate is backfilled (below).

The track is now real, listenable, and shows its identity (coordinate, label, duration). The audio-derived fields are not set yet — they show as "processing" until Phase 2 lands.

## Phase 2 — Enrich (asynchronous, queue/cron-pulled)

After a successful add the Worker does **nothing else**: it sets `enrichment_status = pending` and returns. **There is no on-add push.** Every enrichment step is pulled asynchronously by an agent cron that drains the step's queue (see [The async pipeline](#the-async-pipeline--self-healing-queues-no-push) below). The agent has what the Worker can't — the admin Fluncle CLI, and (for the heavy steps) `ffmpeg` + Remotion — but holds **no platform secret**: R2, Postiz, Cartesia, Firecrawl, and Turso all live on the Worker, and the agent reaches them only through the authenticated admin API with just its admin token. The work a finding moves through:

1. **Analyzes the audio** — BPM, musical **key** (+ confidence), and spectral features (brightness, sub-bass weight, mid-band flatness) via the DSP pipeline (`packages/video/src/pipeline/analyze-audio.ts`), reading the **captured full song** (`source_audio_key`) when the capture side-channel has landed it, else the 30s **preview** — permanently, not just for a transition (the enrich queue itself is capture-independent, it just prefers the better source when one exists). It also records **analysis provenance** — `analyzed_from` (`full` \| `preview`), `bpm_source`/`key_source`, and the two confidences — so a preview-grade estimate is distinguishable from a full-song one. This closes the **capture→enrich race**: capture and enrichment are independent self-healing queues, so a finding whose enrich tick fires _before_ its capture lands is analyzed from the preview, permanently — unless something re-derives it. Now the capture sweep, on landing a full song, re-queues enrichment whenever the row was not analyzed from full audio (`analyzed_from != "full"`, NULL legacy rows included), so the next tick re-derives BPM/key from the full song. The feature vector is internal creative fuel for the video agent (the vibe-placement model it once seeded is retired — audio can't learn the manual map; the automatic sonic grouping now comes from the embedding below — see "Vibe placement").
2. **Embeds the audio** — a 1024-d **MuQ** audio embedding, computed on the box by the `fluncle-embed` cron (torch/MuQ) and written back through `fluncle admin tracks update <id> --embedding-file`. It is stored **twice, in one write**: as a JSON array in `embedding_json` (the source of truth) and as a native libSQL **`F32_BLOB(1024)`** in `embedding_blob` — the form the **database** can rank. This is the automatic **sonic-similarity** space: `get_similar_findings` ranks it to power the `/log` "more like this" row, the `/mix` rail scores its sonic term from it, and the operator-named **sonic galaxies** (the browse-by-feel lens, and later the game rendering those galaxies as spatial regions) are k-means clusters over the same space, assigned nightly by the `fluncle-cluster` cron (see [cluster-engine.md](./agents/cluster-engine.md)). Independent of analysis — its own self-healing queue (`embedding_json IS NULL`; once capture ships, gated on `source_audio_key` too, per the RFC).

   **The ranking happens IN SQL, never in the isolate.** Every similarity read issues one `order by vector_distance_cos(embedding_blob, ?) limit N` and gets back only the winners (~2.5 KB). The old shape — select every row's `embedding_json`, cosine-rank in the Worker — is dead: a 1024-d vector is **21,804 B** as JSON, so the unpaginated candidate scan hit `turso dev`'s **10 MiB response cap at 460 embedded findings** (a this-year wall, not a 100k one) and was on course to OOM the 128 MB Worker isolate in production (689 MB of JS heap at 100k). Three rules carry the new path, all measured: **bind the probe vector as a raw BLOB, never as a JSON string** (26,700 ms → 1,883 ms at 100k on hosted — a cliff that does **not** reproduce in local dev, so nothing but the code comment will warn you); pre-filter with a btree before the scan where the query allows one (the galaxy lens does — 274 ms at 100k); and **never** `CREATE INDEX … libsql_vector_idx` on a populated table (it wedged hosted Turso's write path for 20+ minutes and silently builds an empty index locally — there is no ANN index here, by decision). `embedding_json` stays as the source of truth and the backfill source; `scripts/backfill-embedding-blob.ts` runs on every deploy (`db:backfill`, before `wrangler deploy`) so a blob always exists by the time the new code serves.

3. **Stores the exact analyzed 30s preview** through `fluncle admin tracks preview` into the PRIVATE `fluncle-source-audio` bucket at `<logId>/preview.<ext>`, beside the captured full song — private analysis/model-training input, not public media and not a playback source, and never world-served (REF-05 moved it off the public `fluncle-videos` bucket, where it had been a copyright exposure). (The **full song**, once the capture side-channel lands it, lives in the SAME private bucket keyed by `source_audio_key`, and is what enrichment and the embedding read in preference to the preview.)
4. **Renders the video** (the `packages/video` kit) and uploads the bundle with `fluncle admin tracks video`. The CLI uploads each artifact **directly to R2** via short-lived presigned PUT URLs the Worker signs (`POST .../video/uploads` → PUT to R2 → `POST .../video/finalize`), so large cuts bypass Cloudflare's ~100MB edge body limit; the **Worker** signs the URLs and sets `video_url` to the review cut, and never hands R2 credentials to the agent. (A small bundle can still use the legacy single multipart `POST /api/admin/tracks/:id/video`.)
5. **Writes the analysis back** through `fluncle admin tracks update <id>` — `bpm`, `key`, `features`, and the provenance (`--analyzed-from`, `--analyzed-at`, `--bpm-source`/`--key-source`, `--bpm-confidence`/`--key-confidence`) — and flips `enrichment_status` to `done`.

> Two-master model: the bundle stores a clean square `footage.mp4` source + a portrait baked-text `footage.social.mp4`, deriving every other orientation/audio variant on the fly via Cloudflare Media Transformations rather than storing it. The full model, surface map, and one-time migration are specced in [video-variants.md](./video-variants.md). The transition is **per-finding and gradual**: a finding adopts the new layout only once its square ships and stamps `video_squared_at`; until then it serves the legacy single-file bundle. So the descriptions below hold for both — surfaces read the signal and fall back.

This takes a while, and that's fine: the find was already live. When it finishes, the analysis fields and the video appear across the Galaxy.

## The async pipeline — self-healing queues (no push)

Phase 2 is not one monolithic run — it is a set of **independent steps**, each a self-healing queue an agent drains on a schedule. Every step has the same shape:

- **A "ready and not done" queue query** (on `list_tracks_admin` / `listTracks`). **Dependencies live in the query** — a step's prerequisites _are_ its query's conditions, so there is no orchestrator: each step pulls exactly the work that's ready for it. (Audio analysis: `enrichment_status ∈ {pending, failed, stale-processing}`. Observation: `context_note IS NOT NULL AND observation_audio_url IS NULL`. Video: `enrichment_status = done AND video_url IS NULL`.)
- **An idempotent worker**, keyed per finding (`enrich:${logId}`, `observe:${logId}`, …), so re-pulling an in-flight item is a no-op and an external cron can fire on a fixed interval safely.
- **A reliability marker** that separates _never-tried_ from _done_ from _tried-and-failed_ from _terminally-impossible_. A bare "value IS NULL" conflates the first two with the last, so a step with a genuine "no result possible" outcome carries a **status enum** (`enrichment_status`, `discogs_status`: `pending`/`done`/`failed`/`unmatched`); an always-retriable step uses a **success timestamp + null-retry** (`lastfm_loved_at`; observation and video read their own output URL). The query reads the marker to skip hopeless cases and not re-burn a rate-limited budget.

A run that dies mid-step leaves the marker un-advanced, so the next tick re-attempts it — nothing blocks, nothing stays stuck. The same machinery serves new finds (picked up on the next tick) **and backfills**: a backfill is not a throwaway script, it is the step's normal cron finding a non-empty queue.

The **enrichment** step is the worked example: its queue is every finding `pending` ∪ `failed` ∪ **stale `processing`** (a `processing` row whose `updated_at` is older than `ENRICH_STALE_PROCESSING_MS`, 30 min, or has none) — a `status: "queue"` filter on `listTracks`, surfaced by `fluncle admin tracks enrich --queue`. The on-box `fluncle-enrich` cron (a `--no-agent` job on Hermes, every 5 min) reads that queue directly, analyzes each finding on-box (`ffmpeg` + `bun`), and writes the result back via `fluncle admin tracks update` — no Worker re-fire. A new find lands at the schema default `enrichment_status = "pending"` (queue-eligible), so the cron picks it up on its next tick with no on-add push; the operator's board affordance re-queues a finding by PATCHing its status back to `pending`. (Distinct from the _video render_ backlog queue.)

**The dependency DAG.** From `add`, five steps run independently, in parallel — **audio analysis**, the **audio embedding** (MuQ), **Discogs** resolve, **Last.fm** love, and the **context note** (Firecrawl facts). Two steps wait on a prerequisite, expressed in their query: **video** waits on audio analysis (`enrichment_status = done`), and the **observation** waits on the **context note** (which is now its own step, distinct from the observation that consumes it). Manual vibe placement (the `/admin/tag` tool) was **retired** (the route is removed) — audio can't learn the map, so the grouping it fed moved to the automatic embedding clusters; the editorial `note` stays operator-owned (the auto-note only ever fills an empty one); Phase 3 publish stays operator-gated.

**Who runs what — the agent runtime (Hermes).** The agent runtime is the self-hosted **Hermes agent**: the cloud orchestrator (running `claude-sonnet-4.6`), which drains every queue via the `fluncle` CLI and either calls a Worker endpoint that does the work server-side or runs the light compute itself. Hermes is home for **everything** — the deterministic vendor steps (Last.fm, Discogs, context-note Firecrawl, all Worker-side), **the spoken observation** (Sonnet authors the recovered-audio script from the context note; the Worker renders it with Cartesia), and the light **audio analysis** (`ffmpeg` + JS DSP, live on Hermes as the on-box `fluncle-enrich` `--no-agent` cron — the image carries `ffmpeg` + `bun`). **Video render** (headless Chromium + WebGL) is too heavy for the Hermes box itself (2 vCPU / 4 GB), so it is the one step Hermes does not run in-process — instead the `fluncle-render` `--no-agent` cron **conducts** it (live 2026-06-24): it wakes a separate scale-to-zero box.ascii render box, which renders + ships one queued finding via a remote `claude -p`, then parks the box. This replaced the old Mac-bound desktop routine — renders no longer depend on the operator's laptop being open. Hermes holds no vendor key and no operator power (the agent ceiling: reversible / internal / no public footprint); publish-class stays operator-only. Each step writes back through the generic admin update path, then feeds whatever step's query it now satisfies. The Hermes orchestration (crons, role tiers) lives in [docs/agents/hermes-agent.md](./agents/hermes-agent.md) + the cron roster at [docs/agents/hermes/cron/](./agents/hermes/cron/).

## The update path

Phase 2 needs to write back, and the operator needs to curate — so tracks are mutable after add, through one generic, admin-only command:

```
fluncle admin tracks update <track_id> --bpm <n> --key "<k>" --features <json> --video-url <url> ...
```

Backed by `PATCH /api/admin/tracks/:id`. It writes an **allow-list of curation/enrichment fields only** — `bpm`, `key`, `features`, `video_url`, `enrichment_status`, `note`, and `galaxy_id` (the sonic-galaxy assignment written by the `fluncle-cluster` cron). Identity fields (title, artists, Spotify ids, Log ID) are **immutable once set** — they come from Spotify and never change. The one sanctioned exception is the straggler repair: `isrc` and `logId` accept a **one-time backfill into a null slot** (`logId: "auto"` derives the coordinate the add flow would have minted from the found date + identity); a write against an already-set value is rejected. Every public content update bumps `updated_at`; internal preview archive writes do not, because sitemap/log lastmod should reflect visible changes only.

**BPM/key follow a source hierarchy — `operator` > `rekordbox` > DSP — enforced server-side in `updateTrack` from the authenticated tier (never the request body).** An agent-tier (DSP) write of `bpm`/`key` onto a row whose `bpm_source`/`key_source` is already `rekordbox` or `operator` is silently dropped for that value (the rest of the same write still applies) — the DSP key estimator is weaker than a DJ-graded Rekordbox key, so an overwrite would be a regression, not an upgrade. An operator write always wins, and a hand-set value with no explicit source is stamped `operator` so it is durably protected from later DSP passes.

## Preview playback vs. preview archive

Public playback is live-only. `/api/preview/:idOrLogId` tries the stored Deezer `preview_url`, refreshes Deezer by ISRC if that token died, then falls back to iTunes; it never reads archived R2 previews and sends `Cache-Control: no-store` so the relay does not become an edge-cached media tier. The archive stores exactly one official 30s preview per finding in the PRIVATE `fluncle-source-audio` bucket at `<logId>/preview.<ext>` (beside the captured full song), for private analysis only — never world-served (REF-05 moved it off the public `fluncle-videos` bucket). New enrichments archive the official 30s preview; the feature vector itself is computed from the **captured full song when present, else that preview** (Phase 2 step 1). Backlog backfill archives a freshly resolved official preview, which is best-effort and may differ from the bytes an older enrichment analyzed. Archive metadata stays internal (`preview_archive_*`) and is not part of `/api/tracks`; the captured full song (`source_audio_key`, the same private bucket) is likewise internal — never public playback, never `/api/tracks`.

## Grouping: the sonic galaxies (vibe placement retired)

Findings are grouped by **feel**, not sub-genre. There's no finite, agreed drum & bass sub-genre taxonomy, and an absolute label is an argument; a _relative_ placement is answerable. The first cut of this was manual: the operator dropped each finding on a 2D map (energy × mood) in the admin tagging tool, storing a coordinate (`vibe_x`/`vibe_y`) whose quadrant named the finding's galaxy. A signal test (n=45) settled it — audio **can't** learn the manual placement (the mood axis lands at coin-flip; the browse-by-feel appendix), so the `features_json` training-signal plan is dead, the `/admin/tag` tool is removed, and the manual coordinate is gone: `vibe_x`/`vibe_y` were DROPPED (migration `0063`) once it turned out the retired coordinates were still being read — they were leaking into the observe/note authoring prompts and driving a phantom "needs tagging" badge in the admin sidebar. `galaxyForVibe` / `apps/web/src/lib/galaxies.ts` are gone. A finding's placement is now the sonic galaxy the cluster cron assigns.

The grouping now comes for free from the **audio embedding** (`embedding_json`), and the galaxies stopped being fiction. The operator-named **sonic clusters** (k-means over the MuQ embedding space, assigned nightly by the `fluncle-cluster` cron into `tracks.galaxy_id`) **are** the galaxies now — the four canon galaxies became the real, sound-derived map rather than a hardcoded quadrant constant. Every embedded finding gets a real galaxy, old and new alike; the `/log` "more like this" row is the same space at nearest-neighbour range, and the public browse-by-feel lens at `/galaxies` is the map (dark behind a launch gate until the operator has named the whole map). See [cluster-engine.md](./agents/cluster-engine.md) for the engine.

## Phase 3 — Publish (per-platform)

Once a track has a video in R2, it can go to social platforms — but **a push is not one thing: what it DOES differs per platform, and the table below is the contract.** TikTok lands a private (`SELF_ONLY`) inbox draft the operator finishes in-app (the official sound attaches only there). **YouTube Shorts is a DIRECT PUBLIC UPLOAD on the push** — no draft, no staging, no human gate between the command and the public feed. Instagram is manual-only for a finding. So: a YouTube push IS publishing. Everything goes through **Postiz** (one integration that speaks TikTok / Instagram / YouTube), so the Worker holds a single `POSTIZ_API_KEY` instead of per-platform OAuth.

The deciding question per platform is: **can the API carry the caption, the cover, and the audio?** A platform needs the manual inbox/draft hand-off **only when it can't** — which makes TikTok the exception, not the template. If direct post carries all three, we push directly (or schedule) and skip the draft entirely.

| Platform                            | Cut pushed                              | Caption               | Cover                            | Audio                                                                                                                               | Flow                                                                                                                                                                                                                     |
| ----------------------------------- | --------------------------------------- | --------------------- | -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **TikTok** (live)                   | `footage.social.mp4` + `audio=false` MT | ✗ inbox drops it      | ✗ set in-app                     | ✗ licensed sound attaches only in-app                                                                                               | **Draft → manual finish.** Lands in the @fluncle app **inbox** (`SELF_ONLY`); the operator pastes the caption from `note.txt`, sets the cover, adds the official sound, and publishes natively.                          |
| **YouTube Shorts** (live)           | `footage.social.mp4`                    | ✓ title + description | ✓ custom thumbnail (`cover.jpg`) | ✓ the video's own (baked-in) audio                                                                                                  | **Direct public upload** (Data API v3) on the push — title/description/thumbnail all carry. Content ID typically _claims_ (rights holder monetizes, we don't) rather than blocks short clips — accepted. No in-app step. |
| **Instagram Reels** (not automated) | —                                       | —                     | —                                | ✗ a baked-in master gets **muted/removed** on a business/creator account; IG's licensed library is app-only and locked for business | **Manual, in-app only.** No legitimate API path that respects the music, so the pipeline doesn't post to Instagram — any IG presence is a hand-made post.                                                                |

So TikTok keeps the `draft` step because the inbox is the only way a human can supply the caption, cover, and licensed sound; **YouTube posts directly and publicly** the moment the operator pushes (its API carries caption + cover + the video's own audio; Content ID may claim it — we accept that, the goal is reach not revenue). **Instagram is deliberately left out**: baking the master into a Reel gets muted/removed on our account type, and there's no API route to its licensed audio, so there's no clean automated path — Instagram is a manual in-app post (see the `fluncle-publish` skill).

```
fluncle admin tracks draft  <track_id|log_id> --platform tiktok       # TikTok: inbox draft
fluncle admin tracks draft  <track_id|log_id> --platform youtube      # YouTube: direct public Short
fluncle admin tracks social <track_id|log_id> --platform tiktok --status published --url <url>
```

`POST /api/admin/tracks/:id/social/:platform/draft` is the single push endpoint, branching by platform (`tiktok`, `youtube`): both push the portrait baked-text social cut — under the two-master layout (`video_squared_at` set) that is `footage.social.mp4` (TikTok as an `audio=false` Media Transformation URL so the operator attaches the licensed sound in-app; YouTube as-is). A legacy finding (no signal yet) still pushes its `footage.mp4` (the old portrait+text cut) and `footage-silent.mp4`, so un-migrated tracks are unaffected. TikTok lands in the app inbox (status `draft`); YouTube uploads as a Short with the track title + `cover.jpg` thumbnail, directly and publicly (status `published`). (The `draft` verb in the path is TikTok-shaped; for YouTube the same call publishes.) Postiz doesn't return the public post URL on create, so the operator fills it (or marks `failed`) afterward via `PATCH …/social/:platform`, or from the `/admin` board. This is **not** part of the enrichment workflow — it's a separate capability (the `fluncle-publish` skill); the Hermes runtime chains enrich → video → publish across its queues, but they're distinct steps.

## Per-platform publication (`social_posts`)

Publication state is **per platform**, separate from the track's own pipeline (which tops out at "video in R2"). One row per `(track, platform)`:

| Field                   | Notes                                                       |
| ----------------------- | ----------------------------------------------------------- |
| `platform`              | `tiktok`, `youtube` (Instagram is manual, not tracked here) |
| `status`                | `draft` → `scheduled` \| `published` (or `failed`)          |
| `external_id`           | the Postiz post id (for later reconciliation)               |
| `url`                   | the public post URL — set by the operator when published    |
| `scheduled_for`, `*_at` | timing                                                      |

`status` reflects the per-platform flow: a TikTok inbox push sits at `draft` (not yet public — the operator finishes it in-app); a YouTube push posts directly, landing straight at `published` (with no `url` until the operator records the public link).

There is no per-track "published" flag — a track's reach is the union of its `social_posts`. Telegram stays its own auto-post boolean on `tracks` (a different, no-review model) for now.

## Data model (the enrichment fields)

On the `tracks` table, beyond Phase-1 identity/metadata:

| Field                                                                                          | Set by                                        | Notes                                                                                                                                                                                                                                                                                                                                                                                        |
| ---------------------------------------------------------------------------------------------- | --------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `log_id`                                                                                       | Phase 1                                       | permanent Galaxy coordinate; the cross-surface marker                                                                                                                                                                                                                                                                                                                                        |
| `isrc`, `label`, `preview_url`, `popularity`, `duration_ms`                                    | Phase 1                                       | cheap HTTP enrichment                                                                                                                                                                                                                                                                                                                                                                        |
| `in_release_id`, `in_master_id`                                                                | Phase 1                                       | read-only Discogs lookup (best-effort, by artist + title — Discogs has no ISRC search); the `discogs.com/release/{in_release_id}` URL is a per-finding `sameAs` on `/log`. Inert (null) until a confident match writes them; no-ops without `DISCOGS_USER_TOKEN`. See [lib/server/discogs.ts].                                                                                               |
| `preview_archive_key`, `preview_archive_source`, `preview_archive_mime`, `preview_archived_at` | Phase 2 / backfill                            | the analyzed official preview in the PRIVATE `fluncle-source-audio` bucket at `<logId>/preview.<ext>` (REF-05 moved it off the public `fluncle-videos` bucket); internal, not playback                                                                                                                                                                                                       |
| `source_audio_key`, `capture_status`                                                           | Phase 2 (capture side-channel)                | `source_audio_key` = R2 key in the private `fluncle-source-audio` bucket for the captured FULL SONG (presence = captured); the archived 30s preview lives beside it at `<logId>/preview.<ext>`. `capture_status` = `pending`/`done`/`unmatched`/`failed` (notNull default). Non-blocking side-channel — never gates enrich/embed; readers prefer it over the preview.                        |
| `source_audio_captured_at`, `source_audio_attempted_at`, `source_audio_failures`               | Phase 2 (capture side-channel)                | capture bookkeeping: `_captured_at` stamps when bytes landed; `_attempted_at` + `_failures` drive the retry backoff/cap on a `failed`/`unmatched` attempt (the Discogs/Last.fm Retry-After discipline). Internal state — writing them doesn't bump `updated_at`.                                                                                                                             |
| `bpm`, `key`                                                                                   | Phase 2 (agent)                               | audio-derived; key carries a confidence the agent may gate on                                                                                                                                                                                                                                                                                                                                |
| `analyzed_from`, `analyzed_at`, `bpm_source`, `key_source`, `bpm_confidence`, `key_confidence` | Phase 2 (agent)                               | BPM/key analysis provenance: `analyzed_from` (`full` \| `preview`; NULL legacy = assume preview-grade) drives the capture→enrich re-derive; the sources/confidences record where each value came from + how sure the analyzer was. Internal — never in a public DTO (stripped by `toPublicTrackListItem`), writing them doesn't bump `updated_at`.                                           |
| `features_json`                                                                                | Phase 2 (agent)                               | spectral vector; internal creative fuel for the video agent (the vibe auto-placement it once seeded is retired — see the `embedding_json` row)                                                                                                                                                                                                                                               |
| ~~`vibe_x`, `vibe_y`~~                                                                         | DROPPED (migration `0063`)                    | The energy × mood coordinate from the removed `/admin/tag` tool. DROPPED: nothing wrote them, but stale coordinates were still being READ into the observe/note prompts and a phantom admin "needs tagging" badge. Superseded by `galaxy_id`.                                                                                                                                                |
| `galaxy_id`                                                                                    | Phase 2 (`fluncle-cluster` cron)              | the finding's sonic galaxy — FK to `galaxies.id`, assigned nightly by the `fluncle-cluster` cron (k-means over `embedding_json`). Internal grouping like the embedding; present only once embedded and assigned. Member counts are derived (`COUNT(*) GROUP BY galaxy_id`), never a stored count. Writing it doesn't bump `updated_at`. See [cluster-engine.md](./agents/cluster-engine.md). |
| `video_url`                                                                                    | Phase 2 (agent)                               | R2-hosted MP4 (`<log-id>/footage.mp4`); the legacy portrait+text cut, or (once squared) the clean square crop source                                                                                                                                                                                                                                                                         |
| `video_squared_at`                                                                             | Phase 2 / backfill                            | the two-master layout signal ([video-variants.md](./video-variants.md)): set when the SQUARE `footage.mp4` ships (portrait `footage.social.mp4` alongside); presence flips archive surfaces to MT crops + publishes from the social cut, absent = the legacy single-file layout                                                                                                              |
| `context_note`                                                                                 | Phase 2 (agent)                               | firecrawl-derived FACTS (label/year/release); internal creative fuel, never on `/log`, never quotes lyrics. Writing it alone doesn't bump `updated_at`.                                                                                                                                                                                                                                      |
| `observation_audio_url`, `observation_duration_ms`, `observation_generated_at`                 | Phase 2 (agent)                               | Fluncle's spoken field observation (R2 `observation.mp3`; the `_url`'s presence is the "has observation" flag). See [observation-agent.md](./agents/observation-agent.md).                                                                                                                                                                                                                   |
| `enrichment_status`                                                                            | both                                          | `pending` (after add) → `processing` → `done` \| `failed`; a `processing` row older than `ENRICH_STALE_PROCESSING_MS` is re-queued (stale-processing, see above)                                                                                                                                                                                                                             |
| `updated_at`                                                                                   | every write path that touches a VISIBLE field | last real content change; sitemap `lastmod` reads `coalesce(updated_at, added_at)`. Internal fuel writes (`features_json`, `context_note`, `embedding_json`) don't bump it.                                                                                                                                                                                                                  |
| `embedding_json`                                                                               | Phase 2 (agent, `fluncle-embed`)              | 1024-d MuQ audio embedding as a JSON array — the source of truth, and what `list_track_embeddings` (the `fluncle-cluster` corpus read) serves. Internal analysis fuel like `features_json`; writing it doesn't bump `updated_at`. It is **82% of the database** at 100k rows; dropping it once the blob path has proven itself in production is the recorded follow-up.                      |
| `embedding_blob`                                                                               | Phase 2 (agent, `fluncle-embed`)              | the SAME vector as a native libSQL `F32_BLOB(1024)` (4,096 B) — the form the database ranks with `vector_distance_cos`, written in the same statement as `embedding_json` and backfilled from it on every deploy. Every similarity read (`get_similar_findings`, the `/mix` rail, a galaxy's core-first order) ranks against this column and never loads a vector into the Worker.           |

### The cost ledger (`cost_events`)

Enrichment steps also emit into the append-only **`cost_events`** ledger (COST-01) — one row per billable unit of work spent on a finding (or a non-finding step), NEVER pruned. Each row carries `costBasis` (`cash` = real incremental money — OpenRouter distil, Cartesia TTS, Firecrawl, Resend; `subsidized` = a fixed-plan draw — subscription LLM tokens + on-box compute) orthogonal to `source` (`measured` vs `estimated`), plus the step/vendor/unit, a `quantity`, and a nullable `estimatedUsd` (null = unpriced, never $0). Two write paths: **Worker-local** best-effort `insertCostEvents()` at the in-process vendor call sites (context-distil, Cartesia, Firecrawl, Resend — `cash`), and the agent-tier idempotent **`record_cost`** POST for box-side numbers. The capture is always best-effort — a ledger failure can never break the enrichment step it rides alongside. The operator reads it at `/admin/usage` (cash and subsidized in separate columns, never summed); rates for the no-dollar vendors live in the editable `apps/web/src/lib/server/cost-rates.ts`.

## Surfacing

Surfaces show a field only when it's present, and a track whose `enrichment_status` is `pending` reads as still being analyzed (in the recovered-log register, per VOICE.md — e.g. "Still listening…", not a sterile spinner). Once enrichment lands, BPM, key, the video, and the audio observation all appear. The music never waits on any of it.
