# Audit backlog — the nightly auditor's open findings

The **nightly `fluncle-audit` sweep** files a finding here when it chooses not to fix it itself — the high-impact, high-risk, cross-cutting, or canon-touching ones (see the auditor's operating contract in `docs/agents/hermes/scripts/audit/prompts/_preamble.md`). It is **machine-appended and committed as part of each night's PR**, so a filed finding survives the PR's merge instead of being buried in a squashed commit message.

**Forward-facing, open findings only — never a changelog.** Like `docs/planning/ROADMAP.md`, this ledger carries only what is still ahead: a resolved, rejected, or overtaken row is **deleted in the PR that settles it**, with the resolution named in that PR's body — git history keeps the analysis. A ruling that must outlive its row does not live here: write it into the canon doc or code comment it governs, then delete the row.

Keep it distinct from the human-owned list: `docs/planning/ROADMAP.md` is the planning backlog (non-canon per AGENTS.md). The operator promotes a row there when it is worth scheduling — a promoted item cites the row it came from — and the audit never writes to the roadmap directly.

## How it's maintained

- The **auditor** (1am) appends one row per finding it files tonight, most-severe first. It **dedupes**: before appending, it checks for an existing row with the same `domain` + `location` + gist and skips it rather than re-filing. It may also RESOLVE up to two open rows per night (the preamble's conversion rule): fix the thing in the night's PR and delete the row in the same commit.
- The **reviewer** (5am) may resolve a small filed finding by fixing it — deleting its row in the same PR.
- The **operator** resolves rows by acting on them (or promoting them to the roadmap), then deletes them; the deleting commit names what settled the row.

## Columns

`filed` (UTC date) · `domain` · `sev` (high/med/low) · `location` (`path:line`) · `finding` · `proposed_fix` · `ref` (PR/commit that filed it)

| filed | domain | sev | location | finding | proposed_fix | ref |
| ----- | ------ | --- | -------- | ------- | ------------ | --- |

<!-- The auditor appends rows below this line. Newest run on top. -->

| 2026-09-01 | voice | low | `apps/web/src/lib/server/voice-lint.test.ts:67` | **The voice lint parses JavaScript, so every NON-JS public asset is outside all three rails — and the extension's Chrome Web Store listing plus both of its screens are the half nobody had named.** The lint's header already records `apps/web/public/*.txt` (`llms.txt`, `humans.txt`) as a known gap it cannot reach, on the true reason: oxc parses JavaScript, not plain text. The same reason silently covers three more shipped public files that the comment did not name until tonight, all under `apps/extension`: `manifest.json:3,5` carries the extension's NAME and its one-line DESCRIPTION — the listing a stranger reads on the Chrome Web Store — and `src/popup.html` + `src/options.html` carry the wordmark, both `<title>`s, the options tagline, and the three field labels a reader meets BEFORE `options.ts` hydrates them (and permanently, if that script ever fails). Measured tonight: all five files are CLEAN under the three rails — no banned word, no prose em dash, no exclamation mark — so this is a latent hole, not a live defect, exactly the state `llms.txt` and the web manifest were in before `identity.test.ts` pinned them. Tonight closed the DRIFT half only: `apps/extension/src/copy.test.ts` (new) pins all eleven strings to `src/copy.ts`, whose own header claims to hold "every human-facing string in Fluncle Lens, in one place" and did not. That keeps the copies honest to each other; it runs no rail over what they SAY. | Build the text-asset scanner the header has called "a separate, worthwhile slice" twice, and give it the three rails the JS scan runs: banned words, prose em dash, exclamation marks. Plain text, HTML, and JSON in one pass — the extraction differs per format (a `.txt` is prose whole; HTML wants element text, not attributes, or `content="width=device-width"` fires rail (a) on every viewport meta; JSON wants a named field list, since a manifest is mostly machine keys). Filed rather than fixed because it is new DECISION LOGIC inside a build-gating test — how the net decides what it covers, not another entry in a coverage map — and because the extraction rules above are a judgment call per format that deserves review before it can fail a deploy. Scope it with the sibling gap already filed for the generated OpenAPI document (2026-08-09), which is the same shape from the other direction: prose the JS scan cannot see. | `audit/20260901-voice` |

| 2026-08-31 | design | med | `apps/web/src/routes/log.$logId.tsx:596` | **The "Close in sound" band is a track-shaped surface that carries no instrument readout, and it just shipped onto a second public page.** DESIGN.md §5's **Readout Rule** is written as a universal: "Every track-shaped surface — the Track Row, a chat Finding Card, a recommendation row, a picker candidate, any future card that names a track — carries the finding's instrument readout: the chip row (duration first, then BPM, then key) and the release year on its metadata line, wherever the data exists", and it closes the loophole explicitly: "A missing chip is a data gap upstream … never a layout choice." The band names a track and renders a cover plus one `Artist — Title` line, nothing else: `/log`'s `.log-similar-list` (`log.$logId.tsx:596-614`, styled `styles.css:1894`) and, since #1239, `/track/<trackId>`'s `SonicNeighbours` (`components/track-destination.tsx:248`, styled `styles.css:8881` as `.track-neighbours`). Every other track-shaped surface in the app obeys the rule through one shared helper — `TrackReadout` (`components/recommendations/rec-rows.tsx:17`) over `TrackChips` — which the recommendation rows, the front-door lead (`components/front-door/lead.tsx:13`) and the Track Row all render; these two bands are the only ones that do not, and nothing in either file records a reason. THE TWO HALVES ARE NOT THE SAME COST. `/log`'s `similar` is already `TrackListItem[]` (`lib/server/tracks.ts:1500`), which carries `bpm`, `key`, `durationMs` and `releaseDate` today — so that half is a component + CSS change with no data work at all. `/track`'s half is not: `SonicNeighbour` (`lib/server/track-page.ts:103`) is a five-field DTO (`albumImageUrl`, `artists`, `logId`, `title`, `trackId`), so the projection behind it has to grow before the chips can render, and it is a MIXED-register list — an unlit row's chips would be the first thing that band prints about a track Fluncle never certified. | An operator/canon ruling on ONE question, then apply it to both bands at once: is a compact neighbour list a track-shaped SURFACE (the rule binds, chips land) or a STRIP — the same object as the adjacent-galaxies strip beside it (`styles.css:2840`, `routes/galaxies.$slug.tsx:150`), whose whole job is a glance and a link? Both readings are defensible from the canon as written, which is why this is filed rather than fixed. (a) THE RULE BINDS — render `TrackReadout` on both bands (no new component; it already takes `bpm`/`durationMs`/`musicalKey` and drops what the row cannot back) and add the release year to the metadata line. `/log` needs only `log.$logId.tsx` + `.log-similar-*`; `/track` additionally needs the four fields on `SonicNeighbour` and on the query that fills it (`listSonicNeighbours`, `track-page.ts:462`, and the sonar path's `hydrateNeighbours`, `:588`), and a decision on whether an UNLIT neighbour prints chips at all — the Unlit Rule governs light, not data, so the default reading is that it does. (b) THE STRIP IS EXEMPT — amend the Readout Rule with the carve-out named and reasoned (a neighbour strip is a way ON, not a record to read), and say so in a comment on both bands so the next agent does not re-file this. Whichever wins, both bands must land together: they are the same component idea on two pages, and they already disagree with the rest of the app. | `audit/20260831-design` |
| 2026-08-31 | db-query-shape | med | `apps/web/src/lib/server/artist-dossier.ts:606` | **`rankArtists` still pays the full stale-set scan on every PRODUCTIVE tick, and `rank_catalogue` — the sweep it is the sibling of — proved that scan away.** `countStaleArtists` (`artist-dossier.ts:365`) counts `STALE_ARTISTS_INNER`: a `group by ta.artist_id` over `track_artists ⋈ track_embeddings` (the largest join table in the database — the repo's own scale rig sizes the 150k regime at ~225k edges, `docs/db-scale-backlog.md` Keystone 2) UNIONed with a correlated `not exists` evaluated once per `artist_centroids` row. The tick already ran the same subquery once as `STALE_ARTISTS_PAGE`, so a productive tick pays it TWICE. This PR removed the EMPTY-batch half (an empty page over `order by artist_id asc limit N`, N>0, proves the set is empty, so the count could only be 0) — the branch every settled tick takes. What remains is the NON-empty return at `:606`, which is every tick of a cold drain: `ceil(artists / limit)` ticks, each paying one extra pass over the largest join table. `rank_catalogue` retired exactly this as Wave 1 item 1 (`docs/db-scale-backlog.md` § Shipped, #847/#848) and its shape is in-tree: `remainingCatalogueRankWork` (`catalogue.ts:1374`) returns the `RANK_MORE_REMAIN = 1` SENTINEL on a full batch and `0` on a short one with NO scan, and takes the real count only behind an opt-in `countRemaining` flag for the human CLI readout. `remaining` is consumed the same way here — the contract calls it "the 'run me again' signal" (`packages/contracts/src/orpc/admin-artists.ts:532`) and the CLI comment says `remaining > 0` means run it again (`apps/cli/src/commands/admin-artists.ts:215`). NOT fixed here because it is not a same-file change: unlike `rank_catalogue`'s hoist, `rank_artists` has no `countRemaining` input to hang the exact count on, so shipping the sentinel alone would silently turn the number `apps/cli/src/cli.ts:7185` prints ("N still stale.") from a count into a 1 — an operator-facing meaning change — and adding the flag is a cross-surface edit (the oRPC contract's `.input()`, the handler, the CLI command + its flag, and the `verb_noun` naming check). | Mirror `rank_catalogue` op for op. (1) Add `count` to the `rank_artists` contract input (`packages/contracts/src/orpc/admin-artists.ts:534`) and thread it to `rankArtists(limit, countRemaining)`, exactly as `rank_catalogue` carries it; expose it as `--count` on `fluncle admin artists rank` so the human readout keeps the exact number on a deliberate manual run. (2) Give `rankArtists` a `remainingArtistRankWork` twin of `remainingCatalogueRankWork` (`catalogue.ts:1374`): `countRemaining` → the real count; else a full batch (`staleArtists.length >= bounded`) → the positive sentinel, a short batch → 0. Reuse `RANK_MORE_REMAIN`'s reasoning verbatim — a positive value is a signal, never a count. (3) The one thing to settle before shipping the short-batch → 0 arm, and the reason this wants a human: unlike `rankCatalogue`, which stamps the fingerprint on EVERY row in its batch (including a row that produced no winner) so a hopeless row can never be re-picked forever, `rankArtists` sends an artist whose blobs all fail `readEmbeddingBlob` down the ORPHAN branch — it deletes the centroid while the artist still credits embedded tracks, so the STALE arm re-picks it next tick. A short batch containing such an artist is therefore NOT drained, and `0` would stop the CLI's drain loop one tick early. Either stamp a terminal fingerprint on that row the way `rankCatalogue` does (the better fix — it also stops the artist looping forever) or keep the count on the short-batch arm and take the sentinel only on the full-batch one. No schema change and no index in any variant, so nothing here is gated on hosted Turso. | `audit/20260830-db-query-shape` |
| 2026-08-29 | tests | high | `docs/agents/hermes/scripts/audit-sweep.sh:24` | **The sweep-driver test sandboxes are not hermetic, and on the box that runs them they execute the REAL `claude` and hit the REAL Sentry API.** `audit-sweeps.test.ts:39` builds a fixture whose `claude`/`bun`/`gh`/`git` are temp-PATH stubs and prepends that directory to `PATH`. The three drivers then RE-prepend `/usr/local/bin` AHEAD of the caller's PATH — `audit-sweep.sh:24`, `audit-review-sweep.sh:18`, `sentry-triage-sweep.sh:39`, all identical and all commented "The runner execs with a minimal PATH; prepend the known install dirs so bun/claude/gh/git/fluncle resolve." On any host where `/usr/local/bin` holds a real one of those binaries, the real binary wins and the stub is dead. That host is exactly the Hermes box these sweeps ship to: here `command -v claude` → `/usr/local/bin/claude`, `bun` and `gh` likewise (`git` is `/usr/bin/git`, so only THAT stub still wins). Measured consequence on this box: `bun run test:scripts` — a `deploy:gate` leg (`package.json:56`) — exits 1 with **11 failures**, 8 in `audit-sweeps.test.ts` (the sweep invokes the real `claude`, which answers `Not logged in · Please run /login`, so `{ok:false, errors:1}` where the test expects `{ok:true, errors:0}`; the real `gh` ignores `STUB_AUDIT_PR_URL`, so `produced:0`) and 3 in `sentry-triage-sweep.test.ts:648` (the sweep's real outbound fetch gets `401 Invalid token` from sentry.io and aborts at `fetch-failed` BEFORE the claude call, so the guard test `the stub claude really ran — otherwise every assertion below is vacuous` correctly goes red — and note the sibling secret-leak assertions then pass VACUOUSLY over an empty env). Causality is proven, not inferred: neutralising ONLY line 24 in the fixture's own COPY of the script (the fixture already `copyFileSync`s it, so the repo is untouched) turns the same run into `{"ok":true,"errors":0,"produced":0}` — the expected value, nothing else changed. Two written claims are false on such a host: `audit-sweeps.test.ts:4` ("the suite cannot reach git remotes, GitHub, Claude, a package registry, or the run ledger") and `docs/agents/hermes/scripts/bunfig.toml` ("Their tests must never do the same" — the `[test] preload` no-network rail arms the bun-test PROCESS and cannot reach a `spawnSync`'d bash child). Prod is not currently blocked — a Cloudflare build container and the GitHub runner carry no `/usr/local/bin/claude`, which is why `main` still deploys — so the damage is that the audit system's own health tests are red-by-default on the box, on any operator machine with the Claude CLI installed there, and would silently mask a REAL regression in these drivers. | Make the fixture's stub directory unloseable, and do it in the DRIVERS rather than the tests — the tests assert the right thing and must not be weakened to accommodate the PATH. The prepend itself is load-bearing (the systemd runner really does exec with a minimal PATH), so the shape is to keep prepending while letting a caller win: give each of the three scripts one override read before the prepend, e.g. `export PATH="${AGENT_BIN_PATH:+${AGENT_BIN_PATH}:}/usr/local/bin:/root/.bun/bin:${PATH:-/usr/bin:/bin}"`, and have `fixture()` set `AGENT_BIN_PATH` to its `bin` dir alongside the `PATH` it already sets. Filed rather than fixed for three reasons the auditor may not settle alone: it edits the audit sweep's OWN drivers (the repo's guard hook states the rule for the adjacent case — "a sweep does not get to edit its own rails"), the change alters what the box's 01:00/05:00 timers resolve at runtime and a wrong `AGENT_BIN_PATH` in the unit env would silently redirect the real `claude`, and the naming/mechanism is a call the operator should make once for all three drivers plus any future one. Close the vacuity separately while there: `sentry-triage-sweep.test.ts`'s secret-leak assertions should assert `invoked` first (or share the guard's result) so they can never pass over an empty environment. | `audit/20260829-tests` |

| 2026-08-29 | tests | med | `package.json:66` | **Three workspaces are structurally outside `deploy:gate`, and nothing notices a fourth.** `deploy:gate` (`package.json:56`) reaches every package's suite through `bun run test` → `turbo run test` (`package.json:66`), and turbo runs the `test` task only for a workspace that DECLARES one (`turbo.json:33`). Three declare none, so their source is never executed by the deploy boundary at all: `packages/ui` (37 source files — the shared Shadcn primitives every `/admin` board and public page composes, `src/lib/utils.ts`'s `cn` included), `packages/media` (17 — the Remotion still renderers plus three PURE declarative spec tables, `src/remotion/app-icon-specs.ts`'s `APP_ICON_SPECS`/`MOBILE_ASSET_SPECS`, `src/remotion/socials-specs.ts`'s `SOCIAL_SPECS`, `src/remotion/mixtape-cover-specs.ts`'s `MIXTAPE_COVER_SPECS`), and `packages/sprites` (1 — `src/index.ts`'s `SPRITES` map, the `spriteUrl(ref)` builder and `SPRITE_PALETTE`). This is a DIFFERENT hole from the `.tsx` component row filed 2026-08-06: that one is about a missing DOM harness inside a package the gate already runs; this one is about packages the gate never enters, where two of the three have pure units that need no harness at all. The compounding half is that the absence is silent — the repo fails a build for an oRPC route with no contract (`orpc-coverage.test.ts:385`) and for a stale carve-out (`:399`), but a NEW workspace that ships without a `test` script joins these three with no signal anywhere. | Rule on the deploy boundary first, then build to the ruling — the same sequencing the 2026-08-06 row applies to the e2e suite ("a real build-time cost, so it is the operator's call"). (1) DECIDE whether `deploy:gate` should widen: enrolling a package makes every future Cloudflare build run its suite, and `turbo.json:33`'s `dependsOn: ["^build"]` means enrolling `packages/media` also pulls its Remotion dependency build into the gate's critical path. (2) If yes, `packages/sprites` and `packages/media` are then plain fixes — add `"test": "bun test"` and one colocated file each over the pure specs (every id unique, every declared size positive, `spriteUrl` round-tripping each `SPRITES` entry); `packages/ui` is NOT, because 37 components land straight on the unresolved DOM-harness ruling the 2026-08-06 row already owns — settle that row first and `ui` follows it. (3) Close the silence with a net in a package the gate ALREADY runs: read the root `package.json` workspace globs, and fail when a workspace carrying source declares no `test` script unless it is on an explicit, commented allow-list — the exact shape of the four oRPC scaffolds, so it needs no new source of truth. Filed rather than fixed because step 1 moves the deploy boundary and step 3 CREATES a build-gating net rather than adding an entry to one. | `audit/20260829-tests` |

| 2026-08-29 | docs | med | `apps/web/src/lib/server/api-alias-parity.test.ts:5` | **A doc's HTTP path claim has no net, and 33 of them had been dead for five weeks.** The `/api/v1`-only cut landed in `5f7bef15` (2026-07-21) and its consumer sweep in `c9f858d8` (2026-07-22); `apps/web/src/routes/api/-alias.ts:10` states the resulting rule — "the bare `/api/*` alias for a contract op is GONE — a bare oRPC path falls through to the TanStack router as a 404" — and only the FILE-ROUTE carve-outs stay dual-mounted. The code side of that split IS enforced: `api-alias-parity.test.ts` walks both trees and fails the build when a dual-mounted route is missing a half. Nothing walks the DOCS. This PR fixed 33 bare-path citations across 18 files that survived the cut, including six the box's own timer READMEs give as the endpoint their sweep POSTs (`publish-advance-timer/README.md:5`, `social-capture-timer/README.md:3`, `social-metrics-timer/README.md:24`, `funnel-snapshot-timer/README.md:15`, `pin-watch/README.md:77`, `anchor-timer/README.md:15`) while the shipped scripts beside them use `/api/v1` (`publish-advance-sweep.sh:51`, `social-capture-sweep.sh:44`, `social-metrics-sweep.ts:218`, `funnel-snapshot-sweep.ts:123`, `fluncle-healthcheck.ts:2128`, `anchor-sweep.ts:998`), plus `apps/web/tests/e2e/README.md:24` against `apps/web/playwright.config.ts:72`. The harm is exact and one-directional: an operator or agent who copies a documented path gets a 404 from the router with no hint that the prefix is the reason, and the failure looks like a broken endpoint rather than a stale doc. The root `README.md:49` states the rule correctly, so the canon was right the whole time and only the downstream docs rotted. | Add a doc-path gate beside the existing one: walk every tracked `.md`, extract each `` `/api/…` `` token, and fail when a token that is NOT a dual-mounted file-route path omits the `/api/v1` prefix. The two inputs both already exist in-repo — `api-alias-parity.test.ts:18-22` already enumerates the file-route tree, and `packages/contracts/src/orpc/*.ts` carries every contract `path:` — so the net needs no new source of truth. Filed rather than built because it CREATES a build-gating net rather than adding an entry to one, and the design has two real judgment calls the brief reserves: (a) whether a doc naming a bare carve-out path (`docs/admin-shell.md:68`/`:74` cite `POST /api/admin/chat` + `POST /api/admin/tracks/:id/preview`, both legitimately dual-mounted) should PASS as written or be pushed to the canonical prefix too — the alias is back-compat, so the answer decides whether the docs are allowed to document it; and (b) whether the net's scope includes `docs/planning/**` and the ledger, which quote historical paths on purpose and would false-fail forever. Whichever way (a) goes, write the ruling next to the assertion; `doctrine-parity.test.ts` is the working shape for a doc-reading gate in this repo. | `audit/20260828-docs` |
| 2026-08-29 | docs | low | `docs/naming-conventions.md:13` | **The convention doc's own CLI inventory drifted by twelve whole command groups — so twenty-odd admin commands shipped without ever being checked against the convention they are supposed to obey.** §1.1 opens "The `fluncle` tree ... plus the hidden `tracks` group and the hidden `admin` tree" under the heading "## 1. The surfaces, as they stand today", which reads exhaustive, and the naming checklist at §6 sends a new feature here to find its precedent. Dumping the real tree from `createProgram()` (`apps/cli/src/cli.ts:447`) and diffing it against the table found twelve admin groups with NO row at all (`queue`, `publish`, `capture`, `clips`, `recordings`, `newsletter`, `prompts`, `notes`, `observations`, `frontier`, `reach`, `albums`) and five rows short of their real subcommands (`admin tracks` missing eight, `admin backfills` five, `admin artists` four, `admin catalogue` three, `admin auth` two, `admin submissions` one). All of it is repaired in this PR from the CLI's own `.description()` strings, so the table is currently true. The asymmetry is the finding: the API half of the same document is gated — `orpc-naming.test.ts` fails the build on a `verb_noun` violation and `orpc-coverage` on a missing contract — while the CLI half is a hand-maintained snapshot that nothing re-reads, so it rots in exactly the direction that matters (a NEW command, the one case the checklist exists for, is the one most likely to be missing). | Bind the table to the tree the way the API half is already bound. `createProgram()` is exported and the audit dumped the full tree in one script, so a test can import it, parse the `| Group | Command |` cells out of §1.1, and assert set-equality in both directions — a command with no row fails, and a row naming a command that no longer exists fails too (the second half is what catches a RENAME, which is the convention's whole subject). Filed rather than built for the same reason as the row above: it creates a gate rather than extending one, and it needs one ruling first — whether `{ hidden: true }` commands (`admin help`, and the `tracks`/`admin` groups themselves) are IN the inventory or exempt. The doc's own line says the hidden `tracks` group and hidden `admin` tree ARE in scope, which would put `admin help` in too; deciding whether a help stub earns a convention row is a taste call, not an audit edit. Cheaper alternative worth weighing first: drop the exhaustive framing from §1.1's opening sentence and say the table is a REPRESENTATIVE set of shapes, which removes the claim instead of testing it — but note that weakens §6's "find your precedent here" instruction, which is the table's actual job. | `audit/20260828-docs` |
| 2026-08-28 | surfaces-seo | med | `apps/web/public/llms.txt:31` | **Two `web`-weighted surfaces reach NO agent-discovery document, and one of them is structurally unable to: the static `llms.txt` cannot read a runtime gate.** `apps/web/src/lib/server/agent-discovery.ts:5` imports `public/llms.txt?raw` and `:491` re-serves those exact bytes, so `/llms.txt` — `discovery.llms`, `weights: { web: "primary" }` — is a file on disk with no access to the app's state. That is fine until a surface's advertisement is CONDITIONAL, and one now is. (1) THE SONIC-GALAXIES LENS. `web.galaxies` (`packages/registry/src/index.ts:465`) is dark until the operator has NAMED the whole map, and the gate has OPENED: `curl -o /dev/null -w '%{http_code}' https://www.fluncle.com/galaxies` answers **200**, and both GENERATED documents carry their gated line live — the `Accept: text/markdown` homepage prints "[Galaxies API](…/api/v1/galaxies) … Browse them at https://www.fluncle.com/galaxies" and `llms-full.txt`'s "## More" block prints "The sonic galaxies: https://www.fluncle.com/api/v1/galaxies". `curl -s https://www.fluncle.com/llms.txt | grep -i galax` returns only the tagline, the `/about` bullet, and the rave-terminal line: **no lens, no `/galaxies`, no galaxies API**. So the loudest AI-discovery document is the ONE that omits a live browse surface the other two advertise. (2) THE GALAXY GAME. `web.galaxy` + `subdomain.galaxy` are `web: "primary"` — only four web routes carry that weight — and `galaxy.fluncle.com` / `/galaxy` appear in NEITHER `llms.txt` NOR the markdown homepage NOR `llms-full.txt`. Its own HTML cannot cover for that: `routes/galaxy.tsx:32` renders `noindex` because the game is a client-only canvas, so a crawler that fetches it reads chrome and no content. Prose in a discovery map is the only way a model learns the game exists, and the only place that prose lives today is the `/about` FAQ. Same class as the open `agent-discovery.ts:359` row (the archive-search op missing from four of five documents) — that row names the remedy, this one shows the class is not one op wide. Filed rather than fixed on two gates the auditor cannot clear: the lens line is new public agent-facing copy needing the `copywriting-fluncle` drafting gate and the `canon-reviewer` acceptance gate (the Agent tool is disabled on this run, the `nav-model.ts:259` precedent), and the galaxies half cannot be a static line at all without first ruling on (b) below. | Rule on the mechanism before writing the copy, because the mechanism decides whether copy is even possible. (a) THE GAME is unconditional — `/galaxy` and `galaxy.fluncle.com` are always-200 and always-live — so it is a plain static line in `llms.txt`'s "## Tools" block (`:80-86`, beside the CLI installer, Lens and the rave terminal) plus the markdown homepage's matching block, with the `/about` FAQ's ratified sentences ("There's a game too, at galaxy.fluncle.com, where each finding is a star you can fly to") as the drafting precedent so nothing is invented. Say in the line that the page is a canvas game, so a model reading it does not fetch the URL expecting prose. (b) THE LENS is the real decision, and there are exactly two honest answers. EITHER accept that `llms.txt` is static and say so — write the constraint into `agent-discovery.ts:491` and drop the registry's expectation that it advertises gated surfaces (the `web.galaxies` note has been corrected in this PR to name the two documents that CAN gate) — OR make `/llms.txt` generated like its two siblings: keep `public/llms.txt` as the body and let the handler append the same `galaxiesLine` the markdown home already builds from `isGalaxyMapFullyNamed()`, which costs one existing call and makes the primary map gate-aware once and for all. The second is the better answer if any FUTURE surface is gated; the first is honest if none will be. That is a direction call, not an audit edit. (c) Whichever way (b) goes, the durable half is the coverage test the `agent-discovery.ts:359` row already proposes — assert every non-`pending` surface at `web: "primary"` appears in `llms.txt` + the markdown home, with an explicit, commented exemption list for the gated ones. `doctrine-parity.test.ts` is the working shape. It changes a build-gating net's decision logic, so it is the operator's call. | `audit/20260827-surfaces-seo` |
| 2026-08-28 | surfaces-seo | low | `packages/registry/src/index.ts:397` | `web.artist` is the only entry in the catalog whose `route`/`url` names an address that 404s. It carries `route: "/artist"` and `url: "${SITE}/artist"`, and `curl -o /dev/null -w '%{http_code}' https://www.fluncle.com/artist` answers **404** — there is no `/artist` index route (`apps/web/src/routes/` has `artists.index.tsx` and `artist.$slug.tsx`, and nothing at `/artist`). The `url` field's own doc at `:177` says "The canonical absolute URL, when the surface lives at a fixed address," so the value contradicts the field. Its two siblings do not have this shape: `web.labels` is `route: "/labels"` and `web.albums` is `route: "/albums"` — the real, always-200 hub — while each entry's `exposedContent` covers the hub AND the `:slug` page, exactly as `web.artist`'s does (`:390-392` leads with "/artists — one A–Z index of every drum & bass artist Fluncle holds"). The parameterised surfaces are also already spelled honestly elsewhere in the catalog: `feed.fresh.artist.rss` uses `/artist/:slug/fresh.xml`, and `post-deploy-probe.ts:275` skips any route containing `:` as "parameterised route — no fixed address to probe". So `/artist` is the one value that reads concrete and is not. NO CONSUMER IS BROKEN TODAY, which is why this is low: `web.artist` has no `probeConfig`, `statusProbes()` therefore never reaches it, the `liveSurfaces()` consumers (`apps/cli/src/commands/status.ts:50`, `apps/web/src/lib/server/tools/registry.ts:674`) read only `operatorNotes` + `exposedContent[0]`, and the registry header states that `surfacesForContext`/`surfacesByWeight`/`surfacesByKind` "have no consumer outside this package yet". It is a latent 404 waiting for the first consumer that renders `surface.url`. | Decide what the surface IS, then make the fields say it — the two readings need different edits and the choice is a naming call, not a typo fix. (1) IF it stands for the HUB, mirror `web.labels`/`web.albums` exactly: `route: "/artists"`, `url: "${SITE}/artists"`, and rename `web.artist` → `web.artists` so the three graph surfaces read alike. The rename touches `docs/surfaces-doctrine.md` (`doctrine-parity.test.ts` matches the `name` verbatim and build-fails until the row is renamed) and would then make the surface HTTP-probeable, which is a second, separate decision — the `operatorNotes` currently justify the missing `probeConfig` with "the route is slug-parameterized, so there is no fixed URL to GET-probe", a sentence that stops being true under this reading. (2) IF it stands for the slug-parameterised FAMILY, spell it as one: `route: "/artist/:slug"`, which is honest, keeps the `operatorNotes` justification intact, and makes the existing `post-deploy-probe.ts` skip rule recognise it — but then `/artists`, a real indexable hub in the sitemap (`lib/sitemap.ts:376`) and in `llms.txt`, has no registry entry at all and should get one. Reading (1) is the smaller catalog and matches the siblings; reading (2) is more literal. Either way it is one data edit plus the doc row, and it should land with a one-line note in the entry saying which reading won, so the next auditor does not re-open it. | `audit/20260827-surfaces-seo` |

| 2026-08-27 | security | med | `apps/web/src/lib/server/live.ts:128` | **`record_live_state` is AGENT tier and its tier-map justification says it publishes nothing, but the handler POSTS FREE TEXT TO THE PUBLIC CREW TELEGRAM CHANNEL AND PINS IT.** `orpc-auth-coverage.test.ts:631-634` records the tier as `admin` on the reasoning "the record_health precedent; it writes only the internal single-row live_state table (no publish), so the box agent token drives it each minute." The first half is the tier, the second half is not what the code does. `setLiveState` (`live.ts:105-165`) reads the stored row, and on an off→on flip calls `postLiveToTelegram(input.title)` (`:128`) and then `pinChatMessage` (`:134`). `postLiveToTelegram` (`telegram.ts:176`) reads `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHANNEL_ID` Worker-side and sends; `formatLiveTelegramMessage` (`telegram.ts:161-171`) interpolates the caller's `title` verbatim into the message body between curly quotes. So an agent-scoped token — the credential the box holds, and the one the split exists to bound — reaches a public publish action. The contract's `title` is `z.string().nullable()` with no `.max()` and no content gate, there is no marker guard, no kill switch, and no throttle: alternating `{live:false}` / `{live:true}` posts and pins a new message each time. The comparison inside the same tier map is the argument: `announce_mixtape` is OPERATOR tier at `:216` for exactly this reason — "it posts a public Telegram crew callout (and is one-shot, marker-guarded), so the agent token 403s" — and `env.ts:429-430` states the invariant plainly: "a compromised agent holds only the agent token, which the Worker refuses for publish actions." This op is the counterexample to that sentence. The two agent-tier ops that DO publish (`advance_publish_queue`, `drip_clips`) each say so in their tier entry and each sit behind an operator-tier kill switch (`set_publish_advance`, `set_clip_drip`); this one has neither the disclosure nor the switch. Severity is med rather than high because it needs an already-compromised (or buggy) agent token, and the blast radius is a Telegram post the operator can delete — but it is the publish boundary, and the tier map currently misstates it. The `title` also renders on public read surfaces (`getLiveState` → the web home banner, `/api/v1/status`, the MCP live note at `mcp.ts:543`, and via /api/status the SSH, CLI and dig surfaces), so the same unbounded field is public text on six surfaces. | Operator call, three parts, and the first is free. (1) CORRECT THE TIER-MAP ENTRY at `orpc-auth-coverage.test.ts:631-634` whichever way (2) goes — the comment must not say "no publish" about an op that posts to Telegram. (2) RULE ON THE TIER. Moving `record_live_state` to `operator` is the clean read of the publish boundary, but it BREAKS the on-box `fluncle-live` poller (`docs/agents/hermes/scripts/fluncle-live.ts:245-278` POSTs with the agent token every minute), so it is not a free flip — it would need the poller to carry an operator token, which is the thing the box is designed not to hold. The cheaper shape that keeps the tier: SPLIT the publish out of the state write — `record_live_state` stays agent tier and writes `live_state` only, and the Telegram callout moves behind an operator-armed switch (the `set_clip_drip` shape) or fires from a Worker-side trigger that reads the stored row rather than the request body. Either way the box keeps its one-minute write and the crew channel stops being reachable from an agent token. (3) BOUND THE FIELD once (2) is settled. `title` is a Twitch stream title, which Twitch itself caps at 140 characters — an EXTERNAL bound, not an in-repo constant, which is why it is filed rather than fixed under tonight's sizing rule. If (2) keeps any path from `title` to a public surface, give it a `.max()` and a content gate sized off whatever the ruling settles. | `audit/20260826-security` |

| 2026-08-27 | security | med | `apps/web/src/routes/api/auth/$.ts:8` | **The two Better Auth endpoints that SEND EMAIL to a caller-chosen address are the two the repo's own durable rate limiter does not cover.** `auth/$.ts` wraps the Better Auth catch-all and applies `enforceRateLimit` to exactly two paths: `/sign-up/email` (5/h, `:8-15`) and `/sign-in/email` + `/sign-in/username` (20/h, `:17-24`). `/request-password-reset` and `/send-verification-email` get nothing from this layer, and both spend a Resend credit and deliver mail to an address the ANONYMOUS caller names (`public-auth.ts:341` `sendResetPassword` → `resend.ts sendPasswordResetEmail`; `public-auth.ts:362` → `sendVerificationEmail`). They are not unprotected — Better Auth ships a default special rule of 3 per 60s for exactly those paths (`node_modules/better-auth/dist/api/rate-limiter/index.mjs:370-384`) and `rateLimit.enabled` defaults to true in production (`.../context/create-context.mjs:169-174`) — but that built-in limiter violates BOTH invariants `rate-limit.ts:1-25` was written to establish, and the header of that file names them as prior deepsec findings. (a) STORAGE: the default is `storage: "memory"` (`create-context.mjs:174`, since this app configures no `secondaryStorage`), a module-level `Map` (`rate-limiter/index.mjs:6`). On Cloudflare Workers that is PER-ISOLATE and evicted with the isolate — the exact shape `rate-limit.ts:22-25` rules out ("One durable store. Always the DB (`rate_limit_counters`), never per-isolate memory"), and the same shape it records the old newsletter limiter being replaced for ("a per-isolate in-memory array that resets on every redeploy — effectively no limit"). (b) KEY: `advanced` (`public-auth.ts:289-291`) sets only `cookiePrefix`, so `advanced.ipAddress` is unset and Better Auth keys on `x-forwarded-for` (`@better-auth/core/dist/utils/ip.mjs:194,204`) — the header `rate-limit.ts:36-42` explicitly refuses ("we deliberately do NOT fall back to `x-forwarded-for` (a caller can set it to any value)"), while the repo's own bucket keys on `cf-connecting-ip`. Better Auth 1.6.26 does harden the read — with no `trustedProxies` it trusts only a SINGLE-value header (`ip.mjs:186-191`) — so a caller who supplies their own XFF makes Cloudflare's appended value a two-entry chain, the resolve returns null, and the request lands in the shared `NO_TRUSTED_IP_KEY` bucket (`rate-limiter/index.mjs:281-287`). That fails safe for the attacker and is not a bypass, but it means the effective key for anyone sending the header is a single global bucket rather than their own — a limiter nobody has characterised on this runtime. The asymmetry is the tell: someone already judged the built-in limiter insufficient for sign-up and sign-in and reached for the durable one; the two endpoints that mail a third party were not carried across. Nothing is currently known to be abused — this is posture, not an incident. | Operator call, because every option here changes WHO GETS SERVED, which tonight's brief puts outside a fix. Two directions, not exclusive. (A) EXTEND THE EXISTING WRAPPER — add `/request-password-reset` and `/send-verification-email` to `authRateLimit` in `auth/$.ts` with their own `action`/`limit`/`windowMs`, which reuses the durable atomic counter and the `cf-connecting-ip` bucket the repo already trusts, and is the smallest change that puts both endpoints under the same rules as their two siblings. The numbers are the ruling: Better Auth's own default is 3/60s, and the repo's neighbouring email-adjacent budgets are `account.avatar` 10/h and `auth.signup` 5/h — pick from that range deliberately rather than inheriting a library default. Note that a per-IP cap does not stop the OTHER abuse shape (many requests for ONE victim address from many IPs), so consider a second bucket keyed on the target email. (B) FIX THE LIBRARY LIMITER'S TWO DEFECTS so every OTHER Better Auth path (there are dozens the wrapper does not name) is covered too: set `advanced.ipAddress.ipAddressHeaders: ["cf-connecting-ip"]` in `createPublicAuthOptions`, and give `rateLimit` a storage that is not per-isolate memory — Better Auth supports `storage: "database"` against the `rateLimit` model (`@better-auth/core/dist/db/get-tables.mjs:34`), which would need a Drizzle table and therefore a MIGRATION, and a migration is on the auditor's hard-rail list. (B) is the durable one; (A) is the one that can ship this week. Whichever lands, add a test beside `apps/web/src/lib/server/password-reset.test.ts` asserting the limit bites, so the next endpoint that mails a stranger inherits a net rather than a habit. | `audit/20260826-security` |

| 2026-08-17 | architecture | low | `apps/web/src/lib/server/tools/specs.ts:328` | One canonical `verb_noun` name, `list_similar_artists`, is worn by TWO differently-shaped public machine-facing operations, and the ratified convention has no rule for the case. The CONTRACT op (`packages/contracts/src/orpc/artists.ts:77`, `GET /artists/similar`, operationId `listSimilarArtists`) takes `slugs` — a comma-separated list of 1 to 6 artist SLUGS — and answers "sounds like the average of all of these". The MCP/chat TOOL of the same name (`apps/web/src/lib/server/tools/specs.ts:328`) takes `{ name, limit? }` — one artist NAME, which it resolves through `toArtistSlug` → `getArtistBySlug` → `getArtistNeighbours` — plus a per-call result `limit` the HTTP op does not expose. They agree on the single-anchor answer (the contract's own docblock notes an average of one is itself), so this is not a behaviour bug; it is one NAME over two input contracts. `orpc-naming.test.ts` cannot catch it — it reads `CONTRACT_OPERATION_NAMES` only, so the tool registry is outside its net, and `SHARED_TOOL_SPECS` has no name-collision check against the contract registry. Verified tonight: the tool's spec comment previously claimed no public HTTP op existed (written 2026-07-18, `9c5a34f9`); the op landed two days later in `3e77c109` (#795) and the comment was never revisited. That comment is corrected in this PR — the NAMING question it exposed is what is filed here. `docs/naming-conventions.md` §1.3 describes the tool inventory and §3 the projection rules, and neither says what happens when a tool and an HTTP op share a name but not a shape. | Operator ruling, because both directions are defensible and one of them edits ratified law. (a) RATIFY THE ASYMMETRY: add a clause to `docs/naming-conventions.md` §3 saying a shared `verb_noun` name spans surfaces that may differ in ADDRESSING (a name an agent has in hand vs a slug a client already holds) but never in the ANSWER, and require a spec comment naming the sibling op — cheapest, and it matches what the code already does, but it is an edit to the convention doc, so it is his call not the auditor's. (b) UNIFY: widen the tool's input to accept slugs (or the op's to accept names) so the two become one operation with one input contract — cleaner against Convention B's literal wording, but it changes a PUBLIC agent-facing tool schema that MCP clients and ChatDnB already call, which is a product-direction change, not a refactor. Whichever way it goes, the durable half is a TEST: extend `orpc-naming.test.ts` (or `tools/registry.test.ts`) to cross-check `SHARED_TOOL_SPECS` names against `CONTRACT_OPERATION_NAMES` and fail on a collision whose input schemas differ — today nothing would flag the next one. Note that `get_status`, `build_set`, and the three `list_*_catalogue` tools are MCP-only names with no contract op at all, which is a different and already-documented case. | `audit/20260817-architecture` |

| 2026-08-16 | voice | low | `apps/web/src/routes/radio.tsx:1098` | **One action, three labels: "open this finding's log page" is named "View the log" on the radio player, "Read the log" on SSH and in every crew-feed post, and "Open the log page for &lt;track&gt;" in the web archive's own controls.** VOICE.md §4's Chrome Rule is explicit that this is a defect and not a variation — "**one action, one label**: before naming any control, check the sibling surfaces … A new label for an existing action is a bug, not a variation." The live spellings: `apps/web/src/routes/radio.tsx:1098` renders a `<Button>` reading "View the log"; `apps/ssh/main.go:1466` renders `terminalLink("Read the log", logPageURL(coord))` on a finding's detail, and `apps/web/src/lib/server/telegram.ts:30` and `:118` write `Read the log: <url>` into the finding and mixtape posts a stranger meets (Bluesky sidesteps the question entirely — it posts a link CARD, no labelled link); and the web archive's row controls (`track-row.tsx:35`, `saves-door.tsx:320`, `rec-rows.tsx:84`, `apps/mobile/src/components/finding-row.tsx:41`) all use the aria-label "Open the log page for &lt;track&gt;". A FOURTH chrome spelling was measured on the 2026-08-24 voice sweep and is not in the count above: `apps/web/src/routes/embed.$logId.ts:122` renders "Open the log" on the oEmbed card, the unfurl a stranger meets in Slack or Discord — the same kind of control as `/radio`'s button, so the chrome count is now three spellings across three surfaces with the two crew-feed instances prose on top. The count favours "Read the log" three to one, but the count is not the argument: the two crew-feed instances are PROSE links inside a post, not chrome, and the aria-label family is a screen-reader string that has to name its target. Contrast the case that IS settled — `apps/web/src/components/identity-states.tsx:71` ratifies "Listen on Spotify" in a comment and reasons out loud why Beatport reads "Buy" and YouTube "Watch"; nothing comparable exists for the log link. (The SSH mixtape detail's sibling divergence, "Hear it on Mixcloud/SoundCloud" against that ratified "Listen on X" family, WAS fixed tonight — it had the comment to point at.) | Rule the ratified string for "open a finding's log page", write it into a comment the way `identity-states.tsx:71` does, then sweep the surfaces onto it. The likely answer is "Read the log" for a chrome control (it is what SSH and both crew feeds already say, and "read" is the verb the archive uses for its own pages), leaving the aria-labels alone as the accessible-name variant they are — but that is a call about whether a prose link in a Telegram post and a button on `/radio` are "the same control" for the Chrome Rule's purposes, which the canon does not settle. Whatever wins should land as a sentence in `references/voice.md` §4 under the Chrome Rule, because this is the second cross-surface label question in two audits and the rule currently gives no test for when two surfaces are the same control. Public copy on both sides, so both the `copywriting-fluncle` drafting gate and the `canon-reviewer` acceptance gate apply. | `audit/20260816-voice` |

| 2026-08-15 | design | med | `apps/web/src/pipeline/create-pipeline.ts:436` | **`/pipeline` renders its entire infographic in the system sans — the one surface in `apps/web` that opts out of the brand faces.** The self-injected stylesheet opens with `font:13px/1.4 ui-sans-serif,-apple-system,"Segoe UI",system-ui,sans-serif` on `.fpl`, and because every descendant inherits from it, nothing on the page — the 40-odd station cards (`.card .lb` / `.card .wh`), the chips, the band tags, the lore paragraphs, the kiosk labels, the heartbeat readout — is ever set in Space Grotesk or Oxanium. DESIGN.md §3's **Canon Travels Rule** closes its exemption list explicitly ("the surfaces we do not control the type on, and they are the only ones: a TTY …, Raycast's own chrome, an HTML email …, and baked bitmap lettering inside a sprite"), and `/pipeline` is none of them: it is a route in this app (`apps/web/src/routes/pipeline.tsx`), served from the same origin, under the same `__root` `<link>` that already loads both faces with the One Box Rule's `ascent-override`/`descent-override` metrics (the six `@font-face` blocks at `styles.css:81-172`). The faces are on the page; this sheet simply never asks for them. Note the **Lore-Art Paint Exemption** (DESIGN.md §2) does NOT cover this — it names `/pipeline` for hue diversity only, and the file's own header says so in the same words ("The exemption buys hue diversity and nothing else"). Separately, the sheet carries two uppercase-tracked eyebrows DESIGN.md §6 bans by name — `.hw-lbl` at `:449` (`font-size:9px;letter-spacing:.18em;text-transform:uppercase`) and `.band .tag` at `:473` (`letter-spacing:.16em`) — which want the same ruling and the same pass. Three other canon defects in this file were fixed tonight (the ungated heartbeat pulses, the ungated kiosk hover float, and five pure-white/pure-black veils); the type is what is left. | Set the stack to the canon pair and re-check the fixed boxes, in that order. The edit itself is one line — `.fpl{font:13px/1.4 "Space Grotesk",ui-sans-serif,system-ui,sans-serif}` plus `font-family:"Oxanium",…` on the numeric/brand runs (`.zoomctl .zlabel`, already `tabular-nums`; the `.chip.cad` cadence readout) per The One Voice Rule. What makes it a file rather than a fix is the SECOND half: this layout is hand-tuned in absolute pixels against the current metrics, so a face swap can overflow boxes that have no room to grow — `.card` is a fixed `width:188px` holding `.lb` at 12.5px and `.wh` at 10.5px, `.legend .name` animates to a hard `max-width:160px`, `.zoomctl .zlabel` is a fixed `width:44px`, `.band .tag`/`.sub` are `white-space:nowrap` at absolute offsets, and the whole world is positioned off `COLW`/`B(c)` constants (`:420-428`). Space Grotesk is wider than the macOS/Windows system sans at the same px, so the failure mode is silent clipping and wrapped station labels, not a build error — it needs a real browser at both zoom extremes plus the `max-width:640px` branch, and there is no browser on the audit box. Do the two eyebrows in the same pass (drop the uppercase + tracking, keep them small and bold — DESIGN.md §3's Label role), since they are the type decisions most likely to need a size nudge once the face changes. | `audit/20260815-design` |
| 2026-08-15 | design | med | `packages/ui/src/components/dropdown-menu.tsx:40` | **Every floating popup in the app ships an elevation drop shadow, which DESIGN.md §4 prohibits outright.** The rule is unusually absolute — "Box-shadows are effectively banned (the sole exception is the hairline `shadow-xs` baked into the outline button)" and "a surface with a drop shadow is prohibited outright" — and depth is supposed to come from translucency over the cosmos. Six generated Shadcn surfaces carry one anyway: `dropdown-menu.tsx:40` (`shadow-md`) and `:137` (the submenu, `shadow-lg`), `popover.tsx:35`, `select.tsx:83`, `hover-card.tsx:51`, `combobox.tsx:61` (all `shadow-md`), and `sheet.tsx:54` (`shadow-lg`). That is not a corner case: `DropdownMenuContent` appears in 10 files, `PopoverContent` in 10, `SelectContent` in 5, `HoverCardContent` in 3, `SheetContent` in 2, `ComboboxContent` in 2 — and 11 of those files are public, not `/admin` (`track-row.tsx`, `catalogue-groups.tsx`, `graph-link.tsx`, `nav/crew-slot.tsx`, `account/saves-door.tsx`, `docs-page-actions.tsx`, `routes/radio.tsx`, `routes/tracks.tsx`, …). The repo already knows: three surfaces kill their shadow one at a time, each with the rule quoted in a comment — `.graph-card` (`styles.css:6119-6130`, "the card kills the `shadow-md` its Shadcn popup ships with"), `[data-slot="sidebar-inner"]` (`styles.css:5054-5061`), and the behind-the-scenes Sheet (`behind-the-scenes.tsx:46`, "the generated `shadow-lg` (drop shadows are banned on the public surface"). Those three are the argument that the other six are unintended, and they are also the argument for why the fix is not mechanical: each was a considered, per-surface override rather than one sweep. Worth noting the same sentence's other half has drifted too — canon sanctions `shadow-xs` on "the outline button", but it also rides `switch.tsx:17`, `checkbox.tsx:11`, `input-group.tsx:18`, `toggle.tsx:22`, `toggle-group.tsx:45`, `select.tsx:42`, and `combobox.tsx:21`. | Rule on WHERE the override belongs, then apply it once. Two directions. (a) ONE APP-SHEET BLOCK — `apps/web/src/styles.css` grows a single rule over the six popup slots (`[data-slot$="-content"]`, or the explicit list `dropdown-menu-content`, `dropdown-menu-sub-content`, `popover-content`, `select-content`, `hover-card-content`, `combobox-content`, `sheet-content`) setting `box-shadow:none`, exactly as `.graph-card` and `[data-slot="sidebar-inner"]` already do; this survives a Shadcn CLI regeneration, which is the whole reason the existing three live in the app sheet rather than in `packages/ui`. (b) EDIT THE GENERATED COMPONENTS in `packages/ui/src/components/` so every consumer of the design system inherits it — correct in principle (the shared package IS the system, and `apps/mobile`/`apps/extension` do not get the app sheet's overrides) but it puts a hand edit in files the CLI rewrites, which AGENTS.md's "use Shadcn components by their canonical generated exports" argues against. Filed rather than fixed on blast radius and one unverifiable premise: the popups are opaque Sleeve Black with `ring-1 ring-foreground/10`, and the graph-card comment reasons that the ring carries the edge in the shadow's place — but a `SelectContent` opening over the bright sun region of the cover backdrop is exactly where that hairline is thinnest, and there is no browser on the audit box to confirm the popup still separates. Settle the `shadow-xs` half in the same ruling: either amend DESIGN.md §4 to say "form controls" rather than "the outline button", or strip it from the six non-button primitives. | `audit/20260815-design` |
| 2026-08-15 | design | low | `apps/web/src/routes/status.tsx:223` | **The two console pages head their sections with the uppercase-tracked eyebrow DESIGN.md bans by name, and the code records a reason the canon does not answer.** `status.tsx:223` and `reach.tsx:441` both define their own `SECTION_HEADING_CLASS` ending in the same `border-b border-border pb-2 text-sm font-semibold uppercase tracking-wide text-foreground` — copy-pasted with only the layout prefix differing (`mb-4` on status, `flex items-baseline justify-between` on reach) — and hang the pages' `<h2>`s off it (`status.tsx:619`, `:666`; `reach.tsx`'s section heads). DESIGN.md §6 says "don't add uppercase-tracked eyebrow labels; labels are bold and small, not tracked-out", and §3's Label role repeats it ("Bold and small, never uppercase-tracked"). The only uppercase the canon sanctions is the Oxanium brand lockup — the stamped nameplate and the FOUND stamp — and these are neither: they are Space Grotesk, the reading face, tracked out at `text-sm`, which is precisely the settings-screen register §3 says the pairing exists to avoid. The divergence is deliberate and self-documented: the comment at `status.tsx:219-222` gives the reason ("the page's three sections read at one consistent, visible weight — they were near-hidden muted-xs labels before"), which is a real hierarchy problem the canon sentence does not solve. Both pages are **workstations** under §5's Three Areas Rule, where "the interface carries the meaning" and there is no sanctioned heading role between the plate masthead and the Label. So this is a genuine gap in the canon rather than sloppiness, which is why it is here rather than reverted — reverting recreates the near-invisible headings the comment describes. | A canon ruling, then make the loser follow, and de-duplicate either way. (a) NAME THE ROLE — add a console/section-heading entry to DESIGN.md §3's Hierarchy (the register between the masthead and the Label that a workstation needs) and say whether it is uppercase-tracked or, better, small-and-bold at a higher-contrast ink with the hairline rule doing the separating; then §6's "don't" gets the "except …" clause it is missing. (b) HOLD THE RULE — drop the `uppercase tracking-wide` and buy the visibility back the way the canon already allows: keep the `border-b` rule, raise the weight to the Label's 800, and lift the ink to `text-foreground` (which it already is), so the heading is bold and small rather than tracked out. Whichever wins, the constant is copy-pasted across two routes and should become one shared export before a third console page copies it a third time — note `/pipeline` has its own third spelling of the same idea (`.hw-lbl` and `.band .tag`, filed above), so the ruling should cover all four sites at once. Filed rather than fixed because both branches change shipped visual hierarchy on public pages and (a) edits a canon rule's closed list. | `audit/20260815-design` |
| 2026-08-13 | docs | med | `apps/cli/src/commands/admin-tracks.ts:178` | **271 code comments across 142 files cite an RFC that no longer exists — the dangling half of `AGENTS.md`'s own prune rule.** That rule mandates deleting a shipped RFC ("a built RFC is removed, never flipped to a 'done/Final' status") and, when deleting, "repoint any comment that cited it". The deletions happened; the repointing did not finish. `docs/rfcs/` is gone entirely (no such directory), and `git log --diff-filter=D -- 'docs/rfcs/*'` shows the prunes landing across at least eight commits (`a8cfca20`, `f0ae06b3`, `6161ef84`, `c2ab52d7`, `5f11af20`, `234cd2ff`, `ba97df38`, `96fe7745`) — one of which (`f0ae06b3`) is literally titled "prune the shipped RFC, **repoint dead-RFC citations**, triage the audit ledger", so this is a known, partially-done sweep rather than a new discovery. What remains, by citation count: `RFC artist-primary-capture` (42), `RFC musickit-second-authority` (32), `RFC plan→recording→mixtape` (21), `RFC full-audio` (21), `RFC dnb-identity-graph` (18), `RFC recording-primitive` (17), `RFC bpm-key-accuracy` (17), `RFC U3b` (17), `RFC label-lineage-remixer` (16), plus ~16 more names and bare unit refs (`RFC U1`, `RFC Unit B`, `RFC D1`). They are dense in exactly the load-bearing places — `apps/cli/src/cli.ts` alone carries a dozen, and they also sit in `apps/mobile/src/components/`, `docs/agents/hermes/*-timer/README.md`, and `docs/agents/hermes/Dockerfile`. The harm is a dead pointer at the moment of maximum need: a reader hitting `// (RFC bpm-key-accuracy)` above a non-obvious branch greps the tree, finds nothing, and has no way to know the rationale is recoverable only by archaeology through a deleted file's history. Note the _bare unit_ refs are worse than the named ones — `(RFC U1)` and `(RFC Unit B)` do not even name which RFC, so they are unrecoverable without knowing the commit that wrote them. | Repoint each citation to the canon doc the shipped work now lives in, which is what the prune rule intends: `RFC bpm-key-accuracy` → `docs/track-lifecycle.md`, `RFC full-audio`/`RFC U3b` → `docs/gpu-batch-embed.md` + `docs/album-artwork.md`, `RFC artist-primary-capture` → `docs/artist-relationship.md`, `RFC label-lineage-remixer` → `docs/label-entity.md`, `RFC recording-primitive`/`RFC plan→recording→mixtape` → `docs/fluncle-studio.md`, `RFC musickit-second-authority` → `docs/album-entity.md` + `docs/album-artwork.md`, `RFC dnb-identity-graph` → `docs/artist-relationship.md`. Filed rather than fixed on impact and judgment, not size: 271 sites across 142 files is a repo-wide sweep touching CLI, mobile, web, and the box scripts, and — the actual blocker — the mapping above is a **guess per citation** until each one is read in context. Several RFCs shipped across more than one canon doc, and the bare `(RFC U1)`/`(RFC Unit B)` refs cannot be mapped from the text at all without reading the deleted file out of git history to learn which RFC's Unit 1 was meant. That is a rationale the code does not carry, which the brief puts on the file side of the line. Two cheap follow-ons worth doing whichever way the sweep goes: (a) resolve the bare-unit refs FIRST since they are the unrecoverable ones and there are only ~30; (b) add the repoint step to the prune rule's checklist in `AGENTS.md` so the next prune cannot leave the same tail — the rule already says to repoint, but nothing verifies it, and this is the second time the tail has been left. | `audit/20260812-docs` |
| 2026-08-13 | docs | low | `docs/agents/hermes/cron/README.md:159` | **Each sweep's per-tick batch cap is stated in three unbound places, and tonight one of them was wrong.** `cron/README.md:159` claimed `fluncle-observation` runs at "cap **3**/tick" while `observe-sweep.ts:76` sets `const BATCH_CAP = 1` — and the same file contradicted itself twice (`:167` "**`BATCH_CAP=1`** (one finding per tick)", `:396` "confirm the per-tick batch is small (`BATCH_CAP=1`)"), as did `docs/agents/hermes-agent.md:130` ("Cap 1/tick"). Fixed tonight (3 → 1). The number is not cosmetic: `observe-sweep.ts:72-75` records that the cron runner kills a `--no-agent` job at 120s and that a single `claude -p` authoring plus a Cartesia render already sits near that budget — "two blew it" — so a doc reading "3" invites an operator to widen a cap that is load-bearing on a timeout, and each extra tick spends real Cartesia credits. A verification pass over the other eight (`embed` 1, `context` 6, `enrich` 4, `note` 1, `observe` 1, `triage` 3, `artist` 5, `logbook` 1) found all in step, so this was a single drift and not a systemic rot — but nothing would have caught it, and the cadence layer directly beside it IS mechanically guarded (`doctrine-parity.test.ts` for surface names, `cron-roster.test.ts` for the 44 timers, and a check tonight found every `probeConfig.cadenceMs` matching both its doc row and its systemd `OnUnitActiveSec`). The caps are the one number in that neighbourhood with no net under it. | Bind the documented cap to the constant the way the cadence layer is already bound. A test in `docs/agents/hermes/scripts/` could parse the `cap **N**/tick` cells out of `cron/README.md`'s roster table and assert each against its sweep's `BATCH_CAP`. Filed rather than fixed because the gate's decision logic needs a judgment call the brief reserves: three sweeps derive the cap from the environment (`entity-bio-sweep.ts:97` `parsePositiveInt(process.env.ENTITY_BIO_BATCH_CAP, 1)`, `clip-sweep.ts:40` `Number(process.env.CLIP_BATCH_CAP ?? "1") \|\| 1`, `capture-sweep.ts:196` `Number(process.env.FLUNCLE_CAPTURE_BATCH_CAP ?? "4")`), so the test must either static-parse the default out of a defaulted expression — brittle against a harmless refactor — or import each module and read the resolved constant, which runs box-script module scope inside the test process. Picking between a brittle parser and a heavier import, and deciding whether a doc-prose gate earns its false-positive budget at all, is the operator's call on gate design rather than an auditor's. A lighter alternative worth weighing first: drop the redundant cap from the roster table entirely and let `:167`-style prose next to each sweep own the number once, which removes the third copy instead of testing it. | `audit/20260812-docs` |

| 2026-08-12 | surfaces-seo | med | `apps/web/src/lib/server/agent-discovery.ts:359` | **The archive SEARCH op reaches one of the five agent-discovery documents, and it is the loudest public API surface Fluncle has.** `api.search.archive` (`GET /api/v1/search/archive`) carries `weights: { web: "primary" }` in `packages/registry/src/index.ts` — the only API entry in the whole catalog at `primary`, above `api.findings` and `api.tracks` — and it is the live four-tier resolver `docs/search.md` specifies (coordinate → exact entity → FTS5 token → the LLM filter tier), served by the public `search_archive` contract op (`packages/contracts/src/orpc/search.ts:147`, path `/search/archive`). A mechanical check of the five documents `agent-discovery.ts` serves finds it in exactly one: **static `llms.txt` YES** (`apps/web/public/llms.txt:61`), **markdown homepage NO** (`markdownHomeResponse`, the `text/markdown` rendering an Accept-negotiating agent gets), **A2A agent card NO** (`agentCard()`, `:191-240`), **`llms-full.txt` NO** (`renderLlmsFull`'s "## More" block, `:419-431`), **`.well-known` SKILL.md NO** (`skillMarkdown`'s "## Read the archive", `:558-564`). The failure is not merely absence, it is a MISLEADING PRESENCE, and that is what makes it an AEO defect rather than a gap. Three of the four documents that omit the archive search DO advertise the OTHER search op — `GET /api/v1/search`, the Spotify CANDIDATE search whose only purpose is to feed a submission — under the heading "Submit". The A2A card goes furthest: its `search-tracks` skill (`:192-199`) is described as "Search Spotify for track candidates by name or Spotify track URL", tagged `["drum-and-bass", "search", "spotify"]`. So an agent that ingests the agent card or SKILL.md — the two documents built expressly for machine capability discovery — is told Fluncle can be WRITTEN TO and searched against SPOTIFY, and is never told the drum & bass archive itself is queryable. `search_archive` does appear in SKILL.md, but only as a bare name inside the MCP `tools/list` sentence (`:583`), i.e. reachable only by an agent that already speaks MCP and connects — not by one reading the HTTP contract. This is the exact drift class the registry's own header warns about: it lists llms.txt (agent-discovery.ts) among the HAND-HONOURED consumers, where "this catalog states the intent, a human still wires the consumer", and unlike the doctrine doc (`doctrine-parity.test.ts`) and the SSH menu (`ssh-menu.test.ts`) nothing build-fails when the wiring is missed. Note the `pending` flag is not the explanation — `api.search.archive` is live and non-pending, and six in-repo callers already hit the endpoint (`components/search/search-command.tsx:141`, `lib/webmcp.ts:98`, `components/mix/mix-builder.tsx:303`, `components/recommendations/playlist-panel.tsx:72`, `components/chat/chat-coordinate.tsx:81`). | Add the archive search to the four documents that omit it, then close the class so the next `primary` surface cannot repeat it. (1) COPY: `apps/web/public/llms.txt:61` already carries a ratified line for this exact op — "search the archive by coordinate, artist, label, album, a bare word, or a plain-language question, as JSON" — so the drafting has a verbatim precedent to mirror rather than invent, and the register is settled (Narrator, machine-facing, honestly-plain third person). It still needs the `copywriting-fluncle` drafting gate and the `canon-reviewer` acceptance gate, because the A2A entry is not one line but a skill OBJECT (`description` + `examples` + `name` + `tags`) declaring a capability, and because `search-tracks`'s own description should be tightened in the same pass to say SPOTIFY CANDIDATES out loud so the two search skills cannot be confused. Filed rather than fixed on that gate alone: `canon-reviewer` cannot run unattended on this box (the precedent the `nav-model.ts:259` row set), and this is public agent-facing copy on the primary entity-recognition surface. (2) PLACEMENT is a small judgment call worth making deliberately: in the markdown homepage and SKILL.md the archive search belongs under "Read the archive"/"Data" with the other reads, NOT under "Submit" where the Spotify search sits — putting them adjacent is what produced the confusion. (3) THE DURABLE HALF: consider a coverage test asserting every non-`pending` `api`-kind surface at `web: primary`/`secondary` appears in `llms.txt` + the markdown home + SKILL.md, the shape `doctrine-parity.test.ts` already proves works over this catalog. That one is a build-gating net's decision logic, so it is the operator's call, not an auditor's — but it is the only fix that stops a sixth document drifting next time. | `audit/20260811-surfaces-seo` |
| 2026-08-10 | security | med | `packages/contracts/src/orpc/admin-tracks.ts:203` | The admin LOOSE-BODY convention routes input bounds around the one place the build can check them, so `input-caps.test.ts` is a hand-picked LIST rather than a derivation and its coverage is whatever a previous pass happened to write. 50 `z.looseObject(` bodies and 102 `z.unknown()` fields across `packages/contracts/src/orpc/**` declare their shape as "narrowed in-handler" (the convention is stated per body in-file — `admin-tracks.ts:198-201` for the presign body, `:207-211` for the finalize body beside it), which is a defensible tolerance choice but has a side effect nobody wrote down: a field typed `z.unknown()` has no size the contract can bound, so `input-caps.test.ts`'s `accepts(op, input)` helper — which reads `op["~orpc"].inputSchema` — cannot assert a ceiling on it even in principle. Nothing derives boundedness over the op registry the way `orpc-auth-coverage.test.ts` derives auth tier off the composed router, so an unbounded input is caught only if someone thinks to add a block. Tonight proves the shape both ways: `anchor_track` had an unbounded `candidates` array (fixed in this PR) despite its own contract naming the bounded `resolve_anchor` as its precedent, and it went unnoticed because no net looks. TWO RESIDUALS remain, both with an in-repo constant that would size them. (1) `presign_track_video_uploads` (`admin-tracks.ts:203`, AGENT tier) takes `fields: z.unknown().optional()`; the handler (`apps/web/src/lib/server/orpc/admin-tracks.ts:975-1013`) rejects a non-string and an unknown field but never caps the count or dedupes, so N copies of `"footage"` sign N presigned PUT URLs in one response. Blast radius is bounded and worth stating plainly: every URL still targets `<logId>/<known-artifact-name>` from the fixed 16-entry `VIDEO_ARTIFACTS` table (`apps/web/src/lib/server/video-bundle.ts:33`), so duplicates re-sign the SAME keys — the cost is SigV4 CPU and response size, never a new writable key in the world-served `fluncle-videos` bucket. (2) `set_clip_schedules` (`packages/contracts/src/orpc/admin-mixtapes.ts:624`, OPERATOR tier) takes `clipIds: z.array(z.string())` with no `.max()` and no per-id length cap, and its handler loops sequentially doing three DB round-trips per id (`admin-mixtapes.ts:830-835`). Neither is stranger-reachable — both sit behind an admin tier, so the threat is a buggy or compromised sweep/session, which is exactly the threat model `input-caps.test.ts` names in its own header. | Two decisions, in this order, and the first is the one that stops the class recurring. (1) RULE on whether the loose-body convention should carry contract-edge ceilings. The convention exists so a field can be normalized tolerantly in-handler rather than 400ing on a stale box build — a real property worth keeping — but tolerance about a value's SHAPE and tolerance about its SIZE are separable, and only the second is a footgun. The cheapest honest version keeps `z.unknown()` for scalars and requires a bounded `z.array(...).max(N)` for anything that is a LIST, since a list is the only axis that amplifies. Whatever is ruled, write it beside the convention's own statements (`admin-tracks.ts:198-201` and `:207-211`) so the next loose body inherits it. (2) Only then consider whether `input-caps.test.ts` should DERIVE rather than list — walk the op registry, and fail the build for any input schema exposing an unbounded array. That is attractive (it is the `orpc-auth-coverage.test.ts` shape, and that net has held) but it is restructuring a build-gating test's own decision logic, which the auditor's contract reserves for a human: it needs an allow-list for the ops where unboundedness is deliberate, and a `z.unknown()` field cannot be introspected at all, so the derived net would silently pass exactly the residuals above unless (1) lands first. The two residuals are then mechanical and each has its number ready: cap `fields` at 32 (≥2× the 16-entry `VIDEO_ARTIFACTS` table, `video-bundle.ts:33`) and dedupe before signing; size `clipIds` off whatever `list_clips` is bounded to once it HAS a bound (`admin-mixtapes.ts:288` currently takes no limit, which is why no constant sizes this one today — fixing that read is the prerequisite, not an afterthought). | `audit/20260810-security` |

| 2026-08-09 | voice | med | `apps/web/src/lib/server/voice-lint.test.ts:97` | The **published OpenAPI document's own prose is outside the voice lint's net**, and tonight proved it empirically: the only two prose em dashes left on any public Fluncle surface were both in it. `generateOpenApiDocument` (`apps/web/src/lib/server/orpc.ts:415`) assembles the spec served at `/api/v1/openapi.json` from three string sources, and a stranger reads all three in the Scalar reference at `/docs/api` and in the Postman export (`apps/web/src/routes/api/v1/postman[.]json.ts`): (1) each public op's `summary`, authored in `packages/contracts/src/orpc/*.ts`; (2) the `info` block (`title` / `summary` / `description`, `orpc.ts:418-426`); (3) the shared fault schema + response descriptions (`ERROR_SCHEMA` at `orpc.ts:348`, `ERROR_RESPONSE` at `orpc.ts:364`), attached as the `default` response on EVERY public op. None of the three is reachable from `SCAN_ROOTS` (`voice-lint.test.ts:97-119`): `packages/**` is excluded wholesale by the header's stated source boundary, and `apps/web/src/lib/server/orpc.ts` is in the "REST of `apps/web/src/lib`" that the header leaves out. The gap is not theoretical — this audit fixed `packages/contracts/src/orpc/search.ts:148` and `orpc.ts:366`, and both would have been caught on the build had the net reached them. Same shape as the two gaps the lint header already names (`apps/ssh/main.go`, `apps/web/public/*.txt`), but with a cleaner answer available than either. | Add a `describe` block over the GENERATED document rather than over source — the precedent is the existing "voice lint over the surfaces registry" block (`voice-lint.test.ts:577`), which rails the imported catalog as DATA for exactly this reason. `await generateOpenApiDocument()` already filters the admin tier (`filter: ({ contract }) => !isAdminPath(...)`, `orpc.ts:417`), so a data check over its output is public-only BY CONSTRUCTION and needs no admin/public split of its own — which matters, because operator `summary` strings legitimately use the em-dash clause join (§5's operator tier) and a raw source scan of `packages/contracts/src/orpc/**` would fire on dozens of them. Walk `document.info`, every `paths[*][method].summary`/`.description`, and `components.schemas.*.description`, and apply the same three rails plus the same escape hatch. Two open questions for the human before anyone writes it: (a) whether an OpenAPI `summary` is COPY at all or a machine field like an `operationId` — `/docs` is a Workstation in `references/voice.md` §5, so the Chrome register governs and the rails hold, but that has never been ruled explicitly for the spec; (b) whether the generator can be invoked inside `apps/web`'s vitest environment without the Worker bindings the router pulls in. Filed rather than fixed because it changes how the build-gating net decides what it covers, which the auditor's contract reserves for a human. | `audit/20260808-voice` |
| 2026-08-08 | design | med | `apps/web/src/routes/embed.$logId.ts:21` | The oEmbed card claims a typography exemption DESIGN.md does not grant, and its markup contradicts the claim in the same file. The header comment at `:21` reads "the sanctioned font fallback (system sans) keeps it font-load free", but DESIGN.md §3's **Canon Travels Rule** names its exemptions exhaustively — "The exemptions are the surfaces we do not control the type on, and they are the only ones: a TTY …, Raycast's own chrome, an HTML email …, and baked bitmap lettering inside a sprite" — and the oEmbed card is none of them: it is a standalone HTML document Fluncle itself serves from `fluncle.com`, so it fully controls the type. The rule's stated stake is exactly this surface's job ("A rendered artifact that falls back to the system sans is off-brand in the place the brand is most visible … the OG card is what lands in a link preview"), and the card is the OG card's sibling — the thing a pasted fluncle.com link becomes inside Notion / WordPress / Ghost. The document also disagrees with itself: `body` at `:90` sets `font-family:ui-sans-serif,system-ui,sans-serif`, while `.coordinate` at `:97` asks for `Oxanium` with no `@font-face` anywhere in the emitted `<style>`, so the coordinate — the one glyph run that is unambiguously brand — silently renders in the same system sans as everything else. `.nameplate` at `:96` (`font-weight:800; letter-spacing:3px; text-transform:uppercase`) is the brand lockup and never names a face at all. Note this is a claim-of-sanction defect, not a render defect: the card looks deliberate today, which is why nobody has caught it. Its palette half is already correct and was tightened in this PR (the last hand-copied hex now reads from `@fluncle/tokens`, matching the file's own promise that the card "cannot drift off-palette"); the type half has no equivalent. | Two defensible directions, and choosing between them is a canon call, not an auditor's. (a) EMBED THE FACES — the document is served from the same origin as `/fonts/oxanium-latin.woff2` and `/fonts/space-grotesk-latin.woff2`, so it can carry two `@font-face` blocks with the ratified `ascent-override`/`descent-override` metrics (copy them from `apps/web/src/styles.css`, do not re-derive — DESIGN.md §3's One Box Rule is what makes the coordinate and the title share an optical centre line). This makes the card obey the Canon Travels Rule, and costs it the "no external assets" property its header advertises: two woff2 subsets fetched inside a third-party iframe, `font-display: swap` so the card still paints instantly. That tradeoff is the whole decision — the card is deliberately tiny and asset-free, and a link preview is the one place a FOUT is most visible. Latin-subset-only is likely the right scope (the card renders a coordinate, a title and an artist), but a title in Latin-ext would then fall back mid-string, so decide whether the ext subset rides along. (b) AMEND DESIGN.md §3 to name the oEmbed card as a fifth exemption with its reason stated (an embedded card must stay asset-free, and a third-party page is not ours to make fetch fonts) — which requires touching the "and they are the only ones" clause, since it currently forecloses exactly this. Whichever wins, the file must stop contradicting itself in the same change: under (a) the `@font-face` blocks land and `.nameplate` names the brand face explicitly; under (b) `.coordinate:97` drops the `Oxanium` it can never load and the `:21` comment says WHICH exemption it is claiming rather than asserting an unnamed "sanctioned" one. Filed rather than fixed because (a) changes a public surface's network behaviour on third-party pages and (b) rewrites a canon rule's closed list. | `audit/20260807-design` |
| 2026-08-08 | design | low | `apps/web/src/components/track-row.tsx:59` | The **Checkpoint Row** as built diverges from its own canon spec on both halves of its one distinguishing line. DESIGN.md §5 "Checkpoint Row (the mixtape)" specifies "a title block of the mixtape name over a Stardust line carrying the member count and run time (`12 findings · 58 min`) **in place of** the BPM/key/tags chip row". The component renders the member count alone on the Stardust line (`{bangersLabel}`, `:59` — `bangersCount` at `lib/format.ts:86` yields "12 bangers", not "12 findings") and then renders the run time as a `Badge` in a chip row directly beneath it (`:62-68`, `formatAlbumDuration` → "58 min"), i.e. in the exact slot the canon sentence says the checkpoint row does not use. The divergence is deliberate and self-documented — the comment at `:60-61` gives the reason ("mirrors a finding's duration chip, so a checkpoint row stands the same height as the rows around it"), which is a real layout argument the canon sentence does not answer. The noun half is separately live: DESIGN.md §2's **Unlit Rule** closes with "**'Finding' is the only named object in Fluncle's world**", so `12 findings` vs `12 bangers` is a VOICE.md question about what a mixtape's members are called, not a typo. Nothing renders wrong; the row is legible, on-palette, and reduced-motion-clean. What is wrong is that the canon and the code state two different specs for a named signature component, so the next agent to touch either one has no way to tell which is authoritative. | An operator ruling per half, then make the loser follow. (1) PLACEMENT — either move the run time onto the Stardust line as the canon spells it (`{bangersLabel} · {formatAlbumDuration(...)}`, dropping the `Badge` row) and accept that a checkpoint row then stands one chip-row shorter than the finding rows around it, which is the exact regression the `:60` comment was written to prevent; or keep the chip and amend DESIGN.md §5 to say so, since the row's OWN rationale (a checkpoint should not visually shrink beside a finding) is a better reason than the canon currently records, and the canon is the place a reason like that belongs. Note the second branch is the cheaper one and the one with the argument already written down. (2) NOUN — settle `bangers` vs `findings` against VOICE.md before touching either file; `bangersCount` is shared (`lib/format.ts:86`), so a change there is cross-surface and needs a grep of its call sites, not a local edit. Filed rather than fixed because branch (1a) changes a shipped public row's layout, (1b) edits canon, and (2) is a voice ruling — three calls, none of them an auditor's, and all three want to land together so the doc and the row stop disagreeing. | `audit/20260807-design` |
| 2026-08-07 | db-query-shape | med | `apps/web/src/lib/server/backfill-artist-edges.ts:334` | The two artist backfill sweeps each pull the WHOLE `artists` table into the Worker isolate, once per pass, and both call it bounded when it is not. `loadArtists` (`backfill-artist-edges.ts:334`) and `loadArtistCorpus` (`backfill-artist-credits.ts:254`) are the identical statement — `select id, name, mbid from artists`, no `where`, no `limit` — and each is documented in-file as "one bounded read"; the edges module's header goes further and states a size, "The whole artist + alias corpus (~1.8k rows)". That number is stale and the word is wrong: `artists` is a GROWING table on the crawl's axis, not the archive's. The credits sweep beside it mints a row per unmatched MusicBrainz credit (`mintArtistByMbid`, `artists.ts:1305`), and the repo's own scale rig sizes the 150k-track regime at `SCALE_ARTISTS` **30,000** artists (`apps/web/scripts/lib/scale-seed.ts:418`). The corpus does not merely cross the wire — each pass then expands it into two in-memory structures (`buildArtistFoldMap` → one Map keyed by fold; `buildIdentityClaimedNames` → a Map of Sets holding every claimed spelling), so the JS heap cost is a multiple of the wire cost, on a `--no-agent` sweep that runs hourly under two box crons (`fluncle-artist-edges`, `fluncle-artist-credits`). This is trap 3 of `docs/local-database.md` § "Local is not production" in its exact backwards-from-safe form: `turso dev` caps a response at 10 MiB and would THROW, so dev fails loudly at the size that hosted — which has no cap — absorbs silently, right up until it OOMs the 128 MB isolate. Nothing is failing today; the shape simply has no ceiling and no alarm between here and the one it will hit. Not fixed, for two independent hard-rail reasons: the design is a WRITTEN RULING in a canon doc — `docs/artist-relationship.md:44`, "the resolver loads the whole `artists` corpus (id + name + mbid) ONCE per pass to build the fold map (rung 2 is impossible without every name in memory)" — and every durable alternative needs a stored column plus a migration. | An operator ruling between two directions, both of which reverse the doc's sentence, so neither is an auditor's to take. (a) STORED FOLD KEY — add a maintained `artists.name_fold` (and `artist_aliases.alias_fold`) written by the same statement as every write of its source, exactly as `has_embedding` / `has_isrc` are, plus the seeder entry the guardrail section of `docs/db-scale-backlog.md` requires ("Add a new maintained column, add it to the seeder in the same change"). The sweeps then fold each batch's ~200 credit names in the isolate and ask `where name_fold in (…)` — bounded by the batch, not the table — which also makes the ambiguity rule (`fold` shared by two identities ⇒ match nothing) a `group by name_fold having count(distinct id) = 1` instead of a full-corpus pre-pass. This is a migration + a maintained column + an index, so it is gated on a hosted-Turso proof at 150k via the `apps/web/scripts/bench-db-scale.ts` pattern — seed with `scripts/lib/scale-seed.ts`, measure p50 + `EXPLAIN QUERY PLAN` before/after through `@libsql/client/web`, destroy the DB — operator-only, since Turso Cloud credentials are not on the audit box. (b) KEEP the in-memory fold and BOUND it honestly: page the corpus read (`order by id limit N`, N a named constant) and have the pass process the batch against one page at a time, accepting that a name whose artist falls outside the page is an unmatched residual the next tick catches — cheaper, no schema, but it trades exactness for a ceiling and that is a product call. Whichever wins, correct the two "one bounded read" comments and the "~1.8k rows" header in the same change, and amend `docs/artist-relationship.md:44` so the doc and the code agree about which axis `artists` grows on. | `audit/20260806-db-query-shape` |
| 2026-08-07 | db-query-shape | med | `apps/web/src/lib/server/search.ts:365` | Search's ENTITY tier scans the growing `artists` table once per query, and the file's own comment said it could not. `entitySql`'s artist arm compiles `where lower(artists.name) <predicate> or exists (select 1 from artist_aliases where artist_aliases.artist_id = artists.id … and lower(artist_aliases.alias) <predicate>)`, and this runs on EVERY search that reaches tier 2 (exact entity) or tier 3 (prefix), for each of five entity kinds. Two things make it unindexable at once: `lower()` wraps the column, so `artists_name_nocase_idx` (`name collate nocase, slug`) cannot be seeked; and a correlated `exists` is not an indexable `or` arm, so SQLite cannot build a multi-index OR and falls back to the table. `EXPLAIN QUERY PLAN` on the real shape (local libSQL, schema-identical — plan evidence, not a hosted number) is `SCAN artists` + `CORRELATED SCALAR SUBQUERY … SEARCH artist_aliases USING INDEX artist_aliases_artist_id_idx` — a full walk of the table with one alias probe per row. The doc block above it claimed the read was bounded "over an `artists` table that stays archive-sized"; `artists` grows with the crawl (`mintArtistByMbid`, `artists.ts:1305`; 30,000 rows at the 150k regime per `scripts/lib/scale-seed.ts:418`), so that premise is false and it is corrected in this PR — the SHAPE is unchanged. The label/album arm carries the same `lower(<table>.name) <predicate> or exists (<alias probe>)` over `labels`/`albums`, plus a correlated cover subquery into `tracks` evaluated for every row that clears the WHERE — i.e. before the `LIMIT`, not for the page. Filed rather than fixed because the fix changes what PUBLIC SEARCH RETURNS and its ordering. | Apply the two-rank split this PR already shipped one call site of. `resolveFilterArtistId` (`search.ts`, same file) is the worked example: `order by name_rank asc … limit 1` over an `or` is exactly "ask rank 0, then rank 1", so it became two statements — rank 0 as `artists.name = ? collate nocase or artists.slug = ?` (a MULTI-INDEX OR over the two existing indexes; `collate nocase` is character-for-character equal to `lower()` here because SQLite folds ASCII only in both), rank 1 driven FROM `artist_aliases` with a PK join back. The entity tier is the same rewrite with two complications that make it a design call rather than a mechanical one, and they are why it is here: the `LIMIT` has to be SPLIT across the ranks (take up to N rank-0 rows, then fill the remainder from rank 1), and an artist matched BOTH by name and by an AKA must be de-duplicated or it renders twice — neither of which the single-statement form ever had to state. Prefix tier 3 also needs checking separately: `name like ? || '%'` can ride a NOCASE index where `lower(name) like …` cannot, but only with no leading wildcard, so the two tiers want the same predicate spelled once and asserted. The alias rank stays a scan of `artist_aliases` (only `alias_slug` is indexed, and matching on the slug instead of `lower(alias)` would change which spellings resolve) — if an index on the raw alias is proposed to close that, that half is gated on a hosted-Turso proof at 150k via the `apps/web/scripts/bench-db-scale.ts` pattern (seed with `scripts/lib/scale-seed.ts`, p50 + `EXPLAIN QUERY PLAN` before/after through `@libsql/client/web`, destroy the DB), which is operator-only. Do the label/album arm in the same pass, and while there decide whether the cover subquery should move to a bounded second lookup over the page's slugs — the shape `listHubPage`'s `hubTiles` already uses for exactly this reason. | `audit/20260806-db-query-shape` |
| 2026-08-06 | tests | med | `apps/web/vitest.config.ts:39` | The `.tsx` COMPONENT LAYER is effectively outside `deploy:gate`: 176 non-test `.tsx` sources under `apps/web/src` are matched by exactly 6 `*.test.tsx` files (`components/watch-button.test.tsx`, `components/catalogue-hub-section.test.tsx`, `components/behind-the-scenes.test.tsx`, `components/nav/nav-footer.test.tsx`, `components/chat/neighbour-card.test.tsx`, `routes/-identity-page.test.tsx`), and the v8 report from tonight's full run puts the rest at literal 0% statements/branches/functions/lines — every `/admin` dialog and door included (`saves-door.tsx`, `findings-door.tsx`, `clip-card.tsx`, `push-dialog.tsx`, `recording-cue-rail.tsx`, `auth-forms.tsx`, `galaxy-door.tsx`, `track-row.tsx`). The technique is NOT missing: those six render with `renderToStaticMarkup` from `react-dom/server` under the existing `environment: "node"` (`vitest.config.ts:39`) — no jsdom, no happy-dom, no `@testing-library` anywhere in the repo — and `components/watch-button.test.tsx:7` names the pattern in its own header ("vitest env = node, no DOM — the catalogue-hub-section.test.tsx style"). What guards the other 170 is real but sits OUTSIDE the deploy boundary: the Playwright suite lives in `apps/web/tests/e2e/**` (7 specs), which `vitest.config.ts:43`'s `include` (`src/**` + `scripts/**`) does not reach, so `deploy:gate`'s `turbo run test` never runs it — it runs as its own `e2e.yml` GitHub Action — and the three admin browser smokes are a local desktop routine by design (`docs/agents/smoke-routine.md`: "NOT a box cron — it needs the seeded local DB, a real Chrome, and the operator's local dev-vars file"). So a component regression a first paint would expose can reach the Cloudflare build with nothing in the gate that renders it. This is also what dominates the coverage ratchet: the floors were re-ratcheted tonight to `floor(measured − 4)` per `vitest.config.ts`'s own rule, but the headline number cannot move far while ~170 files sit at zero. | Not a one-file add and not an auditor's call: it is a harness decision plus a per-component judgment about what a first paint is even supposed to assert. Sequence it. (1) RULE on the harness — either stay on `renderToStaticMarkup` under `environment: "node"` (zero new dependencies, and the six existing files prove it carries the assertions that matter for a dark, cover-led, SSR-first app: does this render at all, does it render NOTHING for a signed-out visitor, does it emit the right `href` / `alt` / `aria-label`), or add a DOM environment for the interaction half — a real dependency add, and AGENTS.md's search-first rule wants the SSR-only branch considered and rejected on the record first, since `components/watch-button.test.tsx:9-11` already draws that line ("that half is a network fact, verified in a real browser past hydration rather than here"). (2) PICK the first tranche by blast radius rather than sweeping 170 files: the never-gates render guarantees (any component that must render empty for a signed-out visitor — the `WatchButton` precedent, a product law and the cheapest possible assertion) and the `/admin` write dialogs, whose regression costs the operator a mutation. (3) Only then consider whether the e2e suite belongs inside `deploy:gate` — that is a CI/deploy-config change on the auditor's hard-rail list and a real build-time cost, so it is the operator's call, not a side effect of this row. | `audit/20260805-tests` |
| 2026-08-05 | docs | low | `docs/agents/hermes/Dockerfile:240` | The baked Oxanium TTF is an ORPHANED image asset: nothing in the repo reads it. The Fluncle Studio clip cut once stamped a brand frame over the footage, and that frame was removed — `apps/cli/src/commands/clips.ts:11-14` states it ("the cut ships CLEAN … the brand frame the earlier design baked in (title + coordinate + Track-ID + ink-halo + fonts) has been removed"), `docs/fluncle-studio.md:90` specifies the filtergraph as crop-only ("No drawtext, no fonts, no `gblur` halo, no overlay"), and `apps/cli/src/commands/clips.test.ts:61` PINS it (`expect(filter).not.toContain("fontfile")`). The `resolveClipFontFile()` / `resolveClipSansFontFile()` resolvers and the `CLIP_FONT_FILE` / `CLIP_SANS_FONT_FILE` env vars named by the Dockerfile's own comments (`:236-238`) no longer exist anywhere in the tree — a repo-wide grep finds them only in comments and docs. The stale DOCS were fixed tonight (`apps/cli/assets/fonts/README.md`, the two Hermes cron READMEs, `clip-sweep.sh`'s comments). What is left is the asset and the build: `Dockerfile:240` still `COPY`s `Oxanium-SemiBold.ttf` to `/opt/fonts/`, `:241` still chmods it, and `apps/cli/assets/fonts` is one of the three paths in pin-watch's baked-content fingerprint (`docs/agents/hermes/pin-watch/README.md:10`, `pin-watch/rebuild-hermes.sh:81`). So every Hermes image still carries a font no code opens. Not fixed: the Dockerfile is deploy config on the auditor's hard-rail list, editing it changes the pin-watch fingerprint and triggers a box image rebuild, and no Docker toolchain exists on this box to verify the build — an unverifiable edit is a hard rail. | Decide whether the font is retired or deliberately kept, then make the build say so. RETIRE is the smaller, more honest branch: drop `Dockerfile:236-241` (the three comment lines, the `COPY`, the `chmod`), delete `apps/cli/assets/fonts/` (TTF + `OFL.txt` + README), and drop `apps/cli/assets/fonts` from `pin-watch/rebuild-hermes.sh:81`'s fallback superset — note the README's own warning that the fallback must stay a superset, so that line and the Dockerfile `COPY` must move together or the fingerprint silently shrinks. KEEP is defensible if the brand frame is expected back (the clip cut is young and the caption currently rides the post): then leave the asset and instead correct `Dockerfile:236-238`'s comments, which today name two functions that do not exist. Either way verify with a real image build plus one `fluncle-studio-clip` tick, which is operator-side. Worth settling because the misleading half is the comment: an agent grepping `resolveClipFontFile()` finds it named in the Dockerfile as live behaviour and will go looking for code that was deleted. | `audit/20260804-docs` |
| 2026-08-04 | surfaces-seo | med | `apps/web/src/lib/nav-model.ts:259` | `/tracks` and `/identity` are SITEMAP-ONLY: they are submitted for indexing but carry no inbound link from any indexed page, so no internal authority reaches them. `nav-model.ts`'s four sections (`travel` / `browse` / `listen` / `crew`, `:259-262`) enumerate every other public index and neither of these, yet the file's own header states the contract it is failing — "the crawl graph still gets its links". An exhaustive grep of `apps/web/src` outside `/admin` and the tests finds exactly ONE inbound link to `/tracks` (`routes/identity.index.tsx:132`, "All tracks") and exactly ONE to `/identity` (`routes/identity.$key.tsx:122`, plus `components/identity-lookup-form.tsx:30`'s `<form action="/identity">`, which is a GET form and not an `<a href>` a crawler follows). The one page that links to `/tracks` is therefore `/identity`, whose own only inbound links come from `/identity/<key>` pages that render `noindex, follow` and are deliberately out of the sitemap (`routes/identity.$key.tsx:72`). Both hubs ARE listed unconditionally in the sitemap's static `pages` child (`lib/sitemap.ts:384`, `:415`) and advertised in `public/llms.txt:24` and `:58`, and both are `web`-weighted in the registry (`packages/registry/src/index.ts` — `web.tracks` secondary, `web.identity` tertiary). The GSC 28-day window (2026-07-03 → 2026-07-31, `.audit/seo-data.json`) corroborates: neither URL appears in the 100-row page sample, which reaches all the way down to 1 impression — so both sat at ZERO impressions while `/labels` took 66 and 90 album pages took 168 between them. Not fixed: `lib/nav-model.test.ts:17` pins the section membership as a curated contract ("reaches every public index surface", a hand-listed set that names neither), and `:42` pins the travel/browse fork item-for-item, so adding a berth edits a ratified list; the new entry is also public copy needing the `copywriting-fluncle` drafting gate and the `canon-reviewer` acceptance gate, and the latter cannot run unattended on this box. | Decide, per hub, whether it earns a colophon berth, then wire the winner through `nav-model.ts` + its two pinned tests together. `/tracks` has the clearer case and the clearer home: it is "the whole list, newest release first, filterable by release year, tempo, key, and label" (`public/llms.txt:24`) — the music's own taxonomy, which is precisely what the `browse` section is defined to hold ("what he found it AMONG"), so it slots beside `artists` / `albums` / `labels` / `fresh` without disturbing the fork the header argues for. `/identity` is the harder call and may legitimately stay a door rather than a destination: it is a lookup surface, not a browse surface, and `crew` (about / reach / newsletter / docs / pipeline) is the only section it could sit in without breaking the fork. Either way the label goes through `copywriting-fluncle` and lands as chrome under the Chrome Rule (literal words, no garnish), and both pinned lists in `nav-model.test.ts` update in the same commit. If the ruling is that neither belongs in the nav, the honest alternative is to say so in `nav-model.ts`'s header and give `/tracks` an in-page link from a hub that IS linked (`/fresh` and `/albums` are the natural parents) — sitemap-only is a state worth choosing on purpose, not drifting into. | `audit/20260803-surfaces-seo` |
| 2026-08-04 | surfaces-seo | low | `apps/web/src/routes/labels.index.tsx:63` | Ranked recommendation, not a defect: `/labels` is the single biggest search surface Fluncle has and it is stuck on page four. GSC 28 days (2026-07-03 → 2026-07-31, `.audit/seo-data.json`): the page took **66 impressions, 22% of the site's 301**, at average position **37.5** for 1 click (1.52% CTR). The query side names the cluster it is losing — "drum and bass labels" 16 impressions at position 48.4, "drum and bass record labels" 9 at 39.9, "drum n bass labels" 2 at 49.0, "drum n bass record labels" 2 at 48.0, "drum and bass label" 2 at 55.0: 31 impressions across five variants of one intent, none of them ranking. The metadata is NOT the gap and should not be re-tuned — `labels.index.tsx:66-69` already carries the exact phrase in both the title and the description, paged variants vary per page, and the page emits a `CollectionPage` + `ItemList` with `numberOfItems`. The gap is that a bare alphabetical list of names is a thinner answer than what ranks for that query: a searcher asking "drum and bass labels" wants to know which labels matter and why, and the page hands them an index. Filed rather than fixed because closing it means adding editorial content to a canon surface, which is product direction, not an audit edit. | Give the hub something to rank WITH, in the order the effort pays back. (1) The cheapest and most defensible: the `/label/<slug>` pages already carry an authored Fluncle-voiced bio (`docs/agents/bio-agent.md`, the `fluncle-label-bio` cron), so the hub could lead each certified row with its bio's first sentence instead of a bare name — real prose, already written, already canon-gated, no new authoring. (2) Order or group the certified rows by something a reader would ask for (findings held, era, imprint lineage — the `labels` table already carries lineage per `docs/label-entity.md`) rather than only A to Z, and keep the alphabetical lane as the fast path it already is (`components/catalogue-hub-section.tsx`). (3) Only then consider a lede that answers the query directly, which is the part that needs `copywriting-fluncle` and `canon-reviewer` and the part the operator should rule on, since a hub that explains itself is a different page from a hub that lists. Re-measure against this row's baseline (66 impressions, position 37.5) after a full 28-day window; position is the metric to watch, not CTR. | `audit/20260803-surfaces-seo` |
| 2026-08-03 | security | med | `bun.lock:1` | `bun audit` reports **8 advisories (4 high, 3 moderate, 1 low)** across 6 packages. Every one is TRANSITIVE — a name-by-name check of every `package.json` in the workspace finds none of them declared as a direct dependency — and every one arrives through BUILD/DEV tooling rather than the deployed Worker's runtime path. The current set, by package: `sharp` (via `wrangler`, `@cloudflare/vite-plugin`, `@huggingface/transformers`) carries one **high** row covering four libvips CVEs; `adm-zip` (via `@huggingface/transformers`) a **high** 4GB-allocation zip bomb; `image-size` (via `expo`, `react-native`, `react-native-worklets`) two **high** infinite-loop DoS rows in its ICNS and JXL/HEIF parsers; `esbuild` (via nine parents including `vite`, `wrangler`, `drizzle-kit`, `@tanstack/react-start`) the **moderate** dev-server request/response read plus a **low** Windows-only arbitrary file read; `uuid` (via `expo`) a **moderate** missing buffer bounds check; `file-type` (via `node-vibrant`) a **moderate** ASF-parser infinite loop. The exposure is therefore the BUILD BOUNDARY, not the request path — which still matters here, because a push to `main` auto-deploys through Cloudflare Workers Builds and `deploy:gate` runs the whole toolchain on that runner. Not fixed: AGENTS.md assigns bumps to the `fluncle-maintenance` skill, a dependency bump is on the auditor's hard-rail list, and the workspace's `minimumReleaseAge` freshness gate (`bunfig.toml`) deliberately holds a 3-day quarantine that an audit-time bump would jump. | Run the `fluncle-maintenance` dependency pass and let it judge each family on its own merits rather than firing one blanket `bun update`. Sequence by reachability, not by advisory count: the packages that execute on the DEPLOY runner (`sharp` and `esbuild`, both reached via `vite`/`wrangler`/`@tanstack/react-start`, and `adm-zip` via `@huggingface/transformers`) are the ones whose compromise would reach production, so they go first; the `expo`/`react-native`/`@raycast/*`/`node-vibrant` paths are local-or-mobile-build only and can ride a later sweep. Most resolve by bumping the DIRECT parent (`vite`, `wrangler`, `@tanstack/react-start`, `expo`) rather than by pinning a transitive, so check the parent's changelog first and only reach for a `resolutions`/`overrides` pin where the parent has not yet shipped the fix — a pin is a fork of someone else's tree and wants a comment saying when it can be dropped. Re-run `bun audit` as the acceptance check and record the residual count, since some of these (the `@raycast/api` and `expo` chains especially) will have no fixed version available and the honest outcome is a smaller number, not zero. | `audit/20260802-security` |
| 2026-08-03 | security | low | `.github/workflows/gitleaks.yml:48` | The topology half of the public-repo rule is enforced for TWO of the classes AGENTS.md forbids and none of the rest. `gitleaks.yml` runs three steps: the gitleaks CLI over full history (secret VALUES), a working-tree `git grep -nE 'op://[A-Za-z0-9]'` (step 2, `:48`), and a working-tree `git grep -nE '/Users/[a-z]'` (step 3, `:58`). AGENTS.md "Public Repo" forbids a wider list than that — "concrete `op://<vault>/<item>` 1Password paths, hostnames, IPs, identifying service ports, internal URLs, tailnet names, webhook URLs, or local `/Users/...` paths" — so a committed `*.ts.net` tailnet FQDN, a bare IP literal, a real Discord/Slack webhook URL, or an internal hostname would pass CI silently. This is a LATENT gap, not an active leak: a sweep of the working tree tonight found zero `op://` hits of any kind, zero `.ts.net` names, no webhook URL outside test fixtures using obviously-fake tokens (`discord-alert.test.ts:19`, `.../webhooks/test/token`), and every IP-literal hit a synthetic test fixture (`1.1.1.1`, `8.8.8.8`, `9.9.9.9` in the identity/public-write-rails integration suites). So nothing is leaking — the net simply does not cover the classes the rule names, and the two it does cover are the two a previous pass happened to write. | Extend the working-tree hygiene step to the remaining named classes, each as its OWN `git grep` with its own error message (one combined regex would make a hit unreadable, and these need different carve-outs). The tractable ones, in order of signal-to-noise: a tailnet FQDN (`\.ts\.net\b`) has no legitimate committed use and needs no carve-out; a real webhook URL (`https://(discord\.com/api/webhooks|hooks\.slack\.com)/` with a path segment past the host) wants the test fixtures excluded by path. IP literals are the one to think hard about before adding — the test suites are full of deliberate synthetic addresses, so a naive `(\d{1,3}\.){3}\d{1,3}` would redden CI on `1.1.1.1` and train everyone to ignore it; either scope it to non-test paths or skip it and rely on review. Filed rather than fixed for two reasons that both stand on their own: `.github/workflows/*` is on the auditor's hard-rail list (a workflow edit runs arbitrary code with repo secrets), and the choice of WHICH classes to mechanize — versus leaving to `secret-hygiene-reviewer` and human review — is a posture call about how much false-positive noise the gate should carry, which is the operator's to make. Whatever is added, mirror the reasoning into AGENTS.md's Public Repo section so the doc and the gate name the same list. | `audit/20260802-security` |
| 2026-08-02 | architecture | med | `apps/web/src/lib/server/apple-breaker.ts:78` | The two SETTINGS-VALUE coercion helpers are copy-pasted six ways across `lib/server`, and neither has a home. `parseCount(raw, fallback = 0)` — digits-only, `Number.isSafeInteger`-guarded, degrade-to-fallback — is byte-identical in three modules (`apple-breaker.ts:78`, `spotify-anchor-breaker.ts:123`, `spotify-budget.ts:66`), all reading the rolling breaker/call-budget tallies `settings.ts` documents as shape (5) RUNTIME STATE. `parseDial(raw, fallback, min, max)` is byte-identical in three more (`logbook-echo.ts:48`, `note-rejections.ts:47`, `observation-rejections.ts:35`), and those three go further than sharing a parser: all three parse the SAME two dials (`maxOverlap`, `minPhraseWords`) with the SAME bounds (`0.05..1`, `2..20`) against their own `*_ECHO_DEFAULTS` — `settings.ts` shape (4) VOICE-GATE DIALS. The obvious home is wrong twice over. `lib/server/query-params.ts` (which already holds the sibling `parseBool`/`parseLimit`) scopes itself in its header to "raw query params", and these read the flag store, not a URL. **`lib/server/settings.ts` is worse, and this was tried and reverted tonight**: hoisting `parseCount` there typechecks and lints clean but fails 12 tests across `spotify-anchor-breaker.test.ts` + `frontier-playlist.integration.test.ts`, because those suites `vi.mock("./settings", …)` with an explicit factory — so a PURE function living in the DB-backed store gets mocked away with the store, and every caller silently gets `undefined`. Filing rather than fixing is the finding, not an excuse: the naive hoist is actively wrong, and the failure names the constraint. | Give the pure coercers a pure home — the constraint the reverted attempt proves is that it must be a module no suite mocks wholesale, which rules out `settings.ts` and (by its own stated scope) `query-params.ts`. Two defensible directions, and the choice is the operator's because it sets where the NEXT settings reader looks. (a) BROADEN `query-params.ts` from "query-param coercion" to "tolerant scalar coercion for the web's string-typed inputs" — rewrite its header, move `parseCount` + `parseDial` in beside `parseBool`/`parseLimit`; one module, four siblings, no new file, and the mock problem cannot recur because nothing mocks it. (b) NEW `lib/server/settings-values.ts` as the coercion companion to the store, keeping `query-params.ts` honest to its name — but that is a new abstraction, so AGENTS.md's search-first rule wants (a) considered and rejected on the record first. Either way `parseDial`'s three call sites deserve a second look as one unit: three echo gates hand-rolling the same two bounded dials is a shared ECHO-GATE CONFIG, not just a shared parser, and collapsing that is the larger and more valuable half. | `audit/20260801-architecture` |
| 2026-08-02 | architecture | low | `apps/web/src/lib/server/sonar.ts:102` | The six sonar dark-flag SETTERS have no caller anywhere in the repo and no test. `setSonarSonicEnabled` (`:102`), `setSonarArtistsEnabled` (`:107`), `setSonarLogEnabled` (`:112`), `setSonarRecsEnabled` (`:117`), `setSonarRecsCatalogueEnabled` (`:126`) and `setSonarMixEnabled` (`:131`) are each referenced exactly once — at their own declaration. A scan of every git-tracked file finds no second reference (an earlier count of 2 was `apps/web/coverage/**`, a gitignored artifact). Their six sibling GETTERS are all wired and covered (`sonar.test.ts:205-210` table-tests them; `tracks.ts:1876` calls `isSonarMixEnabled`), so this is one-sided. There is also no generic settings-write op: no `set_setting` contract op, no `fluncle admin settings` CLI group. `docs/vector-serving.md:88` names these functions as the mechanism — "`setSonarSonicEnabled` and its five siblings write `\"true\"`/`\"false\"` through the same one flag store" — and in the same breath rules that "**There is deliberately no `/admin` UI for these yet:** they are commissioning switches … Add the board when the flipping becomes routine, not before." Yet the same doc's table records `sonar_sonic_enabled` and `sonar_recs_enabled` as **ON in production**, so those flips happened through a path that is not this code — a hand-written row against the prod `settings` table. The gap is doc-to-code, not a defect: nothing is broken, but the repo's stated way to flip a production feature flag is unreachable from every surface. | Operator ruling on which half is true, because both directions are defensible and one of them reverses a written deferral. (a) DELETE the six setters and amend `docs/vector-serving.md:88` to say plainly that a commissioning flip is a manual `settings` row until the board exists — smallest diff, removes six untested functions, and makes the doc match what actually happens. (b) WIRE them, which means the `/admin` board the doc explicitly defers, or an `update_setting`-shaped op — and that is a NEW public surface, so it drags in the `verb_noun` convention, both coverage nets, and an auth tier (`operatorGuard`, since flipping a production feature flag is not agent tier). Not fixed here because (a) edits a doc that states a deliberate product decision and (b) crosses the auth-tier hard rail. Worth settling either way: six dead exports that a canon doc points at will keep reading as live API to the next agent that greps for how to flip a flag. | `audit/20260801-architecture` |
| 2026-08-02 | architecture | low | `apps/web/src/lib/server/mixtapes.ts:772` | Survey row (worklist, not one defect): after tonight's three hoists, a normalized-body scan over `apps/web`, `apps/cli` and `packages/*/src` found 18 clusters of duplicated function bodies (the count at that scan; four have closed since). The largest coherent one is the ADMIN BODY-FIELD VALIDATORS — `optionalText` in `mixtapes.ts:780` / `subscriptions.ts:287` / `editions.ts:425`, `optionalIsoDate` in those three plus `recordings.ts:863`, and the same validator under two NAMES: `requireText` (`mixtapes.ts:772`, `recordings.ts:841`) vs `requiredText` (`subscriptions.ts:253`). These are what actually validate the admin write bodies, because the matching contracts deliberately type those fields tolerantly (14 `z.unknown().optional()` inputs across the admin contracts, all inputs — no output schema is weakened). The leaf-utility and shared-React half of this list has since been worked down (`releaseInstant`, `readApiError`, `elapsedShort`, `GateNotice`, plus the three sibling fresh-feed item helpers `itemTitle`/`itemLink`/`itemId` the original scan missed) and is no longer open. What remains, each verified byte-identical modulo the function name: `loadAliases` ×2 (`backfill-artist-credits.ts:261`, `backfill-artist-edges.ts:341`), `step` ×2, `fnv1a` ×2 (`lib/log-id-shared.ts:11` ↔ `packages/contracts/src/util/galaxy-slug.ts:20`), `balancedBraces` ↔ `sliceBalancedBraces`. NOT in scope and deliberately excluded: `albumSlug` (`albums.ts:102`) ↔ `labelSlug` (`labels.ts:97`) are the same algorithm but generate URL and DB keys, so unifying them risks changing a live slug — a data question, not a refactor; and `fftInPlace`/`hannWindow` across `packages/{live,video}` is a documented, deliberate vendoring already enforced by `packages/live/src/bridge/fft-vendor-pin.test.ts`. | Work the list down opportunistically rather than in one sweep — each cluster is individually small and low-risk, but together they touch ~20 files across four apps, and a single mega-PR is the wrong shape to review. The sequenced blast-radius order held: the pure leaf utilities went first, then the shared React bit, and what is left is the admin validators — those want the same "where do shared server-input coercers live" answer as the MED row above, so settle that first and land both under it. Two carry their own question: `fnv1a` would make `@fluncle/contracts` export a hash primitive (a public-surface widening for a 10-line function — probably not worth it), and the `requireText`/`requiredText` name split should collapse to one spelling whichever way the validators go, since two names for one body is the rot that makes the next copy look justified. | `audit/20260801-architecture` |
| 2026-08-01 | voice | med | `apps/web/src/routes/about.tsx:25` | The `/about` FAQ speaks in TWO narrators inside one visible list, because the same nine strings serve two registers that canon governs differently. `about.tsx:23` states the coupling on purpose: "The FAQ once, as data: the visible section and the FAQPage schema render from the same strings so they cannot drift apart" — the array feeds both the `<section aria-label="Questions">` block a reader scrolls (`about.tsx:262`) and the `FAQPage` JSON-LD (`about.tsx:103`). Seven answers describe Fluncle in the third person ("Fluncle is the selector behind Fluncle's Findings", "He doesn't search blind", "Fluncle plays a tune, and if it moves the room it gets a coordinate") and two speak as him in the first ("The Galaxy is everywhere **I** send my findings"; "**I** measure them myself, I don't copy them from anywhere"). A reader meets Fluncle described, then Fluncle speaking, then described again. Both registers are canon and they collide here: `references/voice.md` §5 files `/about` under **Lore pages** — "Full voice: first person, ... the §4 stack governing every line" — while §6 (Narrator) carves out "the machine-facing entity description and the SEO scaffold ... where he is described, not speaking, for search and LLM entity recognition: there, honestly-plain active third-person". The rest of the page already picks a side per section and reads cleanly for it: the narrative section is first person throughout (`about.tsx:180-203`) and the "The short version" definitions are third person (`about.tsx:207-243`). The FAQ is the one section that cannot pick, because one string is rendered and serialized at once. Note this is register only — no rail is broken (no banned word, no em dash, no exclamation, sentence case holds), which is why the mechanical lint cannot see it. | An operator ruling on which register owns a dual-purpose string, then a copy pass in that direction. Two defensible branches. (a) MACHINE-FIRST: rule the FAQ part of §6's SEO scaffold (it IS a `FAQPage` payload for LLM extraction, and Google no longer renders FAQ rich results, so its only consumer is machine) and convert the two first-person answers to third person — the smaller edit, two strings, and it makes the majority register deliberate instead of accidental. (b) LORE-FIRST: hold §5's "Lore pages ... full voice, first person" and convert the seven third-person answers, accepting that the `FAQPage` JSON-LD then answers in "I", which is exactly the entity-recognition clarity §6's carve-out was written to protect. Filed rather than fixed because the branches disagree about what `/about` IS, both rewrite canon copy on the primary entity page, and (b) trades measurable AEO/entity clarity for voice — a product-direction call, not an auditor's. Whichever branch wins should be written into §5 or §6 as a sentence about dual-purpose strings, so the next surface that renders and serializes the same copy has a rule to follow. Both paths need the `copywriting-fluncle` drafting gate and the `canon-reviewer` acceptance gate. | `audit/20260731-voice` |
| 2026-07-31 | design | med | `apps/web/src/styles.css:341` | DESIGN.md §5 sets the ambient budget at "exactly two movements, both imperceptible and both gated to `prefers-reduced-motion: no-preference`: the 72s cosmos drift and the ~48s sun-bloom breath — and nothing else moves uninvited" (§2's Live Exception grants the live pulse as the ONE sanctioned ephemeral third). The homepage ships FOUR concurrent ambient movements: `cosmos-drift` 72s (`styles.css:257`) and `bloom-breath` 48s (`styles.css:237`), the canon pair — plus `cover-breath` 5.5s on `.cover-story` (`styles.css:341`, the founding-cover ring on `routes/index.tsx:311`) and `crew-glow-sweep` 5.5s on `.crew-glow` (`styles.css:5010`, the join-the-crew button on `components/nav/crew-slot.tsx:133`). Both extras are gold, both are perpetual, and at 5.5s neither is "imperceptible" the way a 48s/72s breath is. `/404` adds two more of its own (`void404-orbit` 21s + `void404-tumble` 7s, `styles.css:7797`/`7801`) without suppressing the canon pair. The divergence is KNOWN and self-documented: `crew-slot.tsx:10-13` cites "the ambient budget is exactly two movements (DESIGN.md §5)" as its reason to drop the glow on every page EXCEPT home — the rule is quoted and then excepted on the one surface where all four stack. Every one of them is correctly reduced-motion-gated, so this is a budget/canon question, not an accessibility defect. | Two defensible directions, and picking between them is the operator's call, not the auditor's. (a) AMEND DESIGN.md §5 to state the real budget — name `.cover-story`'s breath and `.crew-glow`'s sweep as sanctioned home-only movements the way the Live Exception is named, and say whether a page like `/404` may spend its own; or (b) HOLD the rule and drop the two extras from home, leaving the cover ring and the crew button lit but still (their `prefers-reduced-motion: reduce` blocks at `styles.css:5017` and the un-animated `.cover-story` base already define exactly what "still but lit" looks like, so the still frame is authored, not invented). Filed rather than fixed because either branch changes canon or deletes shipped visual flourishes — (a) rewrites a named rule, (b) reverses a deliberate, commented product choice. | `audit/20260730-design` |
| 2026-07-31 | design | low | `packages/tokens/src/index.ts:30` | `ruleDark` (`#3a342a`) is a palette entry with NO home in DESIGN.md — absent from the frontmatter `colors:` block and from §2's prose — yet it ships on six surfaces: `apps/ssh/main.go:2812`, `apps/web/src/styles.css:7563`/`7571` (`/fresh`), `apps/web/src/routes/embed.$logId.ts` (the oEmbed card), `apps/extension/src/ui.css:21` + `content.css:43`/`110`, `apps/mobile/tailwind.config.js:36`, and `packages/video/src/explainer/surfaces.tsx`. The file's own header rule is "DESIGN.md is canonical. This package mirrors it verbatim" — so a token with no canon entry is definitionally drift. Its stated derivation was also false and is corrected in this PR: the comment claimed "DESIGN.md prose: Dust Line over Deep Field resolves to #3a342a", but Dust Line (`#d0b990` at `0x29` = 16.08%) over Deep Field (`#090a0b`) composites to **#292620**; `#3a342a` needs ~24% dust (per-channel implied alpha 0.246 / 0.240 / 0.233). It is a hand-picked value, a visible step brighter than the rule it claims to be. **`apps/ssh/main.go:2801` repeats the same false claim verbatim** ("ruleColor (#3a342a) is Dust Line over Deep Field") and is NOT corrected in this PR: the Go toolchain is absent from the audit box, so `gofmt -l` / `go vet` could not be run and an unverifiable edit is a hard rail. Compounding it, DESIGN.md:140 calls Dust Line "the only edge treatment in the system", which `ruleDark` contradicts by existing. | Operator ruling, one of two: (a) DOCUMENT it — add `rule-dark` to DESIGN.md's frontmatter `colors:` and a §2 Neutral bullet ("the opaque stand-in for a Dust Line rule on surfaces that cannot composite alpha: the SSH TTY, the extension's injected CSS, the mobile theme, the Satori/Remotion renders, the oEmbed card"), and soften DESIGN.md:140's "the only edge treatment" to name the opaque twin; or (b) RETUNE it to the true composite `#292620`, which also lets `apps/ssh/main.go:2801`'s comment stand as written so the derivation the comment once claimed becomes real, accepting that six surfaces' rules get darker. Filed, not fixed: (a) amends canon and (b) is a cross-surface visual change — and the two branches disagree about whether the brighter rule was intentional, which only the operator knows. Note (b) is the cheaper one to reverse and the one that keeps the palette derivable from three canon colours instead of four. | `audit/20260730-design` |
| 2026-07-30 | db-query-shape | med | `apps/web/src/lib/server/artists.ts:1859` | `listFreshLinks` carries the SAME unbounded shape `docs/db-scale-backlog.md` Wave 1 item 17 recorded for `listArtistReviewRows` — `group by artist_id` + `min(created_at)` + `order by` the aggregate over the WHOLE unreviewed `artist_socials` partition before its `limit 100`. The `LIMIT` bounds the payload, never the aggregation, and the unreviewed set grows with every resolver-minted link (nothing bulk-stamps `reviewed_at`). Item 17's `loc` names only `listArtistReviewRows`, so a fix scoped to that line would leave this one — and this is the WIDER read (100 artists vs the queue's 25), hydrated further with socials + finding counts. Both are operator surfaces: the `/admin/artists` fresh-links section and the attention queue. | Apply item 17's own deferred rewrite to BOTH call sites, not one: select the oldest-N DISTINCT `artist_id`s from the null-reviewed group via a bounded index walk, then aggregate only those ≤N artists. That is a pure query rewrite — no schema change — so it is shippable once the shape is agreed; it is filed rather than fixed because item 17 already classifies it as a design call and the two call sites want one shared helper, not two divergent walks. If an index is proposed alongside it (see the row below), that half is gated on a hosted-Turso proof at 150k via the `apps/web/scripts/bench-db-scale.ts` pattern — seed with `scripts/lib/scale-seed.ts`, measure p50 + `EXPLAIN QUERY PLAN` before/after through `@libsql/client/web`, destroy the DB — which is operator-only: Turso Cloud credentials are not on the audit box. | `audit/20260729-db-query-shape` |
| 2026-07-30 | db-query-shape | low | `apps/web/src/db/schema.ts:3147` | `artist_socials_unreviewed_idx` is a FULL index on `reviewed_at`, despite two schema comments calling it partial and one citing it as "the shrinking-partial precedent" for `artist_socials_candidate_idx`. The comments are corrected in this PR; the index shape itself is unchanged. It does bound the SEEK (NULLs sort first in an ASC index, so the unreviewed head is reachable), but it indexes every reviewed row too — paid on every resolver write to a table the crawler grows — for a slice the readers only ever want the NULL end of. | Narrow it to `index("artist_socials_unreviewed_idx").on(reviewedAt).where(sql`reviewed_at is null`)`, the shape `artist_socials_candidate_idx` beside it already uses. This is an index change, so it is a migration AND unprovable from this box: Turso keeps no planner statistics, so whether the planner still picks it up is empirical. Gated on a hosted-Turso proof at 150k via the `apps/web/scripts/bench-db-scale.ts` pattern — seed with `scripts/lib/scale-seed.ts`, `EXPLAIN QUERY PLAN` on the real `listFreshLinks` / `listArtistReviewRows` statements before/after plus p50 and the build cost, through `@libsql/client/web`, destroy the DB — operator-only. Worth pairing with the rewrite above rather than shipping alone: item 17's finding is that the index cannot bound the GROUP BY by itself. | `audit/20260729-db-query-shape` |
