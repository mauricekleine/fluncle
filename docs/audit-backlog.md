# Audit backlog — the nightly auditor's findings ledger

This is the durable record of findings the **nightly `fluncle-audit` sweep** chose to _file_
rather than fix (the large, risky, cross-cutting, or judgment-call ones — see the auditor's
operating contract in `docs/agents/hermes/scripts/audit/prompts/_preamble.md`). It is
**machine-appended and committed as part of each night's PR**, so a filed finding survives the
PR's merge instead of being buried in a squashed commit message.

Keep it distinct from the human-owned list:

- `docs/planning/ROADMAP.md` — the planning backlog (non-canon per AGENTS.md).

The operator triages from _this_ ledger into it when a finding is worth scheduling; the audit
never writes to it directly.

## How it's maintained

- The **auditor** (1am) appends a row for each finding it filed tonight, most-severe first. It
  **dedupes**: before appending, it checks for an existing open row with the same `domain` +
  `location` + gist and skips it rather than re-filing the same thing every cycle.
- The **reviewer** (5am) may resolve a small filed finding by fixing it — when it does, it flips
  that row's status to `fixed` (with the PR that fixed it).
- The **operator** resolves rows by acting on them (or promoting them to the roadmap/backlog),
  then sets status to `done` / `wontfix`. A row is never silently deleted — status carries the
  history.

## Columns

`filed` (UTC date) · `domain` · `sev` (high/med/low) · `location` (`path:line`) · `finding` ·
`proposed_fix` · `status` (open/fixed/done/wontfix) · `ref` (PR/commit that filed or resolved it)

| filed | domain | sev | location | finding | proposed_fix | status | ref |
| ----- | ------ | --- | -------- | ------- | ------------ | ------ | --- |

<!-- The auditor appends rows below this line. Newest run on top. -->

| 2026-07-19 | architecture | low | `apps/web/src/lib/server/mcp.ts:104` + `apps/web/src/lib/webmcp.ts:157` | The `search_tracks` tool (Spotify candidate search for the submit flow) is defined THREE independent times: the canonical oRPC contract op (`GET /search`, `packages/contracts/src/orpc/search.ts:26`) and two hand-written client copies — an `mcpOnlyTools` entry in `mcp.ts` and a `webmcpOnlyTools` entry in `webmcp.ts` — whose `name` + model-facing `description` + input JSON-schema are byte-identical today but are single-sourced from nothing. The PR-1..PR-6 shared-registry refactor (`server/tools/specs.ts` `SHARED_TOOL_SPECS`) exists precisely to kill this drift for every archive tool (mcp/webmcp/chat all project from one spec, with parity assertions), but `search_tracks` was deliberately left OUT because it hits Spotify, not the archive (specs.ts:198 references it as the odd one out). The consequence: nothing keeps the two client copies — or the contract's own description — in step. This is the exact latent MCP↔WebMCP drift the brief flags ("mcp.ts vs webmcp.ts must be identical"); it is dormant, not live (they match now), but no test guards it. | Judgment call on WHERE to close it: (a) extend the shared registry with a non-archive `spotify`/candidate projection and fold `search_tracks` into `SHARED_TOOL_SPECS` (the durable single-source, but it widens the projection model the registry was scoped to archive shapes); or (b) keep it carved out but add a cheap parity assertion (a test asserting the mcp/webmcp `search_tracks` name+description+schema equal each other and the contract op). Filed (not fixed) because the design question — grow the registry vs. bolt on a guard — is a human call, and it spans three files/two runtimes (server execute vs browser fetch). | open | audit/20260718-architecture |

| 2026-07-18 | voice | low | `apps/cli/src/cli.ts` (systematic; representative `:1264`, `:2263`, `:2282` command descriptions, `:3428`, `:4826`, `:5108` status lines) + `apps/web/src/routes/admin/catalogue.tsx:802` | Operator-tier CLI/admin copy uses `—` (em dash) as a prose clause separator throughout — ~35 instances across `cli.ts` alone ("Stop all catalogue audio capture — the kill switch", "Catalogue capture is RUNNING — it may buy audio", "No gaps — every past sector-day…"), plus ALL-CAPS mid-sentence emphasis (~13: "OPERATOR only", "DRY RUN", "RUNNING", "Frontier minting is OPEN"). VOICE.md's mechanical rails ban the prose em dash (only `Artist — Title` is sanctioned; the `"—"` null-cell glyph is fine) and reserve ALL CAPS for brand-mark plates. But the CLI is the "tool first / medium technical density" register, and terse status emphasis + em-dash clause joins are a conventional CLI idiom, so whether the full prose discipline reaches operator status/help output — or that register earns its own carve-out — is a canon call, not clear drift. It is one systematic house style, not a slip; the public CLI (recent/submit/about/empty states) is clean. | A human ruling on ONE question: (a) rule operator-CLI copy as full-voice and sweep both patterns (em dash → comma/colon/period/parens; caps → sentence case) across `cli.ts` + the admin twin, or (b) add a CLI-operator register note to VOICE.md §5 blessing terse status emphasis and em-dash clause joins in tool output. Filed (not auto-fixed) because it is cross-cutting (~48 strings on one deep surface), the per-line replacement is editorial, and the register question is a judgment call. | open | audit/20260717-voice |

| 2026-07-18 | voice | low | `apps/ssh/main.go:2271` (+ screen titles `:1281`, `:1284`, `:1287`, `:1307`) | The SSH rave-terminal labels the "browse the latest logged findings" action **"Latest bangers"** (menu item + four screen titles), while the web twin for the same action is **"Latest findings"** (`apps/web/src/routes/index.tsx:373`, and the llms.txt heading). VOICE.md's Chrome Rule is explicit that one user action reuses its ratified string verbatim across surfaces ("a new label for an existing action is a bug, not a variation" — ratified 2026-07-11 after exactly this: a mobile "Put me on" vs the web "Submit a track"). "banger" is not a banned word (it is core vocabulary), so this is not a hard violation — but it is a cross-surface label divergence for one action. | Reconcile the label: either align SSH to "Latest findings" (one action, one label), or consciously bless "bangers" as an intentional deep-SSH-register exception and note the carve-out. Filed rather than auto-renamed because the SSH register is deliberately the most scene-native, so which way to resolve is a taste/canon call, and the change is in Go (needs `go build`/`gofmt`/`go vet` to verify). | open | audit/20260717-voice |

| 2026-07-15 | tests | med | `apps/web/src/lib/server/artists.ts:574` (`linkTracksToArtistEntities`), `:613` (`upsertTrackArtists`), `:1042`–`:1269` (the social write path) | The artist entity — the newest of the three graph entities (artist ↔ label ↔ album) — is the only one WITHOUT a DB-backed integration harness. Its siblings both have one against the real migrated schema: `labels.ts` → `labels.test.ts`, `albums.ts` → `graph-entities.test.ts` (both `vi.mock('./db')` onto an in-memory libSQL engine seeded by `createIntegrationDb`). Tonight closed the PURE-helper gap (`artists.test.ts` covers `parseArtistsJson`/`toArtistSlug`/`artistNeedsLook`/`unreviewedSocials`/`assertHttpUrl`), but the load-bearing DB paths — the MINT/LINK edge (`upsertTrackArtists` folding on `spotify_artist_id`, `linkTracksToArtistEntities`), the public-socials status+order projection (`getPublicArtistSocials` + `compareSocialLinks`), and every social write (`addArtistSocial`/`confirmArtistSocial`/`reviewArtistSocial`/`updateArtistSocial`/`removeArtistSocial`) — have no regression net, so a fold/ordering/status-gate regression lands silently on `main`. | Add `artists.integration.test.ts` mirroring `graph-entities.test.ts`: `vi.mock('./db')` onto `createIntegrationDb`, then assert the Spotify-id fold is idempotent, the public projection filters to `auto`/`confirmed` and homepage-leads-then-A–Z, and the review/confirm state transitions. A whole harness (heavy DB setup), not a one-file pure-unit add — hence filed. | open | audit/20260715-tests |

| 2026-07-14 | docs | low | `docs/surfaces-doctrine.md:149` (§2 crons header) + `packages/registry/src/index.ts` | The §2 header claims the surfaces tables are "Generated from the `SURFACES` catalog … Keep this table in step with the catalog," but nothing enforces that — no test compares the doc's surface-name set against the registry, so the tables drift silently. This audit found `cron.verify-captures` (a live, non-`pending` registry surface since it shipped) missing from both the §2 inventory and the §3 per-context matrix, and hand-added it. The next registry surface will rot the same way, invisibly. | Add a build-fail parity test (alongside the existing `packages/registry/src/index.test.ts` invariants) that asserts every non-`pending` `SURFACES` name appears in `docs/surfaces-doctrine.md`, or generate the §2/§3 tables from the catalog outright. Whether to couple a canon doc to a test (brittle string-match) vs. a generator is a tooling/judgment call for a human — hence filed, not auto-added. | open | audit/20260714-docs |

| 2026-07-13 | surfaces-seo | med | `apps/web/src/lib/sitemap.ts:267` (the `pages` case) | The `/docs` section (Fumadocs developer docs + `/docs/api`, `/docs/cli`, …) is entirely absent from the sitemap — no static hub `<loc>` and no per-page child, and no Fumadocs sitemap generator exists (`rg /docs` over the sitemap builders returns nothing). GSC (last 28d) shows `/docs` (pos 3) and `/docs/cli` (pos 5) are already indexed, so Google found them via on-page links alone; the section's coverage is unmanaged and invisible to Search Console's per-sitemap coverage report. Not auto-fixed because the honest fix enumerates the Fumadocs page tree (a new `docs` sitemap kind fed by the fumadocs source), which is a real cross-cutting change, not a one-line hub add. | Add a `docs` sitemap child that walks the Fumadocs `source.getPages()` tree into one `<loc>` per doc page (mirroring the entity children), and add the `/docs` hub to the static `pages` list. Sizeable + touches the docs pipeline, so a reviewed change rather than a nightly auto-fix. | open | audit/20260713-surfaces-seo |
| 2026-07-13 | surfaces-seo | low | `apps/web/src/lib/sitemap.ts:267` (the `pages` case) | The `/reach` page (`web.reach`, shipped #590) is a canonical, indexable public content page (self-referencing canonical + og tags, no `noindex`) but is absent from the sitemap's static hubs. Unlike `/stories` (duplicate of the archive) and `/radio` (thin audio) — defensibly excluded — `/reach` is unique content, so its omission looks like an oversight. Filed rather than fixed because whether Fluncle's own metrics page should rank in search is a product/SEO posture call on a 2-day-old surface, not a clear correctness fix. | If the numbers page should be crawlable, add a `/reach` entry to the `pages` hubs in `sitemap.ts` (and bump the `sitemap.test.ts` hub count). Operator decides the indexing posture. | open | audit/20260713-surfaces-seo |
| 2026-07-13 | surfaces-seo | low | `apps/web/public/llms.txt` | The static `llms.txt` lags the dynamically-rendered markdown home (`apps/web/src/lib/server/agent-discovery.ts`, served on `/` for `Accept: text/markdown`): the dynamic map advertises the labels, albums, and mixtapes hubs (and links every artist page), while the static `llms.txt` lists only Spotify/Telegram/archive/log/mixtapes/about/artists — it omits the `/labels`, `/albums`, and `/logbook` graph hubs and the `/newsletter` archive, all real indexable content surfaces. Filed (not edited) because `llms.txt` is a hand-curated VOICE.md-governed document, and how much of the surface map to expose in the short LLM map vs. the full markdown home is a curation judgment. | Bring the static `llms.txt` up to the dynamic markdown home's coverage: add the `/labels`, `/albums`, `/logbook`, and `/newsletter` hubs (in Fluncle's voice), or consciously decide the short map stays curated and document why. | open | audit/20260713-surfaces-seo |

| 2026-07-12 | security | med | GitHub Dependabot (repo-wide) | `git push` reported GitHub Dependabot flags **13 open vulnerabilities on `main` (7 critical, 2 high, 4 moderate)**. The audit token lacks the `security_events` scope (`GET /dependabot/alerts` → 403), so the specific packages / whether each is a runtime vs build/dev dependency could not be enumerated from here — but 7 critical warrants a human triage pass. This is a standing state of the default branch, not introduced by tonight's diff. | Recommendation (dep-posture, do NOT bump in this audit — supply-chain change is the `fluncle-maintenance` skill's job): open https://github.com/mauricekleine/fluncle/security/dependabot, triage the 7 critical + 2 high by runtime-reachability, and schedule the safe bumps through the maintenance flow (mind the `bunfig.toml` `minimumReleaseAge` gate). | open | audit/20260712-security |
| 2026-07-12 | security | med | `apps/web/src/lib/server/account-data.ts:253` | `merge_private_galaxy_progress` (`PUT /me/galaxy-progress`, private-session, CSRF-guarded) takes `collectedLogIds` as `z.unknown().optional()` (`packages/contracts/src/orpc/me-galaxy.ts:36`); the server (`mergeGalaxyProgress`, account-data.ts:247–255) filters it to strings but never CAPS the array, then loops `for (const logId of new Set(logIds)) await collectLogId(...)` — each iteration a SEQUENTIAL SELECT + INSERT + `touchGalaxyState`. Since Spotify login is open to anyone, a signed-in caller can PUT 100k ids and pin one Worker doing ~100k×3 serialized queries in a single request — a cheap CPU/DB amplification DoS. The shared limiter caps request FREQUENCY, not per-request array size. | Add a server-side cap on the deduped id set before the loop (a real local save is bounded by the archive's collectible count); consider batching the sequential awaits. The exact ceiling needs the max-collectible number, and reject-vs-silently-truncate on overflow is a semantic choice — a human call, so NOT auto-fixed. | open | audit/20260712-security |
| 2026-07-12 | security | low | `packages/contracts/src/orpc/admin-costs.ts:104` | Two authenticated write ops take unbounded arrays with no `.max()`: `record_cost` (agent-tier, `.input(z.array(CostEventInputSchema))`, admin-costs.ts:104) and `update_galaxy_map` (admin-tier, `clusters: z.array(...)` with inner `centroid: z.array(z.number())`, `packages/contracts/src/orpc/admin-galaxies.ts:116–118`). A valid agent/box token could post an oversized array to bloat the cost ledger or the galaxy write. Low exploitability (requires an admin/agent credential), so filed rather than fixed. | Add `.max(...)` ceilings sized to the real batch sizes (the box's cost-post batch; `k` in the cluster fit). Needs domain knowledge of those batch sizes to avoid rejecting valid box traffic — a judgment call, not a blind ceiling. | open | audit/20260712-security |
| 2026-07-12 | security | low | `apps/web/wrangler.jsonc:36` | `CF_CACHE_PURGE_ZONE_ID` is committed as the literal Cloudflare zone id `93b403e3…` — a non-secret runtime IDENTIFIER in the exact class as `R2_ACCOUNT_ID` one line above (it grants nothing without the separate secret `CF_CACHE_PURGE_TOKEN`; the inline comment documents it non-secret). Unlike `R2_ACCOUNT_ID` it is NOT allow-listed in `.gitleaks.toml`. It is not currently flagged (main is green), so this is a consistency / defense-in-depth gap, not a live leak. | Add the zone id to `.gitleaks.toml` `[allowlist] regexes` with the same "non-secret Cloudflare identifier" rationale as `R2_ACCOUNT_ID`. NOT edited here: `.gitleaks.toml` is CI / secret-scanner config (a hard rail), and allow-listing a value in a secret scanner is security-sensitive — human review. | open | audit/20260712-security |

| 2026-07-11 | architecture | med | `apps/cli/src/live.ts:52` | The `live.ts` module is orphaned: its only export, `maybePrintLiveCallout` (the cross-surface "on the decks, live now" callout — a complete, TTY-gated, best-effort feature that reads `/api/status` and prints one Nebula-Violet line, referencing DESIGN.md "The Live Exception"), is imported nowhere (`rg -nw maybePrintLiveCallout` returns only its definition). It is a fully-built feature that was never wired into `cli.ts`, not accidental cruft — so deleting it is a product-direction call, not a mechanical cleanup. | Judgment call — either WIRE it (call `maybePrintLiveCallout(args)` at the top of the CLI's human-command path in `apps/cli/src/cli.ts`, honouring the existing TTY/`--json`/admin gates) so the live beat actually reaches the terminal, or delete the module if the callout is abandoned. A human should decide whether the feature ships. | open | audit/20260711-architecture |
| 2026-07-11 | architecture | low | `packages/contracts/src/index.ts` + 7 module files | Verified-unused dead exports across the workspace (each `rg -nw`'d to its definition line only, incl. tests): standalone symbols `formatDurationField` (`apps/web/src/lib/format.ts:41`), `mixtapeStudioEnvelopeUrl` (`apps/web/src/lib/media.ts:70`), `BREATHER_TOTAL_MS` (`apps/web/src/lib/radio-schedule.ts:199`), `SEARCH_FILTER_SYSTEM_PROMPT` (`apps/web/src/lib/server/search-llm.ts:62`), `MEL_RATE_HZ` (`packages/live/src/contract.ts:154`), `axisGroup` (`packages/video/src/pipeline/intent.ts:248`), `fetchRecentVideoLogIds` (`packages/video/src/pipeline/judge-diversity.ts:289`); plus ~17 unused exported types (12 response/request types on the `@fluncle/contracts` barrel, 4 in `packages/tokens/src/index.ts`, `ShowStateMachine` in `packages/live/src/bridge/state.ts:54`). Several read like deliberately-staged scaffolding (the `live.ts` feature above proves the pattern), so the delete-vs-keep call spans 4 packages and is a judgment call, not a mechanical sweep. | Batch cleanup — delete the genuinely-abandoned symbols (git preserves them) and WIRE the ones that were staged for imminent use; do the contracts/tokens type-surface trim as one reviewed pass, since those barrels intentionally export a complete typed API and a blanket delete could drop intended surface. A separate Tier-3 pass (~80 symbols exported but only used in-file — drop the `export` keyword) is lower-value and out of scope here. | open | audit/20260711-architecture |
| 2026-07-11 | architecture | low | `apps/web/src/lib/webmcp.ts:227` | MCP server↔browser tool-set drift: the server MCP (`apps/web/src/lib/server/mcp.ts:156`) exposes a `get_status` tool that the browser WebMCP surface does not, yet `webmcp.ts`'s own header contract states it "mirrors the server MCP's TOOL set; keep the two in step when the tools change" (resources/prompts are documented as server-only, but `get_status` is a tool). So the two tool sets have drifted against the file's stated invariant. | Judgment call — either add a `get_status` tool to `webmcp.ts` (proxying `/api/status` like its siblings proxy their public reads) to restore parity, or, if a service-health tool is deliberately not wanted in an agent-driving browser, amend the `webmcp.ts` header to carve `get_status` out of the mirror contract explicitly. Low impact (no coverage test enforces MCP parity today), but the header promises parity it no longer has. | open | audit/20260711-architecture |

| 2026-07-10 | voice | low | `apps/web/src/routes/logbook.$sector.tsx:255` | The `/logbook` surface uses "sector" as a structural UI label — the `<h1>` reads `Sector {sectorLabel}`, the prev/next nav and page title carry `Sector NNN`, and both the index intro and meta say "one entry per sector-day of the voyage". VOICE.md §3 bans "sector … as a UI label or a structural noun" (colour-in-first-person-prose only). But VOICE.md §4 (the Found Rule) itself names the Log ID's first coordinate slot "the sector" (days since the epoch), and `docs/agents/logbook-agent.md` makes "sector-day" the canonical structural unit of the whole logbook (the same `036` that leads `036.7.2I`). So this is a genuine canon-internal tension, not clear drift. | Judgment call — needs a human ruling on which clause governs the logbook. Either bless the coordinate reading (the `Sector NNN` heading IS the Log ID's sector slot, so §3's colour-only restriction doesn't reach it) and note the carve-out in VOICE.md §3, or rewrite the logbook's `sector`/`Sector NNN` vocabulary to a coordinate-native label (e.g. lead with the bare `036.·` coordinate) across the two route files + the agent doc. Cross-cutting; do not touch unilaterally. | open | audit/20260710-voice |

| 2026-07-09 | design | low | `apps/web/src/styles.css:237` | The `.cover-story-badge` (the play badge on the homepage story-ring cover) carries a genuine dark neutral-black drop-shadow for depth — `box-shadow: 0 4px 10px -3px rgb(0 0 0 / 0.6)` — the only dark-elevation shadow in the whole public surface. DESIGN.md §4 (One Pane / Through-the-Glass) bans box-shadows for elevation (depth is translucency over the fixed backdrop), and the neutral black tint leans against the Warm Dark Rule. Its sibling `.cover-story` already uses the gold-glow idiom instead. | Judgment call — a designer may want to keep the drop-shadow as the Instagram-story play-badge affordance. If tightening: swap to the gold-glow bloom idiom (`color-mix(… var(--eclipse-gold) …)`) the `.cover-story` ring already uses, or at minimum warm the shadow tint off pure black. | open | audit/20260709-design |
