# Shared --no-agent sweep secrets (op inject -i this -o the in-mount .fluncle-secrets.env).
# Sourced by note/observe/render-conductor. Placeholder refs; see the ops runbook note.
export CLAUDE_CODE_OAUTH_TOKEN={{ op://<automations-vault>/CLAUDE_CODE_OAUTH_TOKEN/credential }}
export DISCORD_ALERT_WEBHOOK={{ op://<automations-vault>/DISCORD_ALERT_WEBHOOK/credential }}
export BOX_API_KEY={{ op://<automations-vault>/BOX_API_KEY/credential }}
