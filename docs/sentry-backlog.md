# Sentry backlog — the nightly triage's filed-issues ledger

This is the durable record of Sentry issues the **nightly `fluncle-sentry-triage` sweep** chose to
_file_ rather than auto-fix (the large, risky, cross-cutting, judgment-call, or hard-to-reproduce
ones — see the triage operating contract in
`docs/agents/hermes/scripts/sentry-triage-prompt.md`). It is **machine-appended and committed as a
docs-only ledger PR** each night, so a filed issue survives instead of being lost in a run's logs.

It is the Sentry-side sibling of the codebase-audit ledger `docs/audit-backlog.md`. Keep the two
distinct: the audit ledger holds findings from the nightly codebase audit; this one holds real
production errors Sentry caught that need a human's fix.

## How it's maintained

- The **triage sweep** (03:30) appends a row per issue it filed tonight, most-frequent-first. The
  driver **dedupes** before triage: an issue already in a row here (or already covered by an open
  triage PR) is never re-triaged, so a row is never duplicated across nights.
- Each filed row ends with an **invisible marker** `<!-- sentry_id:<id> -->` (the numeric Sentry
  issue id). This is what the driver's fetch step reads to dedupe — do not remove it.
- The **operator** resolves rows by acting on them: fix the issue (its own PR resolves it in Sentry
  once merged, via the sweep's reconcile), or decide it is noise and resolve/ignore it in Sentry
  directly. Then set the row's `status` to `done` / `wontfix` — never silently delete (status
  carries the history).
- A **fixed** issue never lands here: the sweep opens a fix PR for it instead, and resolves the
  Sentry issue when that PR merges.

## Columns

`filed` (UTC date) · `project` · `shortId` · `title` · `reason` (why it was filed, not auto-fixed) ·
`status` (open/done/wontfix) · `ref` (the Sentry permalink or the PR that resolved it)

| filed      | project        | shortId           | title         | reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | status | ref                                                                      |
| ---------- | -------------- | ----------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------ |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-7  | Slow DB Query | `WEIGHTED_QUALIFIED_ARTISTS_SQL` (`catalogue.ts:829`) — the `labels → tracks → track_artists` group-by/`having` walk of the whole enabled-label lane, read every rank tick. Shape (B) of `docs/db-scale-backlog.md`; the fix is an index or a rewrite, and AGENTS.md requires both be proven on hosted Turso (five such indexes were already REJECTED at that gate), which triage cannot do.                                                                              | open   | https://fluncle.sentry.io/issues/135764803/ <!-- sentry_id:135764803 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-11 | Slow DB Query | `FINDING_QUALIFIED_ARTISTS_SQL` (`catalogue.ts:820`) — `distinct` over the `track_artists ⋈ findings` graph join, run twice per tick (bare in `readArchiveAffinity`, again inside the fingerprint union at `catalogue.ts:1193`). Same hosted-proof gate as FLUNCLE-WORKER-7, and the two should be judged together.                                                                                                                                                       | open   | https://fluncle.sentry.io/issues/136551873/ <!-- sentry_id:136551873 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-6  | Slow DB Query | Already gone from `main`: this is the pre-Keystone-1 catalogue anti-join in `readCatalogueIdentity` (`left join findings cf … where cf.track_id is null`), which `fadab767` (#859) replaced with `where ct.is_catalogue = 1` on 2026-07-26 06:33 UTC. The issue's `lastSeen` is 06:11 UTC that same day, and no live query still carries the shape. No code change to make — resolve or ignore it in Sentry.                                                              | open   | https://fluncle.sentry.io/issues/135686887/ <!-- sentry_id:135686887 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-17 | Slow DB Query | The rank tick's stale-candidate pick (`catalogue.ts:1216`) — `limit`-bounded, but its staleness disjunction (`catalogue_rank_corpus is null or <> ? or (embedding_blob is not null and capture_priority >= 0)`) has no covering index, so whether the `limit` actually bounds the scan needs an `EXPLAIN QUERY PLAN` on hosted Turso. A judgment call on the sweep's core read, not a mechanical fix.                                                                     | open   | https://fluncle.sentry.io/issues/136656326/ <!-- sentry_id:136656326 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-18 | Slow DB Query | `readCatalogueIdentity` (`catalogue.ts:1044`) — an UNBOUNDED read (no `limit`) of every captured catalogue row's identity columns, once per tick, with the dedupe map built in the isolate. Bounding it means changing how canonical selection is decided, which is product behaviour, not a guard.                                                                                                                                                                       | open   | https://fluncle.sentry.io/issues/136675393/ <!-- sentry_id:136675393 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-X  | Slow DB Query | `isGalaxyMapFullyNamed` (`galaxies-map.ts:235`) — a trivial `count(*)` over the small `galaxies` table, but on the PUBLIC `/log/*` render, so the span is round-trip latency rather than query cost. The fix is caching the launch gate or folding it into a neighbouring read (the pattern `loadNamedGalaxyRows` already uses at `galaxies-map.ts:390`) — a public-hot-path architecture call, and parallelising alone would not shorten the span the detector measures. | open   | https://fluncle.sentry.io/issues/135988649/ <!-- sentry_id:135988649 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-19 | Slow DB Query | The Ear's lens read over `CATALOGUE_SELECT` (`catalogue.ts:2400`) — a 21-column per-row projection. The shipped precedent is db-scale item 7 (swap the fat select for a lean one), but which columns `/admin/catalogue` actually renders is a call on the admin surface, not a mechanical narrowing.                                                                                                                                                                      | open   | https://fluncle.sentry.io/issues/136775025/ <!-- sentry_id:136775025 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1C | Slow DB Query | The fingerprint's sorted qualified-artist read (`catalogue.ts:1191`) — the `union` of FLUNCLE-WORKER-7's and FLUNCLE-WORKER-11's shapes plus an `order by`. It cannot be fixed independently of those two; whatever is done to them decides this.                                                                                                                                                                                                                         | open   | https://fluncle.sentry.io/issues/136919188/ <!-- sentry_id:136919188 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-Z  | Slow DB Query | The `/album/<slug>` findings read (`findingsByEntity` on `tracks.album_id`, `tracks.ts:889`) — a wide per-row projection over the growing `tracks` table on a public page. Same lean-the-projection family as FLUNCLE-WORKER-19, but on a public surface where the DTO is typed against `GraphFindingItem`, so narrowing it further is a cross-cutting change.                                                                                                            | open   | https://fluncle.sentry.io/issues/136228156/ <!-- sentry_id:136228156 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1D | Slow DB Query | `readFindingIdentity` (`catalogue.ts:992`) — the `findings ⋈ tracks` identity read that pulls title + `artists_json` for every certified finding into the isolate each tick. Bounded by the finding count by design; making it cheaper means caching or materialising the identity map, a design call.                                                                                                                                                                    | open   | https://fluncle.sentry.io/issues/136947023/ <!-- sentry_id:136947023 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1G | Slow DB Query | The distinct-label affinity read in `readArchiveAffinity` (`catalogue.ts:858`) — `trim(tracks.label) <> ''` is non-sargable, so no index on `label` can serve it. Making it sargable changes which labels qualify at the edges (whitespace-only strings), so it is a behaviour call, not a guard.                                                                                                                                                                         | open   | https://fluncle.sentry.io/issues/136947891/ <!-- sentry_id:136947891 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1F | Slow DB Query | The `artists_json` affinity read in `readArchiveAffinity` (`catalogue.ts:853`) — pulls a JSON column for every certified finding into the isolate to build a name set. The durable fix is reading the resolved `track_artists` graph instead of the JSON blob, which changes what "a finding's artist" means to the affinity set — canon-adjacent, and outside triage's bar.                                                                                              | open   | https://fluncle.sentry.io/issues/136947888/ <!-- sentry_id:136947888 --> |

<!-- The triage sweep appends rows below this line. Newest run on top. Each row ends with `<!-- sentry_id:<id> -->`. -->
