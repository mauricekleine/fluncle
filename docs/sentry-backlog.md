# Sentry backlog — the nightly triage's filed-issues ledger

This is the durable record of Sentry issues the **nightly `fluncle-sentry-triage` sweep** chose to
_file_ rather than auto-fix (the large, risky, cross-cutting, judgment-call, or hard-to-reproduce
ones — see the triage operating contract in
`docs/agents/hermes/scripts/sentry-triage-prompt.md`). It is **machine-appended and committed as a
docs-only ledger PR** each night, so a filed issue survives instead of being lost in a run's logs.

It is the Sentry-side sibling of the codebase-audit ledger `docs/audit-backlog.md`. Keep the two
distinct: the audit ledger holds findings from the nightly codebase audit; this one holds real
production errors Sentry caught that need a human's fix.

## How it's maintained

- The **triage sweep** (03:30) appends a row per issue it filed tonight, most-frequent-first. The
  driver **dedupes** before triage: an issue already in a row here (or already covered by an open
  triage PR) is never re-triaged, so a row is never duplicated across nights.
- Each filed row ends with an **invisible marker** `<!-- sentry_id:<id> -->` (the numeric Sentry
  issue id). This is what the driver's fetch step reads to dedupe — do not remove it.
- The **operator** resolves rows by acting on them: fix the issue (its own PR resolves it in Sentry
  once merged, via the sweep's reconcile), or decide it is noise and resolve/ignore it in Sentry
  directly. Then set the row's `status` to `done` / `wontfix` — never silently delete (status
  carries the history).
- A **fixed** issue never lands here: the sweep opens a fix PR for it instead, and resolves the
  Sentry issue when that PR merges.

## Columns

`filed` (UTC date) · `project` · `shortId` · `title` · `reason` (why it was filed, not auto-fixed) ·
`status` (open/done/wontfix) · `ref` (the Sentry permalink or the PR that resolved it)

| filed      | project        | shortId           | title                 | reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | status | ref                                                                      |
| ---------- | -------------- | ----------------- | --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------ |
| 2026-08-17 | fluncle-worker | FLUNCLE-WORKER-2N | LibsqlError           | `getDb` (`db.ts:265`) dialled a `file:` URL, which `@libsql/client/web` refuses by design — `docs/local-database.md:11` names that exact limitation as the reason everyday dev runs a local libSQL server over HTTP rather than a bare `file:./local.db`. So the deployed Worker cannot be the origin (prod dials `libsql://`), and the frames agree: they resolve inside a locally built `apps/web/dist/server` bundle in a developer worktree, not a Workers Build artifact. It is the second half of ONE local session with FLUNCLE-WORKER-2M — same bundle hash (`db-CWS-aSpX.js`), 135s apart, first with no `TURSO_DATABASE_URL` at all and then with one pointed at a file. What needs a human is not the dial but the FEED: `server.ts:168` gates the Worker DSN on `import.meta.env.PROD`, which is true for ANY `vite build` output including a local one, so a locally built bundle reports into the production `fluncle-worker` project and arrives in this sweep's worklist wearing the shape of a prod incident. Narrowing that gate (a build-time define separating a Cloudflare Workers Build from a local build) changes the reporting posture documented in `docs/error-tracking.md`, and its failure mode is SILENT — a wrong narrowing stops production errors reporting at all, and no local check can prove the deployed path still reports. Judge it once for both rows. Neither issue's fields carried injected instructions.                                                                                                                                                                                               | open   | https://fluncle.sentry.io/issues/140920117/ <!-- sentry_id:140920117 --> |
| 2026-08-17 | fluncle-worker | FLUNCLE-WORKER-2M | Error                 | `readEnvs` threw `Missing TURSO_DATABASE_URL` (`env.ts:325`, inside the `keys.map` at `env.ts:321`) for `getDb` (`db.ts:265`) under the launch gate `isGalaxyMapFullyNamed` (`galaxies-map.ts:236`) — the reported frame ladder `readEnvs` → `Array.map` → anonymous is that block exactly. The cause is a production-mode BUILD run outside a provisioned Worker: `loadLocalEnv` early-returns on `!import.meta.env.DEV` (`env.ts:270`), so a built bundle never loads `.dev.vars`, and running one locally without the var exported yields precisely this. The deployed Worker takes the var from its binding and would fail this way only with the secret unprovisioned, which would floor every surface rather than emit one event; the frames instead resolve inside a locally built `apps/web/dist/server` bundle in a developer worktree. Same bundle hash and the same two-minute window as FLUNCLE-WORKER-2N, this being the first attempt of that session. Filed for the same reason as that row: the code did what it should, and the open question is the `import.meta.env.PROD` DSN gate at `server.ts:168` letting a local build write into the production project.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | open   | https://fluncle.sentry.io/issues/140920012/ <!-- sentry_id:140920012 --> |
| 2026-08-16 | fluncle-worker | FLUNCLE-WORKER-2K | LibsqlError           | `recordDemand` (`demand.ts:344`) — the demand tick's single atomic rewrite batch took a 524 and aborted the pass out through `apiFault` (`admin-catalogue.ts:166`). The one event (2026-08-15T02:42:48Z) lands on the 04:40 Amsterdam `fluncle-demand` cron's bare trigger. Not a retry: `db.batch` never retries by design (a batch is one unit and may contain writes) and 524 is excluded from `RETRYABLE_GATEWAY_STATUSES` on purpose (`db.ts:73-77`), because an origin timeout may already have applied the write. The exposure is the batch's UNBOUNDED WIDTH rather than any one statement: `writes` is two clears plus one statement per demanded artist, two per demanded label, and one more per artist MBID, all fed from a Simple Analytics page list capped at `DEMAND_PAGE_LIMIT = 1000` (`demand.ts:40`) over a 30-day window — so a busy month hands the origin up to a few thousand statements in ONE transaction, each artist bump an `update tracks … where track_id in (select track_id from track_artists where artist_id = ?)` over the growing `tracks`. The clear is not the growth: `tracks_demand_score_idx` is partial on `demand_score is not null` (`db/schema.ts:1311-1313`) and bounds it. Chunking is not a mechanical edit — the clear-then-bump pass is atomic ON PURPOSE (`demand.ts:301-311`) so a same-window re-run is idempotent and a de-trending entity falls back to baseline, and splitting it opens a window where every score reads null while the crawl frontier is picking against it. That is an outcome-safe write-path design call plus a hosted plan, same family as FLUNCLE-WORKER-16/-22/-26. | open   | https://fluncle.sentry.io/issues/140803808/ <!-- sentry_id:140803808 --> |
| 2026-08-16 | fluncle-web    | FLUNCLE-WEB-H     | TypeError             | `res[exportName ?? "default"]` with `res` undefined — TanStack Router's `lazyRouteComponent` (`node_modules/@tanstack/react-router/dist/esm/lazyRouteComponent.js:22`) reading the resolved module namespace of a lazily-imported route `component` — the auto-split half of every route file (`docs/client-bundle.md:15`). That expression exists in exactly ONE client-bundlable place in the dependency tree (the only other match, `@oclif/core`, is CLI-side and never reaches a browser bundle) and in no `apps/web` or `packages/*` source, so the library frame is the throw. There is no in-app line to fix: the single event's only frame is an unresolved anonymous frame in the minified entry chunk (`/assets/index-C5yAt-Cr.js:29`, no function name), and the WebKit-specific "undefined is not an object" phrasing puts it on Safari, which is also not reproducible from here. Worth a human's eye because the library's friendly path does NOT cover this shape: `lazyRouteComponent` reload-guards a REJECTED import (`isModuleNotFoundError` → one `sessionStorage`-fenced `window.location.reload()`), whereas here the import RESOLVED with `undefined` and walks straight into the property read — the classic stale-chunk-after-deploy signature, which every deploy to `main` can produce for a visitor holding an open tab. The answers (our own stale-chunk reload policy, or an upstream fix) are judgment calls above a triage guard, and one event on one day is not enough evidence to pick one; watch whether it recurs across releases.                                                                            | open   | https://fluncle.sentry.io/issues/140814928/ <!-- sentry_id:140814928 --> |
| 2026-08-14 | fluncle-worker | FLUNCLE-WORKER-2F | LibsqlError           | The rank tick's catalogue-identity read (`readCatalogueIdentity`, `catalogue.ts:1046`) took a fatal 524 five times, from inside the five-way `Promise.all` at `catalogue.ts:1350`. It is the ERROR-side twin of FLUNCLE-WORKER-18, which filed the same statement as a slow query: an UNBOUNDED read (no `limit`) of every captured catalogue row's identity columns, once per tick, with the dedupe map built in the isolate. 524 is excluded from `RETRYABLE_GATEWAY_STATUSES` on purpose (`db.ts:72-77` — an origin timeout may already have executed, and a retry doubles load on an origin that is already timing out), so the `runWithRetry` frame in the stack is the wrapper correctly declining to retry, not a failed retry. Bounding the read means changing how the canonical sibling is chosen — product behaviour, not a guard — and the four sibling reads share the tick's wall clock, so this is one decision for the whole `Promise.all` rather than five. Events continue past 911f467e (2026-08-13 15:26), which indexed the two `/admin/catalogue` LENS reads and does not touch this statement. Judge with FLUNCLE-WORKER-18/-22/-1A/-1C, the same tick's other filed reads.                                                                                                                                                                                                                                                                                                                                                                                                                                                  | open   | https://fluncle.sentry.io/issues/140484177/ <!-- sentry_id:140484177 --> |
| 2026-08-14 | fluncle-worker | FLUNCLE-WORKER-2H | LibsqlError           | `listTrackWork` (`track-work.ts:859`) — the `tracks ⋈ findings` worklist page behind every `list_track_work` queue — escaped the gateway retry with a 502. The wrapper is not at fault, verified end to end this run: the statement classifies as a retryable read (`db.ts:131-139`), 502 is in `RETRYABLE_GATEWAY_STATUSES` (`db.ts:77`), and the numeric `status` the classifier walks the cause chain for really is reachable (`@libsql/client`'s `mapHranaError` passes the hrana `HttpServerError` to the `LibsqlError` as its `cause`). So all three attempts ran and all three took a 502. Both events share the second with FLUNCLE-WORKER-2J (14:14:53Z) — one Turso gateway window seen from two sweeps, not two bugs. The only lever in the code is `DB_RETRY_BACKOFF_MS` (`db.ts:68`), and lengthening it lengthens EVERY read in the app against the Worker's wall-clock budget, which the comment there names as the reason it is short. An operator ruling on the retry budget, not a triage guard.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | open   | https://fluncle.sentry.io/issues/140516334/ <!-- sentry_id:140516334 --> |
| 2026-08-14 | fluncle-worker | FLUNCLE-WORKER-2J | LibsqlError           | The artist-credit backfill's mint path took the same 14:14:53Z gateway 502, on `mintArtistSlug`'s slug probe (`artists.ts:778`) under `mintArtistByMbid` (`artists.ts:1317`) ← `resolveRecordingCredits` (`backfill-artist-credits.ts:219`) ← `resolveArtistCredits` (`backfill-artist-credits.ts:450`). Retry budget spent, exactly as in FLUNCLE-WORKER-2H — judge the budget once for both. What is worth judging separately here is this path's EXPOSURE to a gateway window: `mintArtistSlug` is check-then-insert and probes one `select 1 from artists where slug = ?` per candidate, up to 65 sequential round trips for a single mint (`artists.ts:778-799`), and it sits inside a per-credit loop inside a per-row drain loop, so one blip anywhere in that fan aborts the whole pass instead of the one row. Collapsing the probe ladder into a single read and making the drain per-row-resumable both touch an identity WRITE path with a documented slug race and an adopt-vs-mint ladder that deliberately fails closed (`artists.ts:1305-1315`) — a design call. Same drain-loop family as FLUNCLE-WORKER-26.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | open   | https://fluncle.sentry.io/issues/140516336/ <!-- sentry_id:140516336 --> |
| 2026-08-14 | fluncle-worker | FLUNCLE-WORKER-2G | LibsqlError           | The artist-EDGE backfill's chunked write (`insertEdges`, `backfill-artist-edges.ts:373`) hit a 524 on its `db.batch([…], "write")` — the `insert or ignore into track_artists` plus the keystone-2 hub-count deltas plus the rank-restale statements, deliberately one atomic chunk. Nothing retried and nothing should have: `batch` sits outside the retry proxy by design (`db.ts:236-252` — a batch is one unit and may contain writes) and 524 is excluded regardless. The chunk itself holds up: it is atomic, so no counter can half-apply, and the worklist's `ta.track_id is null` anti-join (`backfill-artist-edges.ts:286-300`) keeps an already-edged row from being re-credited on a later pass. The exposure is the PASS: the throw escapes `resolveArtistEdges` (`backfill-artist-edges.ts:505`) before `stampVisited` runs, so every candidate queued behind the failing chunk loses the pass and the `zeroMatched` rows it did adjudicate go unstamped. An outcome-safe, per-chunk-resumable drain is a design call on a write path. This is the `backfill-artist-edges.ts` sibling of FLUNCLE-WORKER-15's `backfill-artist-credits.ts` `insertEdges`; same family as -16 and -26.                                                                                                                                                                                                                                                                                                                                                                                                                                                 | open   | https://fluncle.sentry.io/issues/140499429/ <!-- sentry_id:140499429 --> |
| 2026-08-14 | fluncle-worker | FLUNCLE-WORKER-2E | LibsqlError           | The rank tick's fingerprint read of the sorted qualified-artist set (`catalogue.ts:1191`) escaped the gateway retry with a 502. It is the ERROR-side twin of FLUNCLE-WORKER-1C, which filed the same statement as a slow query — the `union` of FLUNCLE-WORKER-7's and -11's shapes plus an `order by`, so it cannot be judged independently of those two. The failure mode is FLUNCLE-WORKER-2H's (three 502s in a row, budget spent) rather than FLUNCLE-WORKER-2F's (a non-retryable 524), so the two questions here are separable: judge the retry budget with -2H and the query shape with -1C/-7/-11. Both leave triage nothing mechanical to change — the shape needs a hosted `EXPLAIN QUERY PLAN` AGENTS.md requires and this sweep cannot run. Judge with the rest of the tick (-17/-21/-22).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | open   | https://fluncle.sentry.io/issues/140469690/ <!-- sentry_id:140469690 --> |
| 2026-08-13 | fluncle-worker | FLUNCLE-WORKER-2C | LibsqlError           | `insertRunEvent` (`run-events.ts:723`) — a 502 escaped the run-ledger append even though the named write retry `retryRunEventInsert` was live (it shipped in 70f99d5b on 2026-07-31, as the fix for FLUNCLE-WORKER-1Z), so this is that filing's recurrence AFTER its remedy rather than a repeat of it. All 3 events land in 13:14:29–33Z on 2026-08-12, the same ~4s window as FLUNCLE-WORKER-1W/-2B/-2D, so the four rows are ONE Turso gateway incident that outlasted the retry budget, not four defects. The retry path is correct as written: 502 is in `RETRYABLE_GATEWAY_STATUSES` (`db.ts:77`), the cause-chain walk is bounded, and the write is replay-safe by its deterministic `${unit}:${started_at}` id under `on conflict(id) do nothing`. What ran out is cover — `DB_RETRY_BACKOFF_MS = [50, 150]` plus ≤50ms jitter is ~250ms across 3 attempts, against an outage lasting seconds. Both remedies are judgment calls this sweep must not take alone: widening the backoff is explicitly argued against at `db.ts:66` (an `/artist/*` render fires several of these concurrently and the Worker has a wall-clock budget), and durably queueing the receipt so a lost one is re-sent is new architecture. Judge all four rows together.                                                                                                                                                                                                                                                                                                                                                                                           | open   | https://fluncle.sentry.io/issues/140292523/ <!-- sentry_id:140292523 --> |
| 2026-08-13 | fluncle-worker | FLUNCLE-WORKER-1W | LibsqlError           | `isGalaxyMapFullyNamed` (`galaxies-map.ts:243`) — the single runtime gate behind every public galaxy surface, reached through a server function, so the throw surfaces via `serverFnFaultRedaction` (`start.ts:36`). Its statement is a plain `select count(*)`, so `isRetryableRead` classifies it correctly and `runWithRetry` did engage; the 502 still escaped all 3 attempts. Its newest event (13:14:30Z on 2026-08-12) is inside the same ~4s gateway window as FLUNCLE-WORKER-2B/-2C/-2D, so it is that same incident — but its `firstSeen` is 2026-07-30, one day after the read retry shipped (8aef8066), so this fingerprint has already taken gateway blips across two separate incidents. That makes it the best of the four to judge the general remedy on. The ~250ms retry budget is the whole of the cover today; widening it, or letting the gate hold a last-known-good answer instead of failing the render, are both latency/product calls rather than mechanical fixes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | open   | https://fluncle.sentry.io/issues/137511457/ <!-- sentry_id:137511457 --> |
| 2026-08-13 | fluncle-worker | FLUNCLE-WORKER-2D | LibsqlError           | `getAlbumBySlug` (`albums.ts:248`) via `resolveAlbumPageData` (`-album-page-data.ts:75`) — the public `/album/<slug>` entry read, one event at 13:14:30Z on 2026-08-12, inside the same ~4s gateway window as FLUNCLE-WORKER-1W/-2B/-2C. A `select`, correctly classified and retried; the 502 outlived the ~250ms budget and took the page to the error boundary. There is no defect at this call site to fix — it is the availability face of one gateway incident, and it rides on whatever cover FLUNCLE-WORKER-1W's decision settles on. Note this same read is ALSO filed as FLUNCLE-WORKER-24 for its query SHAPE; the two are separate concerns and neither fix implies the other.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | open   | https://fluncle.sentry.io/issues/140292528/ <!-- sentry_id:140292528 --> |
| 2026-08-13 | fluncle-worker | FLUNCLE-WORKER-2B | LibsqlError           | `readArchiveAffinity` (`catalogue.ts:853`) under `rankCatalogue` (`catalogue.ts:1351`) on the admin `rank_catalogue` tick — one event at 13:14:29Z on 2026-08-12, the same ~4s gateway window as FLUNCLE-WORKER-1W/-2C/-2D. Its reads are `select`s, correctly classified, retried, and outlasted. It sits in a five-way `Promise.all`, so a blip on any one arm fails the whole tick; the sweep is idempotent and the next tick re-ranks from scratch, which makes this the least urgent of the four — no operator-visible surface broke. Lowest priority unless the gateway-cover decision taken on FLUNCLE-WORKER-1W covers it for free.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | open   | https://fluncle.sentry.io/issues/140292522/ <!-- sentry_id:140292522 --> |
| 2026-08-11 | fluncle-worker | FLUNCLE-WORKER-2A | Slow DB Query         | `listCatalogueAppleWork` (`backfill.ts:1786`) — the catalogue Apple-URL worklist read behind the `backfill_apple_catalogue` op. Its shape is ALREADY RULED: `docs/db-scale-backlog.md` item 13 shipped the `coalesce()`-free `order by capture_priority desc, track_id` so it rides the plain `tracks_capture_priority_idx` (`db/schema.ts:1076`), and explicitly REJECTED the proposed partial `tracks_catalogue_apple_queue_idx` — the planner never picked it, and the partial `tracks_capture_priority_track_id_idx` cannot serve a query that deliberately carries no `capture_priority is not null` predicate (adding one would couple metadata backfill to the ranking rail and strand never-ranked rows here and in the Deezer catalogue arm). What the priority index does NOT cover is the four ledger `is null` predicates plus the cooldown, so the walk filters more rows per pass as the worklist drains toward done. Every remaining option — an `INDEXED BY` hint (the same question already deferred on item 12), a new composite, or accepting the shape — is a schema/design call needing an `EXPLAIN QUERY PLAN` on hosted Turso, which AGENTS.md requires and this sweep cannot run. Sibling of FLUNCLE-WORKER-26, the write-side 524 in the same drain.                                                                                                                                                                                                                                                                                                                                                                       | open   | https://fluncle.sentry.io/issues/139875635/ <!-- sentry_id:139875635 --> |
| 2026-08-10 | fluncle-worker | FLUNCLE-WORKER-29 | Slow DB Query         | `loadPersistedHubPageAnchors` (`hub-page-anchors.ts:272`) — the persisted-anchor record read on the DEEP unfiltered `/tracks` page (`tracks-hub.ts:705`, the branch a page only reaches past `HUB_SHALLOW_MAX_OFFSET = 450`). The statement itself cannot be the cost: `hub_page_anchors` is keyed by a composite primary key on `(hub, clause_hash)` (`db/schema.ts:4330-4339`), so SQLite's automatic index makes this a `limit 1` point seek, and the table holds one row per hub + clause set. What the detector measures is Worker→Turso round-trip latency on a public render — the FLUNCLE-WORKER-X shape, not a query-shape one. Both levers are caching calls: memoize the record in the same 60s in-isolate TTL memo the function already uses for the total and the filtered anchors (`tracks-hub.ts:318`, `:612`, `:692`), or fold it into the neighbouring first-id read it already runs in parallel. Memoizing is not free — freshness is decided by comparing the stored fingerprint against a live one (`persistedAnchorDecision`), so a TTL in front of it delays the self-heal after a detached rebuild and keeps re-scheduling refreshes for up to a minute; that is a decision about the pager's documented drift window, not a guard. Judge it with FLUNCLE-WORKER-X and the `/tracks` pager rows FLUNCLE-WORKER-1H/-1J.                                                                                                                                                                                                                                                                                                       | open   | https://fluncle.sentry.io/issues/139668771/ <!-- sentry_id:139668771 --> |
| 2026-08-10 | fluncle-worker | FLUNCLE-WORKER-14 | LibsqlError           | The rank tick's WRITE batch (`rankCatalogue`, `catalogue.ts:1640`) took a 524 and aborted the tick out through `rankCatalogueHandler` → `apiFault` (`admin-catalogue.ts:145-157`). Not a retry: `db.batch` is never retried (a batch is one unit and may contain writes, `db.ts:125-130`) and 524 is excluded from `RETRYABLE_GATEWAY_STATUSES` on purpose (`db.ts:72-77`), because an origin timeout may already have applied the statements. The batch is up to `RANK_BATCH_SIZE = 250` PK-keyed idempotent updates (`catalogue.ts:696`, and the comment at `:1638` states a retry converges), so the tempting fix is exactly the write retry `db.ts` forbids generically — that asymmetry is deliberate and re-deciding it is not a guard. What the throw actually costs is the rest of the tick: the summary batch delta (`:1649`) and the affinity-cache refresh (`:1655`) never run, so the cached counts drift until an idle tick's full recompute heals them. The durable answer is a smaller or chunked write unit, or a drain that survives one failed batch — a design call on a write path. The error-side sibling of FLUNCLE-WORKER-22 (same function, read half) and the same family as FLUNCLE-WORKER-26/-16; judge them together.                                                                                                                                                                                                                                                                                                                                                                                                   | open   | https://fluncle.sentry.io/issues/136628803/ <!-- sentry_id:136628803 --> |
| 2026-08-10 | fluncle-web    | FLUNCLE-WEB-G     | Error                 | `Could not establish connection. Receiving end does not exist.` on `https://www.fluncle.com/` — the standard Chrome `runtime`/`tabs.sendMessage` no-receiver rejection, and there is no throw for it in this checkout: `apps/web/src` contains no `chrome.*` call at all and the event carries no in-app frames. Fluncle's own Lens extension is the only Fluncle-owned sender and both of its sends are already guarded (`apps/extension/src/content.ts:74` `.catch`, `popup.ts:141-152` `try`/`catch`); it ships on its own release train and reports to no Sentry project. So this is a visitor's third-party extension leaking into the page's global handlers, which leaves nothing to fix in code and one posture question for the operator: add an `ignoreErrors` denylist entry to the browser `Sentry.init` (`client.tsx:34-41`) and record it in `docs/error-tracking.md`, or archive it in Sentry. The repo has form on the cost of silencing downstream — `client.tsx:8-27` records FLUNCLE-WEB-7 being archived forever and the future eval-blindness that bought — so which side of that line third-party extension noise falls on is his call, not this sweep's.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | open   | https://fluncle.sentry.io/issues/139683905/ <!-- sentry_id:139683905 --> |
| 2026-08-09 | fluncle-worker | FLUNCLE-WORKER-28 | Slow DB Query         | The artist-credit sweep's worklist page (`listWork`, `backfill-artist-credits.ts:282`) — the same `tracks → track_artists` anti-join already filed as FLUNCLE-WORKER-Y, grouped under its own fingerprint. The function holds two statements that differ only by the `t.track_id > ?` cursor clause and the captured value truncates before they diverge, so this is the same shape either way. Both are `limit`-bounded and ride `tracks_artist_credits_backfill_queue_idx` on paper, so whether the `limit` actually terminates the anti-join scan needs an `EXPLAIN QUERY PLAN` on hosted Turso — AGENTS.md requires that proof and this sweep cannot run it. Judge it together with FLUNCLE-WORKER-Y: one query-shape decision settles both.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | open   | https://fluncle.sentry.io/issues/139612028/ <!-- sentry_id:139612028 --> |
| 2026-08-09 | fluncle-worker | FLUNCLE-WORKER-27 | Failed HTTP Operation | A Discogs `429` on the label-image resolve (`fetchDiscogsLabelImage` → `discogsFetch`, `discogs.ts:527`), and NOT the ordinary throttle it looks like. Measured A/B: the same token called directly answers `200` with `X-Discogs-Ratelimit-Remaining: 60/60` while the Worker sees `rateLimited` on the same label — so the budget being spent is not Fluncle's, it is the shared Cloudflare egress IP's, the same shape already hit on Deezer. The client handling is correct and stays (flag on the first 429, never retry in-request, circuit-break the pass without cooling the label), but it can only ever yield: no per-caller pacing reaches a limit keyed to an address shared with every other Worker on the edge. The fix is to move the vendor fetch off the shared egress onto the box, which is PR #1146. Secondary, unrelated: `discogsFetch`'s `for (attempt < 3)` loop is vestigial — every branch returns inside the first iteration, so it never retries.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | queued | https://fluncle.sentry.io/issues/139593505/ <!-- sentry_id:139593505 --> |
| 2026-08-07 | fluncle-worker | FLUNCLE-WORKER-26 | LibsqlError           | `setAppleMusicUrl` (`backfill.ts:1113`) — the per-candidate URL write in `backfillAppleMusicCatalogue`'s drain loop (`backfill.ts:1794`) took a 524 and aborted the whole pass out through `apiFault` (`admin-backfills.ts:281`). Not a retry: `db.batch` never retries by design (a batch is one unit and may contain writes) and 524 is excluded from `RETRYABLE_GATEWAY_STATUSES` on purpose (`db.ts:61-77`), because an origin timeout may already have applied the write and a retry doubles load on an origin that is timing out. The exposure is the loop's shape rather than the statement: the URL write and its `recordAttempt(…, "done")` stamp are two separate round-trips, so a throw between them can leave a row written but unstamped, and every candidate queued behind it loses the pass. The durable answer is an outcome-safe, resumable drain — isolate the failure per candidate, or fold the URL and its attempt stamp into one batch — which is a design call on a write path, not a guard. Same family as FLUNCLE-WORKER-16 and FLUNCLE-WORKER-22.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | open   | https://fluncle.sentry.io/issues/139291854/ <!-- sentry_id:139291854 --> |
| 2026-08-07 | fluncle-worker | FLUNCLE-WORKER-25 | Slow DB Query         | `listAnchorReviewRows` (`anchor.ts:1714`) — the `anchor-review` source of the `/admin` attention queue, one of sixteen reads `readAttentionSnapshot` fans out on every `/admin` load and every `fluncle admin queue` tick. The partial `tracks_anchor_review_idx` (`db/schema.ts:1203`) indexes only `track_id` under `anchor_review_json is not null`, so it bounds the SCAN but not the per-row cost: the other two predicates (`spotify_uri is null`, `dismissed_at is null`) and all seven projected columns still require a table lookup into `tracks`, and `anchor_review_json` entered the schema on 2026-07-26 (#898, `drizzle/0131` — an `ALTER TABLE … ADD`, so it appends) while `embedding_blob` entered on 2026-07-11 (#456), which puts the review payload physically AFTER the ~4 KB `F32_BLOB(1024)` in the record. That is exactly the overflow-chain walk `db/schema.ts:498-509` measured at 3.32s for three post-blob columns versus 0.23s pre-blob, and the `limit 25` caps the output rather than the walk when most reviewed rows are already anchored or dismissed. Both remedies — widening the partial index to cover the read, or moving the review payload off `tracks` — are schema changes AGENTS.md requires proving with a hosted `EXPLAIN QUERY PLAN`, which this sweep cannot run. Same family as FLUNCLE-WORKER-23; the blob-position half is the trap already recorded on FLUNCLE-WORKER-21.                                                                                                                                                                                                                     | open   | https://fluncle.sentry.io/issues/139187348/ <!-- sentry_id:139187348 --> |
| 2026-08-06 | fluncle-worker | FLUNCLE-WORKER-24 | Slow DB Query         | `getAlbumBySlug` (`albums.ts:232`) — the public `/album/<slug>` entry read. Its outer half is already optimal (a `limit 1` point seek on `albums.slug`, declared `unique` at `db/schema.ts:4038`), so what the detector measures is the correlated `min(t.release_date)` probe plus Worker→Turso round-trip latency. `tracks_album_id_idx` (`db/schema.ts:924`) indexes `album_id` ALONE and the funnel covering index leads with `is_catalogue`, so the probe seeks the album's track rows and then fetches a `tracks` table row per track just to read `release_date` — and `ensureAlbum` folds every pressing of a release group onto one `albums` row, so a sampler or compilation makes that per-row cost real. The two levers are a covering `(album_id, release_date)` index or maintaining the earliest date on the `albums` row itself; both are schema changes AGENTS.md requires be proven against hosted Turso with `EXPLAIN QUERY PLAN` (five such indexes were already REJECTED at that gate), and the second also changes where `datePublished` is authored. It is additionally SERIAL — `-album-page-data.ts:75` awaits it before the page's six parallel reads — which puts it in the same public-hot-path family as FLUNCLE-WORKER-20 (`getArtistBySlug`) and FLUNCLE-WORKER-X; judge the three together.                                                                                                                                                                                                                                                                                                                         | open   | https://fluncle.sentry.io/issues/138929073/ <!-- sentry_id:138929073 --> |
| 2026-08-05 | fluncle-worker | FLUNCLE-WORKER-23 | Slow DB Query         | `getArtistNeighbours` (`artist-dossier.ts:627`) — the `/artist/*` neighbour rail's edge read carries a correlated `exists(track_artists ⋈ findings)` certification probe evaluated per neighbour row, and an `/artist/*` render fires several of these reads concurrently. Both sides are indexed on paper (`track_artists_artist_id_idx`, `findings.track_id` is the PK), so the remedy is a query-shape decision — precompute `certified` onto the edge row, or fold the probe into one join — and AGENTS.md requires proving that against hosted Turso with `EXPLAIN QUERY PLAN`, which this sweep cannot run. Same family as FLUNCLE-WORKER-1F/-1G.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | open   | https://fluncle.sentry.io/issues/138804200/ <!-- sentry_id:138804200 --> |
| 2026-08-05 | fluncle-worker | FLUNCLE-WORKER-22 | LibsqlError           | The rank tick's corpus-count read (`rankCatalogue`, `catalogue.ts:1169`) hit a fatal 524 through `runWithRetry`. 524 is excluded from the retry set deliberately (`db.ts:72` — an origin timeout may already have executed, and a retry doubles load on an origin that is timing out), so this needs a hosted plan for the two `count(*)` subqueries over `findings ⋈ tracks`, not a retry. The error-side sibling of FLUNCLE-WORKER-1A, which caught the same function's stale-candidate SELECT; judge the two together.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | open   | https://fluncle.sentry.io/issues/138803147/ <!-- sentry_id:138803147 --> |
| 2026-08-05 | fluncle-web    | FLUNCLE-WEB-F     | CSP violation         | A `radio.fluncle.com` image blocked on `/log/020.8.6A`. Already fixed on `main`: 7d5eec1b (#1123) admitted that host to `img-src`, and the single event (2026-08-04T21:15Z) predates that commit, so there is no code change left to make. Filed so the operator resolves it in Sentry — the sweep resolves an issue only when its own fix PR merges, and this one has none.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | open   | https://fluncle.sentry.io/issues/138882377/ <!-- sentry_id:138882377 --> |
| 2026-08-04 | fluncle-worker | FLUNCLE-WORKER-21 | Slow DB Query         | The rank tick's corpus-count read (`catalogue.ts:1172`) — the two numbers that ARE the staleness fingerprint (`rankCorpus`), so changing what they count re-stales every stored `catalogue_rank_corpus` and forces one full re-rank of the catalogue (the ~55k-row event `v5` exists to prevent). The obvious rewrite is FLUNCLE-WORKER-W's — express it on the maintained mirrors (`is_catalogue = 0` + `has_embedding`) so `tracks_funnel_scan_idx` can cover it — and it carries that mirror's own documented trap: on an existing database `has_embedding` sits physically AFTER the 4 KB blob, so reading it off a table row is WORSE than the blob's null flag, and the swap only pays if the planner actually picks the covering index. That needs a hosted `EXPLAIN QUERY PLAN`. Judge it with the rest of the same tick (FLUNCLE-WORKER-17/1C/7/11): three sequential round-trips per tick may be one batching decision rather than three rewrites.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | open   | https://fluncle.sentry.io/issues/138484635/ <!-- sentry_id:138484635 --> |
| 2026-08-03 | fluncle-web    | FLUNCLE-WEB-7     | CSP violation         | `blocked-uri: eval` from zod's JIT capability probe, ALREADY FIXED in `main` — `configureZod({ jitless: true })` in `client.tsx` (commit `0b5ecf46`). The issue's last event is 2026-07-29T17:27:33Z, ~9 minutes BEFORE that commit; nothing has fired in the five days since, and `security-headers.test.ts` pins `CONTENT_POLICY` against ever granting `'unsafe-eval'`. There is no code change left to make, so no fix PR can carry it — filed so a human resolves it in Sentry (triage never resolves an issue itself). Secondary observation, not a fix: every event names the workers.dev preview origin rather than `www`, so branch-preview deploys write into the production Security feed under their own `sentry_release`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | open   | https://fluncle.sentry.io/issues/136702489/ <!-- sentry_id:136702489 --> |
| 2026-08-03 | fluncle-web    | FLUNCLE-WEB-E     | CSP violation         | `font-src` blocking Scalar's 14 webfaces on `/docs/api`, ALREADY FIXED in `main` — `withDefaultFonts: false` in `routes/docs.api.tsx` (commit `2e9e96f0`), which stops the requests at the source instead of widening `font-src` to a third-party origin (`customCss` already re-points `--scalar-font` at the self-hosted stack). All 14 events share one timestamp, 2026-08-02T03:20:35Z, ~22 minutes BEFORE that commit: this issue IS the artifact of the real-browser sweep that found it, recorded in `docs/error-tracking.md`. Nothing left to change — filed so a human resolves it in Sentry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | open   | https://fluncle.sentry.io/issues/138210644/ <!-- sentry_id:138210644 --> |
| 2026-08-02 | fluncle-worker | FLUNCLE-WORKER-20 | Slow DB Query         | `getArtistBySlug` (`artists.ts:106`) — the public `/artist/<slug>` entry read. The shape is already optimal: a `limit 1` point seek on `artists.slug`, declared `unique` in the schema (`db/schema.ts:3213`), selecting 13 columns off one rowid. So the span is Worker→Turso round-trip latency — plus whatever `runWithRetry` backoff it wraps, since the `db.query` span encloses the retry loop (`db.ts:230`) — not query cost. Same family as FLUNCLE-WORKER-X: the levers are caching the resolution or folding it into a neighbouring read on a public hot path, and it is serial (the page's six parallel `artist.id` reads all wait on it). A public-hot-path architecture call, not a mechanical fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | open   | https://fluncle.sentry.io/issues/138197823/ <!-- sentry_id:138197823 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1Z | LibsqlError           | `insertRunEvent` (`run-events.ts:686`) — a 502 escaped the deterministic `insert … on conflict(id) do nothing` write while the read-side gateway retry was live. The write is replay-safe by its stable id, but the generic DB wrapper deliberately never retries writes; this path needs its own idempotent transient retry so a gateway blip cannot drop a run receipt.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | open   | https://fluncle.sentry.io/issues/137804620/ <!-- sentry_id:137804620 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1Y | LibsqlError           | `settle` (`crawl.ts:353`) — a 502 escaped the crawl-frontier state write. This is the same gateway incident as FLUNCLE-WORKER-1Z, but not the same fix: the update increments `attempts`, so it cannot be blindly replayed. It needs an outcome-safe/idempotent settle write or explicit recovery semantics.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | open   | https://fluncle.sentry.io/issues/137804546/ <!-- sentry_id:137804546 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1B | LibsqlError           | `reconcileHubCounts` (`hub-counts-reconcile.ts:185`) — the first full-table grouped correction hit a 524. The module already records the 19.3s hosted recompute precedent and says its zero-truth pass is unmeasured; this needs a hosted plan/rewrite or chunked reconciliation, not a retry (524 is intentionally excluded).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | open   | https://fluncle.sentry.io/issues/136852887/ <!-- sentry_id:136852887 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1A | LibsqlError           | The rank tick's stale-candidate SELECT (`catalogue.ts:1216`) hit a fatal 524. This is the error-side twin of FLUNCLE-WORKER-17's slow-query finding over the same statement; judge them together. It needs hosted `EXPLAIN QUERY PLAN` and a rewrite/index proven there, not a retry of an origin timeout.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | open   | https://fluncle.sentry.io/issues/136834111/ <!-- sentry_id:136834111 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-16 | LibsqlError           | `updateTrack` (`track-update.ts:845`) — its transactional write batch hit 524 twelve times. The current wrapper intentionally does not retry writes or 524s because commit outcome is ambiguous; this needs hosted diagnosis plus an outcome-safe write/idempotency design, not a blanket retry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | open   | https://fluncle.sentry.io/issues/136628839/ <!-- sentry_id:136628839 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-15 | LibsqlError           | `insertEdges` (`backfill-artist-credits.ts:322`) — the artist-edge + hub-count + rank-restale write batch hit 524. A partial outcome cannot be inferred and its delta statements make blind replay unsafe; it needs hosted diagnosis and an idempotent/transactional resume design.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | open   | https://fluncle.sentry.io/issues/136628830/ <!-- sentry_id:136628830 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1  | Spotify 429           | `publishTrack`'s metadata GET exhausted `spotifyFetch`'s existing two retries / 10s wait budget, so an operator add surfaced as a 500. The shared throttle breaker reduces optional pressure but the throw path still exists; this needs graceful upstream-429 mapping and/or stronger shared-call pacing, not resolution.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | open   | https://fluncle.sentry.io/issues/135069153/ <!-- sentry_id:135069153 --> |
| 2026-07-31 | fluncle-web    | FLUNCLE-WEB-A     | CSP violation         | Cloudflare's injected RUM script is still being blocked on public pages (last seen 2026-07-31). It is the same root cause as FLUNCLE-WEB-8 and -9, split by route/directive. The human action is to verify and disable Cloudflare Web Analytics at the edge (Simple Analytics already owns measurement), not to allowlist or archive the fingerprint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | open   | https://fluncle.sentry.io/issues/136925170/ <!-- sentry_id:136925170 --> |
| 2026-07-31 | fluncle-web    | FLUNCLE-WEB-9     | CSP violation         | Duplicate family with FLUNCLE-WEB-A/-8: `static.cloudflareinsights.com`, grouped separately under `script-src`. Judge the three together; confirm/disable the edge-injected RUM source rather than weakening the policy or muting a script-injection detector.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | open   | https://fluncle.sentry.io/issues/136925132/ <!-- sentry_id:136925132 --> |
| 2026-07-31 | fluncle-web    | FLUNCLE-WEB-8     | CSP violation         | Duplicate family with FLUNCLE-WEB-A/-9: the same Cloudflare RUM host under `script-src-elem` (one event carried the explicit `:443` URI). Same dashboard-side action; no independent code fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | open   | https://fluncle.sentry.io/issues/136808894/ <!-- sentry_id:136808894 --> |
| 2026-07-31 | fluncle-web    | FLUNCLE-WEB-B     | CSP violation         | A browser tried to load a Perplexity-hosted font while Fluncle's current code and policy remain self-hosted/`font-src 'self'`; this is external injection, not an app asset. Do not archive it: that fingerprint could hide a future real font injection. Only an emitter-side filter with collision-safe provenance would make it noise.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | open   | https://fluncle.sentry.io/issues/136992004/ <!-- sentry_id:136992004 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1X | Large payload         | Beatport resolution asks Firecrawl for the page's complete `rawHtml` and then parses `__NEXT_DATA__` + rendered links (`beatport-resolve.ts:112`); four responses tripped the large-payload detector. The current one-call identity gate is live. It needs a lean extraction response or an explicit, collision-safe detector decision—never a blanket archive of the Firecrawl endpoint fingerprint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | open   | https://fluncle.sentry.io/issues/137763185/ <!-- sentry_id:137763185 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-4  | Large payload         | Cover-master backfill intentionally downloads CAA's `front-1200` into an isolate buffer, with a 5 MB guard only after the body arrives (`cover-masters.ts:200`). The shape is live and still firing. It needs a smaller source rendition or a streaming/content-length cap plus a deliberate detector decision; do not archive the host-wide payload fingerprint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | open   | https://fluncle.sentry.io/issues/135156294/ <!-- sentry_id:135156294 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-W  | Slow DB Query         | `readCatalogueCaptureSpend` (`capture-budget.ts:271`) still expresses the catalogue slice as `left join findings … where f.track_id is null` despite the maintained `tracks.is_catalogue` flag. Rewrite it to the flag and prove the exact aggregate on hosted Turso.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | open   | https://fluncle.sentry.io/issues/135974859/ <!-- sentry_id:135974859 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-12 | Slow DB Query         | The batched own-channel lookup (`tracks.ts:2704`) joins `artist_socials(platform='youtube')` to `track_artists` for the current worklist ids. It is bounded but still slow in production; inspect the hosted plan and judge the existing single-column indexes versus a composite/reordered join.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | open   | https://fluncle.sentry.io/issues/136599513/ <!-- sentry_id:136599513 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1E | Slow DB Query         | `readFindingIsrcs` (`catalogue.ts:942`) reads every finding ISRC and applies `trim(isrc) <> ''` each rank tick. It is bounded by findings but the predicate is non-sargable and still crosses the slow threshold; decide with the other finding-affinity reads whether to normalize at write time, materialise, or cache.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | open   | https://fluncle.sentry.io/issues/136947887/ <!-- sentry_id:136947887 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1H | Slow DB Query         | `/tracks` step one's `order by release_date, track_id limit/offset` id-page read (`tracks-hub.ts:488`) still exists and slows as the offset grows despite the release-date index. It needs hosted proof for keyset pagination or a bounded page strategy; the page hydration is a separate query and not this issue.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | open   | https://fluncle.sentry.io/issues/137171950/ <!-- sentry_id:137171950 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1J | Slow DB Query         | `/tracks` year-lane aggregation (`tracks-hub.ts:662`) scans the covering release-date index, groups `substr(release_date, 1, 4)`, and memoizes for 60s. The code explicitly keeps it as a standing survivor; this production finding is the evidence needed to revisit a maintained year rollup or a longer cache.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | open   | https://fluncle.sentry.io/issues/137183568/ <!-- sentry_id:137183568 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-Y  | Slow DB Query         | Artist-credit `listWork` (`backfill-artist-credits.ts:282`) still uses the `tracks → track_artists` anti-join over a partial-indexed, `limit`-bounded queue. The index had shipped before its final event, so this needs a hosted plan and likely an anti-join/worklist rewrite rather than assuming the limit terminates the scan.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | open   | https://fluncle.sentry.io/issues/136209482/ <!-- sentry_id:136209482 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-7  | Slow DB Query         | `WEIGHTED_QUALIFIED_ARTISTS_SQL` (`catalogue.ts:829`) — the `labels → tracks → track_artists` group-by/`having` walk of the whole enabled-label lane, read every rank tick. Shape (B) of `docs/db-scale-backlog.md`; the fix is an index or a rewrite, and AGENTS.md requires both be proven on hosted Turso (five such indexes were already REJECTED at that gate), which triage cannot do.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | open   | https://fluncle.sentry.io/issues/135764803/ <!-- sentry_id:135764803 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-11 | Slow DB Query         | `FINDING_QUALIFIED_ARTISTS_SQL` (`catalogue.ts:820`) — `distinct` over the `track_artists ⋈ findings` graph join, run twice per tick (bare in `readArchiveAffinity`, again inside the fingerprint union at `catalogue.ts:1193`). Same hosted-proof gate as FLUNCLE-WORKER-7, and the two should be judged together.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | open   | https://fluncle.sentry.io/issues/136551873/ <!-- sentry_id:136551873 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-17 | Slow DB Query         | The rank tick's stale-candidate pick (`catalogue.ts:1216`) — `limit`-bounded, but its staleness disjunction (`catalogue_rank_corpus is null or <> ? or (embedding_blob is not null and capture_priority >= 0)`) has no covering index, so whether the `limit` actually bounds the scan needs an `EXPLAIN QUERY PLAN` on hosted Turso. A judgment call on the sweep's core read, not a mechanical fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | open   | https://fluncle.sentry.io/issues/136656326/ <!-- sentry_id:136656326 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-18 | Slow DB Query         | `readCatalogueIdentity` (`catalogue.ts:1044`) — an UNBOUNDED read (no `limit`) of every captured catalogue row's identity columns, once per tick, with the dedupe map built in the isolate. Bounding it means changing how canonical selection is decided, which is product behaviour, not a guard.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | open   | https://fluncle.sentry.io/issues/136675393/ <!-- sentry_id:136675393 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-X  | Slow DB Query         | `isGalaxyMapFullyNamed` (`galaxies-map.ts:235`) — a trivial `count(*)` over the small `galaxies` table, but on the PUBLIC `/log/*` render, so the span is round-trip latency rather than query cost. The fix is caching the launch gate or folding it into a neighbouring read (the pattern `loadNamedGalaxyRows` already uses at `galaxies-map.ts:390`) — a public-hot-path architecture call, and parallelising alone would not shorten the span the detector measures.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | open   | https://fluncle.sentry.io/issues/135988649/ <!-- sentry_id:135988649 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-19 | Slow DB Query         | The Ear's lens read over `CATALOGUE_SELECT` (`catalogue.ts:2400`) — a 21-column per-row projection. The shipped precedent is db-scale item 7 (swap the fat select for a lean one), but which columns `/admin/catalogue` actually renders is a call on the admin surface, not a mechanical narrowing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | open   | https://fluncle.sentry.io/issues/136775025/ <!-- sentry_id:136775025 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1C | Slow DB Query         | The fingerprint's sorted qualified-artist read (`catalogue.ts:1191`) — the `union` of FLUNCLE-WORKER-7's and FLUNCLE-WORKER-11's shapes plus an `order by`. It cannot be fixed independently of those two; whatever is done to them decides this.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | open   | https://fluncle.sentry.io/issues/136919188/ <!-- sentry_id:136919188 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-Z  | Slow DB Query         | The `/album/<slug>` findings read (`findingsByEntity` on `tracks.album_id`, `tracks.ts:889`) — a wide per-row projection over the growing `tracks` table on a public page. Same lean-the-projection family as FLUNCLE-WORKER-19, but on a public surface where the DTO is typed against `GraphFindingItem`, so narrowing it further is a cross-cutting change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | open   | https://fluncle.sentry.io/issues/136228156/ <!-- sentry_id:136228156 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1D | Slow DB Query         | `readFindingIdentity` (`catalogue.ts:992`) — the `findings ⋈ tracks` identity read that pulls title + `artists_json` for every certified finding into the isolate each tick. Bounded by the finding count by design; making it cheaper means caching or materialising the identity map, a design call.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | open   | https://fluncle.sentry.io/issues/136947023/ <!-- sentry_id:136947023 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1G | Slow DB Query         | The distinct-label affinity read in `readArchiveAffinity` (`catalogue.ts:858`) — `trim(tracks.label) <> ''` is non-sargable, so no index on `label` can serve it. Making it sargable changes which labels qualify at the edges (whitespace-only strings), so it is a behaviour call, not a guard.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | open   | https://fluncle.sentry.io/issues/136947891/ <!-- sentry_id:136947891 --> |
| 2026-07-31 | fluncle-worker | FLUNCLE-WORKER-1F | Slow DB Query         | The `artists_json` affinity read in `readArchiveAffinity` (`catalogue.ts:853`) — pulls a JSON column for every certified finding into the isolate to build a name set. The durable fix is reading the resolved `track_artists` graph instead of the JSON blob, which changes what "a finding's artist" means to the affinity set — canon-adjacent, and outside triage's bar.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | open   | https://fluncle.sentry.io/issues/136947888/ <!-- sentry_id:136947888 --> |

<!-- The triage sweep appends rows below this line. Newest run on top. Each row ends with `<!-- sentry_id:<id> -->`. -->
