# The artist relationship

Fluncle keeps a canonical **artist entity** (`artists`, keyed on the Spotify artist id) with a `track_artists` many-to-many and an identity graph (`artist_socials`). The archive gets an artist entity, resolution, the `/artist/<slug>` pages, and the SEO/AEO graph.

This doc covers the **identity graph** — how `artist_socials` is resolved, reviewed, and rendered into the public artist page + `sameAs` JSON-LD. The entity + tables it builds on are Unit 1.

> **The relationship is identity-only:** resolve → review → confirm → public page. Fluncle does not maintain artist-follow state.

## The data model

`artist_socials` is the identity-graph store — one row per `(artist_id, platform)`:

- `platform` — the socials only (`spotify | youtube | soundcloud | instagram | tiktok | bluesky | mixcloud | twitch | bandcamp | beatport | twitter | facebook | homepage`). Beatport is a link-only store platform (the key DnB store/profile) — it was promoted out of the MB aggregator denylist to a first-class social. Twitch is the live-stream surface (many DnB acts stream there, and Fluncle itself lives at `twitch.tv/flunclelive`) — its channel handle is the first path segment, like SoundCloud/Twitter. Bluesky (`bsky.app/profile/<handle>`, where the handle can be a domain like `nutone.bsky.social` or a `did:plc:…`) is verified by an unauthenticated DID resolution probe — `com.atproto.identity.resolveHandle` on `public.api.bsky.app` (`resolveBlueskyHandle`, lib/server/bluesky.ts): a handle that resolves to a DID is a live, verifiable identity, one that doesn't is a dead link (the honest-404 oracle the liveness sweep auto-removes, alongside soundcloud/mixcloud/youtube). The DID itself is not persisted — `artist_socials` has no per-platform metadata column, and the canonical `bsky.app/profile/<handle>` URL is the stored identity — so resolution is a probe, not a stored field. The KG anchors (`mbid`, `wikidata_qid`, `discogs_url`, `lastfm_url`) live as `artists` columns, not here — Discogs and Last.fm are catalogue records OF the artist rather than channels the artist runs, so they follow the `labels.discogs_label_id` precedent: an identity column emitted into `sameAs`, with no rendered link.
- `status` — `auto` (MusicBrainz/operator, trusted) · `candidate` (Firecrawl-only, awaits a human glance) · `confirmed` (a candidate promoted). Only `auto`/`confirmed` render on the public artist page + `sameAs`.
- `source` — `musicbrainz | firecrawl | operator`.
- `reviewed_at` — Review state belongs to each `artist_socials` link through `reviewed_at`. New or changed links clear the stamp; operator-authored links are born reviewed.

There is no follow/champion state on the row — the graph is purely who the artist is, not what Fluncle did about it.

## The `track ↔ artist` edge (how a crawled track earns its artists)

The `track_artists` edge is what lets `/artist/<slug>` show the rest of an artist's catalogue by an indexed seek (`track_artists_artist_id_idx`) rather than a full `artists_json` scan (the shape AGENTS.md forbids). A CERTIFIED track gets its edge at publish (`upsertTrackArtists`, folded on the stable `spotify_artist_id`). A CRAWLED track earns it two ways, in priority order:

Spotify-anchored tracks connect artists by stable Spotify ID. Tracks without Spotify identity may link by guarded name-fold matching. `scripts/backfill-artist-links.ts` reconciles missing name-linked edges, and `scripts/backfill-artist-graph.ts` fills stable-ID edges where possible.

**A crawl-minted artist is PUBLIC on its content, exactly as a discovered label/album is.** A findings-free artist with a row renders its `/artist/<slug>` page (its findings lead, then the rest of its catalogue), and once it clears the thin-content floor it enters the sitemap too — reachability follows the label/album rule. There is no certified-finding gate on the artist surface: a slug with no `artists` row 404s, a row renders, and the thin-content floor decides whether it indexes. That floor is now catalogue-aware — `resolveArtistPageData`'s `indexable` and `listArtistSitemapRows` count RENDERABLE tracks (certified findings via `countArtistFindings` PLUS the quieter catalogue rows via the catalogue's SQL-counted `totalTracks`), both read through the canonical `track_artists` join so an indexable page is never orphaned from the sitemap. The label/artist catalogue-heading link lights whenever the credited artist has an entity. The `/artists` INDEX is ONE unified A–Z list of every artist Fluncle holds (`listArtistsHubPage`) — certified findings and the wider catalogue in one alphabetical `?page=N` surface, a certified artist's name lit in Eclipse Gold, the rest unlit — so a crawler-discovered artist page is linked rather than orphaned. The unified-index shape is documented once, in [docs/album-entity.md](./album-entity.md#the-sitemap-carries-every-page-the-hub-is-one-unified-index).

### Artist-edge backfill

`backfill_artist_edges` links tracks that lack artist edges by matching credited names against canonical names and trusted aliases. It is bounded, agent-tier, and stamps every visited track.

**The no-minting rule.** A bare name string is not enough identity to CREATE an `artists` row — a row is an entity with a public page — so the backfill mints nothing; it only links names to identities that already exist, exactly like `linkTracksToArtistEntities`. A fold that two distinct identities share is ambiguous and matches nothing (fail-closed); a primary `artists.name` beats an alias for the same fold. It makes NO vendor call (pure DB matching), writes graph identity only (no publish, no certification), and stamps every VISITED track (`tracks.artist_edges_backfilled_at`) — matched, partial, OR zero-match — so a zero-match row is not re-chewed each tick and a re-run over drained history is a cheap no-op (the `mb_recording_id_attempted_at` discipline).

**The residual is the point of the report.** Each pass returns tracks fully matched, partially matched, and zero-matched, plus `unmatchedNames` — the credited names that folded to no existing identity. That residual (a name Fluncle has never certified an artist for) is what decides whether a later paced MusicBrainz credit-sweep to MINT those artists is worth running; the backfill itself deliberately stops at existing identities.

### The MB credit sweep (`backfill_artist_credits`, agent tier)

`backfill_artist_credits` processes zero-matched tracks that carry a MusicBrainz recording identity. It resolves credited artists by MBID, adopts an unambiguous existing row, or mints an MBID-backed entity.

**The three-rung resolve (mbid → adopt → mint).** Each credited artist resolves down a ladder, and the middle rung is what keeps this sweep from becoming a duplicate factory. (1) **Exact mbid** — an `artists` row already carries this MB artist id → use it. (2) **Adopt** — the credit NAME folds _unambiguously_ onto an existing artist that has no `mbid` yet → stamp the mbid onto that row (`adoptArtistMbid`, a non-clobbering `coalesce` + a `mbid is null` guard, the `artist-resolution.ts` precedent) and reuse it. This is the common case, not the edge case: the zero-matched residual is dominated by **compound credit strings** ("Sub Focus & Dimension" as one `artists_json` entry) whose members Fluncle already holds as Spotify-keyed rows (`mbid` still NULL) — slice 0 just could not match the compound string. Minting here would spawn a duplicate per such credit, a mass generator of the split-identity class the label-merge op cleans for labels (artists have **no** merge op at all), so adopting is mandatory. (3) **Mint** — no mbid match and no adoptable fold → mint a fresh row via `mintArtistByMbid` (`lib/server/artists.ts`), the sibling of `upsertTrackArtists`'s Spotify-keyed mint, keyed on the MB artist id. An MB artist id IS identity (a curated, dereferenceable MBID), so a row born from one is honest — the RFC's identity-true mint, and the reason slice 0's no-minting rule does not bind here.

**Fail closed at every ambiguity.** The fold is the slice-0 matcher (`buildArtistFoldMap` + `artist_aliases`), reused wholesale and built **once per pass** (not per credit). A fold two distinct identities share is dropped (ambiguous → no adopt → mint), and a fold match whose row already carries a _different_ mbid is a homonym (→ mint), because a wrong **merge** of two artists is unrecoverable while a rare **split** is not. The `track_artists` edges are then written `insert or ignore` (position from credit order, `role` null, the slice-0 edge shape). The MusicBrainz "Various Artists" placeholder is never minted or edged. A zero-matched track with **no** MB recording identity is **terminally skipped** — stamped so it drains, never retried.

**`artists.mbid` stays non-UNIQUE, and now carries a plain lookup index.** This sweep's three rungs still need no `where mbid = ?` seek: the resolver loads the whole `artists` corpus (id + name + mbid) ONCE per pass to build the fold map (rung 2 is impossible without every name in memory), and reads the exact-mbid map (rung 1) and the adopt decision off that same in-memory corpus — the slice-0 set-based discipline. The index (`artists_mbid_idx`) is earned by the CRAWL-side seal instead, which runs its two mbid clauses inside SQL, per credited artist, on every release the crawler writes — unindexed, each one scans the whole artists table. Uniqueness is deliberately NOT enforced on the column: `mbid` is nullable and SQLite counts NULLs as distinct (the Spotify/name-minted majority is unconstrained regardless); the sweep is single-flight (one paced pass, awaited per credit) with a per-pass in-memory mbid map, so it never mints the same mbid twice within a pass; and the adopt rung _shrinks_ the only real race window by folding compound-credit members onto the existing row instead of minting. A UNIQUE constraint would also be unsafe to add to the populated column — `artist-resolution.ts` has historically stamped `mbid` via independent `coalesce` updates, so a pre-existing duplicate would fail the migration. The rare residual split (two rows for one MB identity) is recoverable; a hard constraint that could wedge a migration or reject an honest adopt is not.

### Homonym seal

Name-fold linking must not connect a credit to an artist whose MusicBrainz identity disagrees. Credits without an MBID may claim an identified artist only through a canonical name or trusted alias. Ambiguous credits remain unmatched for the MBID-backed sweep. Use the catalogue-prune detector and split repair for existing conflations.

**Its own reliability stamp.** The per-row stamp is `tracks.artist_credits_backfilled_at` — DISTINCT from slice 0's `artist_edges_backfilled_at`, which this sweep never writes or re-nulls (slice 0's semantics stay exactly as shipped). Every visited row is stamped — edged OR terminally skipped — so the worklist drains and a re-run is a cheap no-op (the `mb_recording_id_attempted_at` discipline). Because it makes vendor calls, the pass is SMALL (`MAX_BATCH` 40) and carries the label-lineage protections: a 60s response budget that pauses mid-page and resumes on the next request, and the `rateLimited` circuit breaker that stops the pass without stamping the throttled row. It writes graph identity only (no publish, no certification), so the box's agent-token `fluncle-artist-credits` HOST timer drives it (docs/agents/hermes/artist-credits-timer/) at a 5-minute cadence — the ~14.3k residual drains at ~40/tick over ~a day and a half. Minting these edges re-stales The Ear's ranking automatically: the rank corpus fingerprint (`rankCorpus`, `lib/server/catalogue.ts`) already folds the total `track_artists` edge count, so every new edge moves it and the catalogue re-ranks against the fuller graph — no separate fingerprint component is needed for the minted artists, because authorization depends on the edges (captured) and findings/label rulings, never on the raw `artists`-table cardinality.

## Resolution (`resolve_artist`, agent tier)

The box's `fluncle-artist-sweep` cron reads the resolve worklist (`list_unresolved_artists`) and calls `resolve_artist` per row: a MusicBrainz url-rels walk (→ `status=auto`, trusted) plus a Firecrawl gap-fill (→ `status=candidate`, operator-confirm before public). The gap-fill backfills **every missing social platform except `homepage` and `spotify`** (`instagram, tiktok, bluesky, youtube, soundcloud, bandcamp, twitter, facebook, mixcloud, twitch, beatport`) — `homepage` is excluded because MB already covers it, and `spotify` is always known (it's the identity key). Everything Firecrawl returns lands as `status=candidate`, so the wider net stays behind the operator-review gate. `wikidata` classified during the MB walk is routed to the `wikidataQid` KG anchor on `artists`, not into `socials` — and so are the two secondary anchors: `classifyMbAnchorUrl` picks a Discogs `/artist/…` or Last.fm `/music/…` url-rel out of the same walk (path-shape guarded, so a `/release/…` is not an identity) and `persistResolution` stamps them `coalesce(?, …)`, exactly like `mbid`. They are **MB-relation-sourced only** — a URL guessed from an artist's name would be a fabricated anchor — and they are emitted into the `MusicGroup`'s `sameAs` ranked Wikidata > MusicBrainz > Discogs > Last.fm > Spotify > socials. `classifyMbUrl` still returns null for both hosts, so neither can ever become an `artist_socials` row.

The Firecrawl gap-fill uses link-hub scraping first, then hub search and scraping, then disambiguated per-platform search.

Resolve the MusicBrainz artist by name search and exact Spotify URL relation. Fall back to score ≥90 plus exact normalized name only when no candidate exposes a Spotify relation.

The worklist is **self-healing**: `list_unresolved_artists` returns artists with `resolved_at IS NULL` PLUS artists resolved to zero socials whose stamp is older than 30 days, so a transient MB failure (a 503 window, a namesake not yet disambiguated, an artist MB has since gained a Spotify rel for) re-tries at most once per window instead of sticking on 0 socials forever. Artists that already have socials are never re-queued. For an IMMEDIATE flush of a freshly-stamped empty backlog (which the 30-day window wouldn't touch yet), the operator runs `bun run apps/web/scripts/requeue-empty-artists.ts --confirm` (dry-run without `--confirm`), which clears `resolved_at` for every artist with zero `artist_socials` rows.

## Artist avatars (`artists.image_url`)

Each artist carries its avatar source in `artists.image_url` — the largest Spotify profile image (an `i.scdn.co` URL, the same source precedent as `tracks.album_image_url`). The downstream owned-master sweep ingests that source into Fluncle's R2 and leaves the raw URL as the fallback. Two write paths keep the source filled, both idempotent and best-effort so a Spotify hiccup never blocks the fast synchronous add:

- **Create/resolve** — `upsertTrackArtists` (the ingest + entity-backfill path) calls `fillMissingArtistImages` after upserting the rows: for any of the track's artists still lacking an image and still in `image_state='pending'` (a freshly-minted artist always is), it fetches the largest Spotify image via the per-id `/v1/artists/{id}` endpoint and stamps `image_url`. An artist that already has an image or a terminal `none` verdict costs a single indexed read and no API call.
- **Backfill** — `backfill_artist_images` (agent tier, `POST /admin/backfill/artist-images`; CLI `fluncle admin backfills artist-images`) is the bounded, cursor-resumable sweep over pending artists minted before the image column (`image_url IS NULL` + `image_state='pending'` + a Spotify id to look up). Spotify only permits this app's per-id `/v1/artists/{id}` lookup, so the sweep consults the shared Spotify call meter before every artist and stops the tick on proactive budget deferral or an exhausted 429 backoff. A matching 200 response with no usable image is a terminal absence verdict: `image_state='none'`, `image_attempted_at` is stamped, failures reset to zero, and the downstream owned-master sweep no longer waits for a source that does not exist. Malformed/mismatched responses and transient failures remain pending. The endpoint reports the actual `checkedCount`, throttle flags, and the exact post-pass `queueDepth`; a throttle nulls `nextCursor` so the current CLI drain stops without claiming the queue is empty.

Render: the `/artists` index is avatar cards (round Spotify image over name + finding count) and the artist page's "Similar artists" chips carry a small round avatar, both via `ArtistAvatar` — a graceful monogram tile (first letter) stands in when `image_url` is null. The image is decorative (empty `alt`, the name is adjacent); WCAG-AA, keyboard-reachable, reduced-motion-safe.

## Artist aliases (`artist_aliases`) — DnB's many-names problem

A DnB act records under many names (an alias, a real name, a sort name, a locale variant). `artist_aliases` is the store that reconciles them all to ONE artist entity without ever rewriting `artists.name` — the exact structural twin of `label_aliases` (docs/label-entity.md): a per-artist side table of alternative spellings, keyed `(artist_id, alias_slug, source)` unique, INSERT-only (`on conflict do nothing`), so a re-resolve never duplicates one and never disturbs an operator ruling.

**MusicBrainz curates them directly, so the resolve pipeline harvests them for free.** The `resolve_artist` walk already deep-fetches each candidate's `/artist/<mbid>?inc=url-rels`; adding `+aliases` brings the artist's aliases back on the SAME lookup (no extra MusicBrainz call), and `extractAliasesFromArtistData` folds them onto the matched candidate — skipping any alias that folds to the canonical name's slug, deduping by slug, and splitting a MusicBrainz "Search hint" (kept as `kind='hint'`, never rendered) from a real display name (`kind='name'`).

**Trust: `auto`, not `candidate`.** Unlike `label_aliases` (whose Apple×ISRC candidate is a fuzzy cross-source INFERENCE that awaits an operator glance), a MusicBrainz alias is a DIRECT statement of identity — the same authority that makes MB **socials** born `auto` (trusted/public) rather than `candidate`. So `artist_aliases.status` is `auto | confirmed` (no `candidate` tier): MB-harvested aliases are born `auto`, an operator-added alias is `confirmed`, and both feed the public graph. **Already-resolved artists pick this up automatically** — the self-healing worklist (`list_unresolved_artists`) re-queues an artist resolved to zero socials after 30 days, and any fresh resolve harvests aliases on its next pass; a re-resolve of an artist that already has socials is a genuine no-op only because it is not re-queued (aliases ride the same walk, so there is no separate backfill knob to add).

Emit confirmed name aliases as `alternateName` in `MusicGroup` JSON-LD. Artist aliases are outside the track-keyed FTS5 index because indexing them requires artist-table triggers and a many-to-many fan-out join.

## The `/admin/artists` review queue (the operator motion)

`/admin/artists` is the stable MANAGE surface for every artist Fluncle features — one card per artist, name-sorted. The **review model**: review lands on the LINK, not the artist. A link "needs a look" while its `reviewed_at` is NULL — a fresh resolver insert, or a machine re-resolve that changed its URL (`artistNeedsLook` is now just `socials.some(s => s.reviewedAt === null)`). A **Fresh links** section at the top of the board lists exactly those unreviewed links, grouped by artist, each with **Approve** (`review_artist_social`: stamp `reviewed_at = now` AND promote a `candidate` to `confirmed`), **Edit**, and **Remove** — so a single new Twitch link surfaces on its own instead of re-flagging the whole already-reviewed artist. **Edit is inline**: the pencil turns the link text into an input IN PLACE (matched to the row's button band so the row never jumps), Approve becomes **Save**, and Save corrects the URL AND approves it in one act (`update_artist_social`) — the entered URL is validated + normalized against the row's platform on the server (reusing the resolver's `classifyMbUrl` + `normalizeProfileUrl`: a YouTube row rejects an `instagram.com` URL; a deep link collapses to the profile root, or is rejected with a quiet inline reason), then stored `source=operator`, `status=confirmed`, born reviewed (the operator just fixed it). Escape/blur restores the original. This is the daily fix for a resolver miss (a `music.youtube.com/search` page, a label's Bandcamp mistaken for the artist) without leaving the row. The per-artist **"Looks good"** (`review_artist`) stays as the BULK: it stamps every one of an artist's links reviewed at once and promotes any surviving candidates (reviewing IS the trust gate that lets a link onto the public page + `sameAs`). Add/remove a platform inline behind the **Manage links** dialog: a Shadcn `Select` (the platform logo next to `SelectValue`) + a URL `Input` → `add_artist_social` / `remove_artist_social` (an operator-entered link lands `source=operator`, `status=confirmed`, born reviewed). The WORK surfaces as an `/admin` attention row (source `artist-review`, counting UNREVIEWED links per artist) that deep-links here with `?artist=<id>`, auto-expanding that artist.

**The board PAGINATES at catalogue scale.** The crawler mints artists without end, so the board is not one whole-archive read: it pages the artist list ~50 at a time (a keyset seek on `(name, id)` over `artists_name_idx`, a loader-seeded `useInfiniteQuery`, name search pushed server-side as a `name like ?` arm), and each artist's finding count is a GROUPED `track_artists ⋈ findings` join evaluated once per page rather than a correlated per-row scalar. The **Fresh links** section is its own bounded server read (`listFreshLinks`, capped at `FRESH_LINKS_LIMIT`, oldest-first) so the operator's live work stays complete and independent of which page is scrolled — with the true total carried alongside so anything past the cap shows as a quiet overflow note, never hides. The `artist-review` attention source is likewise capped (`ARTIST_REVIEW_QUEUE_LIMIT = 25`, oldest-first) — the exact twin of `LABEL_REVIEW_QUEUE_LIMIT`, so a crawl minting links faster than they are reviewed never drowns the other cockpit sources; `/admin/artists` stays the station where the full list is worked.

### The board's automated-socials cell

The findings board exposes an automated-socials aggregate whose hover card includes the Last.fm breakdown.

## The ops (verb_noun; `packages/contracts/src/orpc/admin-artists.ts`)

| op                      | tier                       | path                                             |
| ----------------------- | -------------------------- | ------------------------------------------------ |
| `list_artist_socials`   | admin (agent-allowed read) | `GET /admin/artists/socials`                     |
| `confirm_artist_social` | operator                   | `POST /admin/artists/socials/{socialId}/confirm` |
| `add_artist_social`     | operator                   | `POST /admin/artists/{artistId}/socials`         |
| `remove_artist_social`  | operator                   | `DELETE /admin/artists/socials/{socialId}`       |
| `update_artist_social`  | operator                   | `PATCH /admin/artists/socials/{socialId}`        |
| `review_artist`         | operator                   | `POST /admin/artists/{artistId}/review`          |
| `review_artist_social`  | operator                   | `POST /admin/artists/socials/{socialId}/review`  |

All are enforced by the build-fail coverage tests (`orpc-admin-coverage` / `orpc-auth-coverage`) and the `orpc-naming` verb set. The server layer lives in `apps/web/src/lib/server/artists.ts` (queue + CRUD + review); resolution in `artist-resolution.ts`.

## Follow scope

Artist relationships are identity-only and expose no follow operations or state. Add following only if the target platform supports it under Fluncle's available API tier and the operation has a durable quota budget.

## Remixer credits (the `track_artists.role` column)

The `track_artists` join gains a nullable **`role`** (null = a performer — a lead/featured artist, the only kind v1 had; `'remixer'` the first non-null value). It is DERIVED, never guessed. `deriveRemixerNames` (`track-match.ts`) reads the title's version descriptor — "(Calibre Remix)", "Song - S.P.Y VIP" — strips the version word off it, and returns only the credited `artists` it EXACTLY folds to. So an uncertified remixer with no `artists` row is never invented (there is no row to stamp), and a co-remix ("Calibre & Fabio Remix") resolves to BOTH when both are credited. It is the SINGLE source of truth: the DB stamp and the JSON-LD emit both read it, so the column and the markup agree by construction.

`stampRemixerRoles` (`artists.ts`) stamps `role='remixer'` **fill-empty-only** at every write path that mints a `track_artists` edge — the publish path (`upsertTrackArtists`), the crawler's name-fold link (`linkTracksToArtistEntities`), and the Spotify-anchor step (`connectAnchorArtists`) — and `scripts/backfill-remixer-roles.ts` (deploy `db:backfill`) catches history up, keyset-paged over the titles that plausibly carry a descriptor. Idempotent and non-clobbering.

**Emitted, markup-only.** On the `/log` `MusicRecording` and the graph-page tracklists a remixer renders as a schema.org `contributor` **Role** (the roles.html pattern: `contributor` → a `Role` carrying `roleName: "remixer"` and REPEATING `contributor` for the `MusicGroup`, with its `@id` when the remixer is a known artist). Additive to `byArtist`; the visible page is unchanged (credits are markup-only for now).
